# Architecture Decision Records

This directory holds the ADRs for radio-server. Each ADR is a short, dated record of one significant
decision and its context (ADR-first is the project convention — see [CLAUDE.md](../../CLAUDE.md)). The
table below is the index: what each ADR decided, its current status, and any supersession. It is
maintained by hand — add a row when you add an ADR.

## Read this first — two things the numbering can't tell you

- **`0001` is used twice.** [`0001-cycle-model.md`](0001-cycle-model.md) is a *process/meta* ADR (the
  headless-cycle working model, no `Status` line); [`0001-two-backend-radio-abstraction.md`](0001-two-backend-radio-abstraction.md)
  is the first *technical* ADR. They are kept as-is rather than renumbered, so existing references stay
  valid; a future tidy could renumber the process ADR to `0000`.
- **`0035` and `0036` are missing, and an earlier `0049`–`0053` never existed on disk.** Those numbers
  belonged to a reverted **M17 / mrefd / Codec2** linking arc that was rolled back wholesale (commit
  `f39a750`, "Revert cycles 41-58"). That arc is **dead**. Its live successor is the **Mumble/Murmur
  link** (ADRs 0041→0049). The current `0049` below is a completely different, live ADR that happens to
  reuse the number. **Anything in the tree referencing M17, mrefd, Codec2, libcodec2, or reflectors is
  stale and does not describe the shipped system.**

## Index

| ADR | Title | Status |
|----|----|----|
| [0001](0001-cycle-model.md) | Cycle model (headless working process) | Accepted (process ADR) |
| [0001](0001-two-backend-radio-abstraction.md) | Two-backend radio abstraction, mock-first build | Accepted |
| [0002](0002-radio-protocol-shape.md) | Radio protocol shape (shared vs CAT-only split) | Accepted |
| [0003](0003-session-auth-state-machine.md) | Session / auth state machine | Accepted |
| [0004](0004-dispatch-service-tts.md) | Command dispatch, service interface, TTS shape | Accepted |
| [0005](0005-station-id-scheduling.md) | Station ID scheduling model | Accepted |
| [0006](0006-canonical-audio-format.md) | Canonical audio format, fail-loud frames | Accepted |
| [0007](0007-cw-id-encoder.md) | CW station-ID encoder (Morse, PARIS timing) | Accepted |
| [0008](0008-dtmf-decode-and-framing.md) | DTMF decode + framing grammar | Accepted |
| [0009](0009-piper-tts-engine.md) | PiperTts engine | Accepted |
| [0010](0010-voice-id.md) | VoiceId (phonetic spoken ID) + CW/voice mode | Accepted |
| [0011](0011-api-layer.md) | HTTP/WebSocket API layer, capability gating | Accepted |
| [0012](0012-scan-engine.md) | Software scan engine (clock-driven CAT loop) | Accepted |
| [0013](0013-controller-loop.md) | Controller loop: pure `step()` over `receive()` | Accepted |
| [0014](0014-rx-audio-streaming.md) | RX audio streaming (binary WS, demand pump) | Accepted |
| [0015](0015-activity-detection.md) | Activity detection (RMS + hysteresis + hang) | Accepted |
| [0016](0016-tx-audio-ingest.md) | TX audio ingest (binary WS in, single-talker) | Accepted |
| [0017](0017-duplex-arbiter.md) | Duplex arbiter (half-duplex TX priority) | Accepted |
| [0018](0018-event-log.md) | Event log (subscriber ledger over JSONL) | Accepted |
| [0019](0019-deferred-event-instrumentation.md) | Deferred-event instrumentation | Accepted |
| [0020](0020-audio-recording.md) | Audio recording (RX to WAV, passive tap) | Accepted |
| [0021](0021-recording-safety-and-tx.md) | Recording safety rails + TX recording | Accepted |
| [0022](0022-web-ui-architecture.md) | Web UI architecture (React SPA, same-origin) | Accepted |
| [0023](0023-rx-playback.md) | Live RX playback in the browser | Accepted |
| [0024](0024-tx-mic-capture.md) | Browser TX mic capture (getUserMedia) | Accepted |
| [0025](0025-config-system.md) | Config system (schema-driven TOML + secrets) | Accepted |
| [0026](0026-settings-api.md) | Settings REST API + secret rotation | Accepted |
| [0027](0027-settings-ui.md) | Web UI settings screen | Accepted |
| [0028](0028-async-scan-runner.md) | Async scan runner | Accepted |
| [0029](0029-aioc-baofeng-bringup.md) | AIOC/Baofeng backend bring-up (audio-only PTT) | Accepted |
| [0030](0030-controller-dtmf-buffering.md) | Buffer RX audio before DTMF decode | **Superseded in part by 0038** (buffered survives as `dtmf.decode_mode="buffered"`) |
| [0031](0031-single-capture-reader.md) | One capture reader for browser + controller | Accepted |
| [0032](0032-tx-lead-in.md) | TX lead-in (silence after key-up) | Accepted |
| [0033](0033-fetch-voice-services.md) | LAN-fetch voice services (quote/battery/bible) | Accepted |
| [0034](0034-pluggable-voice-services.md) | Pluggable voice-service architecture | Accepted |
| *0035* | *(gap — reverted M17 arc)* | — |
| *0036* | *(gap — reverted M17 arc)* | — |
| [0037](0037-web-ui-simplification.md) | Web UI simplification & approachability | Accepted |
| [0038](0038-streaming-dtmf-decode.md) | Streaming DTMF decode (one persistent multimon-ng) | Accepted (**supersedes 0030's default**) |
| [0039](0039-https-secure-context.md) | HTTPS secure context (phone live audio) | Accepted |
| [0040](0040-nonblocking-dtmf-feed.md) | Non-blocking DTMF feed off the RX loop | Accepted |
| [0041](0041-mumble-link.md) | Mumble/Murmur link (bridge RF to a channel) | Accepted |
| [0042](0042-multi-mumble-servers.md) | Multiple Mumble servers, DTMF-selectable | Accepted |
| [0043](0043-ungated-link-off.md) | Link-off combo works without a login | Accepted |
| [0044](0044-retro-ham-visual-refresh.md) | Retro-ham visual refresh (Day/Night themes) | Accepted |
| [0045](0045-link-audio-correctness.md) | Link audio correctness (DTMF mute; no busy-latch) | Accepted; **DTMF-mute mechanism superseded by 0049** (busy-latch fix live) |
| [0046](0046-web-session-open.md) | Open the OTA session from the web UI | Accepted |
| [0047](0047-web-restart.md) | Restart the server from the settings screen | Accepted |
| [0048](0048-cockpit-theme-meter-scale-totp-toggle.md) | Cockpit theme, meter dB scale, TOTP toggle | Accepted (extends 0044) |
| [0049](0049-realtime-dtmf-mute-and-yield.md) | Real-time DTMF mute + Mumble→RF keying yield | Accepted (**supersedes 0045's DTMF-mute mechanism**) |
| [0050](0050-web-mumble-client.md) | Web UI as a Mumble client (browser monitor/talk on the linked channel) | Accepted |
| [0051](0051-local-service-plugins.md) | Slim the shipped service set; local service plugins | Accepted (**supersedes 0034's in-tree-only scope**) |
| [0052](0052-freetext-mumble-names-demo-server.md) | Free-text Mumble names, per-entry password, shipped demo server | Accepted (amends 0042) |
| [0053](0053-bootstrap-installer.md) | One-command bootstrap installer (`scripts/install.sh` / `.ps1`) | Accepted |
| [0054](0054-native-dtmf-decode.md) | Native in-process DTMF decode (Goertzel), additive `native` mode | Accepted |
| [0055](0055-auto-decode-mode.md) | Auto-resolve DTMF decode mode by multimon-ng availability (`auto` default) | Accepted |
| [0056](0056-mumble-on-windows.md) | Mumble link on native Windows (tarball pin + vendored `opus.dll`) | Accepted |
| [0057](0057-installer-ships-the-link.md) | Installer ships the Mumble link by default; libopus via a bundled-wheel carrier (retires the vendored DLL) | Accepted |
| [0058](0058-posix-install-script.md) | `install.sh` is POSIX sh so `curl … \| sh` runs on dash; docs↔script contract test | Accepted |
| [0059](0059-plugin-migrations-and-examples.md) | Removed services ship as `examples/`; named migration errors for `[plugins.*]` and `local_services/` ids | Accepted |
| [0060](0060-native-is-the-default.md) | Resolve `auto` to `native` unconditionally (bench-verified); multimon-ng becomes optional | Accepted (**flips 0055's preference; closes 0054's A/B for decode**) |
| [0061](0061-kv4p-uart-backend.md) | kv4p HT UART/KISS backend shape (state reconciler, first real `CatRadio`, real busy line); pure wire codec only this cycle | Accepted |
| [0062](0062-kv4p-transport-handshake.md) | kv4p serial transport: connect by syncing `DeviceState.appliedSequence` (no HELLO dependency); hold DTR/RTS low on open (no reset-to-get-HELLO) | Accepted |
| [0063](0063-kv4p-backend-capabilities-and-units.md) | kv4p `Kv4pHt` backend: complete-state reconcile; advertise `SCAN` (software sweep) but omit `SET_CHANNEL`; unit mapping (Hz↔MHz, CTCSS Hz↔index, mode↔bandwidth) fails loud | Accepted |
| [0064](0064-kv4p-firmware-repin-shipped.md) | Re-pin the kv4p firmware reference to shipped v2.0.0.1 (`3f0e809`); the audio command ID (`0x07` vs `0x0C`), not `FIRMWARE_VER`, discriminates the two v17 builds | Accepted |
| [0065](0065-kv4p-opus-codec.md) | kv4p audio edge is Opus (48 kHz, 40 ms frames), replacing the dead IMA-ADPCM path | Accepted |
| [0066](0066-kv4p-connect-running-board.md) | kv4p `connect()` re-founded on shipped firmware: passive-first → elicit-with-retransmit → restore; de-clobber the NVS (no zero-write on connect/close) | Accepted |
| [0067](0067-extras-taxonomy.md) | Extras taxonomy: factor leaves (`serial`/`soundcard`/`opus`) and compose backends (`hardware`/`kv4p`/`mumble`) so a node installs only what it uses; `opuslib` named explicitly | Accepted |
| [0068](0068-kv4p-bringup-detections-and-docs.md) | kv4p bring-up: doctor detections for pre-KISS firmware (`de ad be ef` sniff) and band-mismatch (HELLO vs `kv4p.module_type`), shipped with the user docs (new `kv4p-setup.md`, fork by radio) | Accepted |
| [0069](0069-kv4p-tx-bringup.md) | kv4p TX bring-up: doctor TX telemetry rig (`TxStats`, key-up latency, `--tx-lead` sweep); fixes doctor to read `radio.toml`; first bench numbers (`tx_lead` 0.2→**0.5**, ~230 B/frame, window blocks are backpressure) | Accepted |
| [0070](0070-kv4p-rx-sample-rate-correction.md) | kv4p RX sample-rate correction: shipped firmware clocks the RX ADC ~2% fast (`rxAudio.h` `*1.02`) but labels it 48 kHz — broke DTMF and drifts every continuous consumer. Resample the true device rate → 48 kHz at the decode edge (soxr HQ); `kv4p.sample_rate_correction` knob + doctor `--rx-level` rate readout; connect timeout 2→10 s | Accepted |
| [0071](0071-kv4p-rx-dtmf-capture-analysis.md) | kv4p DTMF still fails after 0070: stop analysing, capture it. doctor `--rx-capture` (RX→WAV) + `--analyze-wav` read the DTMF tones straight out of the audio via FFT (independent of GoertzelStream) and return a verdict — clipping (firmware 16× gain) vs off-frequency vs clean-so-decode-wiring; tightens the `--rx-level` correction verdict to 0.2% | Accepted |
| [0072](0072-kv4p-dtmf-energy-floor.md) | kv4p DTMF cause found by reproducing the real capture through the live decoder: the native `NATIVE_ENERGY_FLOOR` (0.02) is ~10× too high for received audio (measured low-tone power ~0.012), so every block read as silence. Lower it to **0.002** (talk-off stays with the scale-invariant ratio gates); adds received-level + frame-size-invariance + talk-off regressions; realigns the 0070 offset regression to a received level | Accepted |
| [0073](0073-radio-holder-seam.md) | A `RadioHolder` seam (api/holder.py): one object owns the active radio (`.radio`, built via the extracted `build_radio(settings)`) and the lifecycle of the radio-bound pipeline — `start()` constructs the `RxPump`/`ScanRunner` against it, `stop()` tears them down (drop PTT if the arbiter holds TX, stop scan, halt pump, reap controller, close radio) idempotently. Pure behaviour-preserving refactor; the keystone for later in-app backend switching (`stop(); rebuild; start()`) | Accepted |
| [0074](0074-multi-backend-config.md) | radio.toml describes more than one backend: presence-based configured set (`[<backend>]` block present + active `server.backend`), each validated at load so a broken *inactive* switch target fails at startup, not at select time. New light `api/backend_config.py` (pure `validate_backend_config`/`validate_configured_backends`/`backend_kwargs`/`configured_backends` enumeration for the next cycle's select endpoint + UI); `doctor` validates the selected backend; no schema change (example/canary unmoved). No switching yet | Accepted |
| [0075](0075-configurable-dtmf-reverse-twist.md) | Make the native DTMF decoder's reverse-twist tolerance configurable via `audio.dtmf_reverse_twist_db` (default 4.0 = today's `NATIVE_REVERSE_TWIST_DB`, threaded into `GoertzelStream`). Bench finding: the UV-5R Mini sends its low group ~6.4 dB hotter than the high, tripping the −4 dB gate so it decodes nothing while a UV-5R decodes fine. Opt-in (bump to ~10) so compliant radios keep the tight, talk-off-safe default; dominance + second-harmonic gates carry talk-off, so the wider gate stays talk-off-clean. Reverse twist only, global (not per-backend) | Accepted |
| [0076](0076-live-backend-switch.md) | The live backend switch: `RadioHolder.rebuild(new_settings)` (atomic under a lock; stop → construct via `radio_factory` → start, rebuilding the controller via a `controller_factory`; **rolls back** to the previous working backend if the target fails to open, so a switch never bricks the server) + `POST /radio/select` (validates against `configured_backends`, 409 unconfigured, 503 on rollback) + `GET /radio/backends` (current + configured list). The select handler `nonlocal`-rebinds the route closures' `radio`/`rx_pump`/`scan_runner`/`controller` (the ADR 0073-deferred "routes follow the live radio" step), re-honors RX demand, re-emits a new `capabilities` event + a status snapshot, and persists `server.backend` through the schema. Endpoint + API only; the UI dropdown is the next cycle. No schema change | Accepted |
| [0077](0077-backend-selector-ui.md) | The backend selector in the web control panel: a `BackendPanel` dropdown over `GET /radio/backends` + `POST /radio/select` (self-hides with <2 backends; tracks the live active radio so a 503 snaps back and names the radio you're still on; "Switching…"/disabled in flight; warns switching drops PTT mid-transmit). The payoff — **reactive re-greying without a reconnect**: `reduceStatus` folds the re-emitted `capabilities` event into `state.caps` and `ControlPanel` prefers it over the one-shot login prop, so the CAT tuning/scan cards appear for the kv4p and vanish for the AIOC live; the additive `disabledCaps` clears on a switch. Bootstraps Vitest + Testing Library (the frontend had no JS test runner). No server/endpoint change | Accepted |
| [0078](0078-preserve-extra-channel-on-settings-patch.md) | Preserve the local-plugin (`[plugins.*]`) extra channel (ADR 0051) across a settings patch: `POST /radio/select` (ADR 0076) and `PATCH /settings` both rebuilt `Settings` from **schema keys only** and called `resolve_settings(...)` with no `extra=`, so a live backend switch (or any save) silently dropped every local plugin's config off `app.state.settings` — the rebuilt controller then gated weather/astronomy/quote/battery/bible **out of the service catalog** until a restart (the on-disk file was untouched, so a restart restored them). Add a public `Settings.extras()` whole-channel accessor and thread `extra=current.extras()` through both patch sites. Runtime-only; no schema change; the UI was faithful (the catalog really had shrunk) | Accepted |
| [0079](0079-persist-auth-session-across-backend-switch.md) | Persist the over-RF auth `Session` across a live backend switch (ADR 0076) — the runtime-state sibling of ADR 0078's config fix. `build_controller` minted a fresh `Session()` on every rebuild, so `holder.rebuild` logged out an authenticated operator the instant the station switched radios. The auth session belongs to the operator at the station, not the per-radio controller: give `build_controller` a `session=` param and have `build_app`'s `controller_factory` closure own **one** `Session` and inject it into every (re)build. Falls out correctly: the inactivity clock carries (neither reset nor extended), a mid-entry DTMF code is discarded (per-controller framer) while an open session stays open, and the rebuilt `StationId` re-arms erring toward ID-ing (Part 97). No schema change; "gated, not secure" makes persisting across a local switch fine | Accepted |
| [0080](0080-kv4p-tx-gain.md) | Give the kv4p a TX audio-level control, `kv4p.tx_gain` (float, default `1.0`). Bench symptom: kv4p announcements/voice are overmodulated — the firmware TX path applies no boost and the backend encodes to Opus with no attenuation, so near-full-scale TTS/CW over-deviates the SA818. The AIOC tames identical audio with `alsamixer`'s playback slider; the kv4p has no sound card and no such stage. Add a plain multiplier applied to the canonical int16 samples **before the Opus encoder** in `TxAudioEncoder` — the single choke point every TX byte (streaming + one-shot + lead-in) flows through — so it scales everything transmitted, not one source. Clamps to full-scale int16 (a `>1.0` value clamps, never wraps); `1.0` is an exact no-op. Threaded through both the initial build and the ADR 0076 rebuild like the other kv4p knobs; marked verify-on-bench (guardrail 1, ~`0.5` starting point). No AIOC change (OS mixer owns its TX level), no limiter/AGC, no RX change | Accepted |
| [0089](0089-dstar-folded-shared-dongle.md) | **D-STAR folded into the real radios: one shared DV Dongle, crossband + browser together** — the ADR 0088 browser reflector seam moved off its standalone MockRadio node onto the two real instances (AIOC 8090, kv4p 8091), so linking a reflector from an instance's own HTTPS web UI crossbands it to **that** radio's frequency **and** the browser, and you talk onto the reflector from **either** the PC mic **or** that FM radio. Four changes: (1) a **TX-owner latch** (`_tx_source` `rf`/`op`) so the crossband RF pump and the browser mic both run at once — whichever opens the over owns it, the other drops, no interleaving into one DSRP session — removing ADR 0088's `rx_to_reflector = not operator_tx` mutual-exclusion; (2) the **DV Dongle is acquired on link, released on unlink** — `DStarLinkManager.connect`→`bridge.start()` creates the vocoder from a factory opening the FTDI port **exclusively** (`pyserial exclusive=True`), `disconnect`→`stop()` closes it; the OS serial exclusive lock is the sole cross-process arbiter, so the two instances share one dongle and a second connect returns a clean **503 "in use by the other radio"**; (3) **browser audio follows link state** — `dstarMode` derived from `state.dstar.active`, not a config flag (linked → `/audio/dstar/{rx,tx}`, unlinked → RF), and `dstar.operator_tx` **removed**; (4) an **activity log** — inbound MYCALL (parsed but discarded before) + our own overs fire an `on_activity` callback → an `activity` WS event + a 30-entry `/dstar/status` ring + a `DStarActivityLog` card. Deploy: `[dstar]` on both instances (identical module A / port 20012), the 8092 node disabled, HTTPS inherited, **no gateway change** (module A already exists; DVAP untouched). The **DVAP tab** (its own gateway module, needs the remote-control interface) is the next cycle. canary 75→74 | Accepted |
| [0090](0090-tx-timeout-timer.md) | **Transmitter time-out timer: a hard cap on continuous key, on every path.** The ADR 0089 folded crossband could get **stuck keying the transmitter** (dead air on the air) with no path to drop PTT but a process kill — because every existing safeguard (`tx.idle_timeout`, D-STAR/Mumble `tx_hang`) is a *silence* timeout that resets per frame, so a **continuous** key is uncapped. New `TotRadio` (`tx/tot.py`): a `Radio`-protocol decorator wrapping the active backend at `build_radio` — the one composition root every keyer (browser TX, D-STAR/Mumble bridges, DTMF services, station ID, REST `/ptt`/`/transmit`) and every backend funnels through, on build and on live swap. Delegates the whole surface via `__getattr__`; intercepts only `ptt`/`transmit`. The watchdog is armed **once on key-up** (never reset per frame — a hard cap, not an idle reset) and **fires on its own `threading.Timer` thread**, so it force-drops PTT even when the caller is parked in a blocking decode (the exact stuck-key mode). A streaming time-out **latches TOT-locked** (drops further `transmit()` until an explicit re-key — stops a self-keying SignaLink); a one-shot overrun does not latch. Config `tx.tot` (seconds; default **180**, `0` disables) beside `tx.idle_timeout`; canary 74→75. The independent safety net beneath the ADR 0091 crossband-close fix. Verified on an injected fake timer (`ptt_log == [True, False]`), never by live-keying | Accepted |
| [0091](0091-dstar-crossband-safety.md) | **D-STAR crossband safety: the over always closes, PTT always drops, the reflector is signal-gated.** The ADR 0089 folded crossband could get **stuck keying the transmitter** — inbound DATA kept arriving (queue never idled) while decodes stopped feeding RF, so `_reflector_to_rf`'s only close paths (end-bit / queue-idle) never fired; and `_teardown` relied on that loop's `finally`, which can't run while the task is **parked in a blocking vocoder decode** (cancel undeliverable, join hangs, and the vocoder that would unblock it was closed *after* the join). Only a process kill freed PTT. Five `dstar/bridge.py` fixes: (1) `_teardown` closes the vocoder **first** (unblocking a parked `_exchange`), force-drops PTT **directly** via new `_force_unkey`, and **bounds** each task join; (2) an **independent RX watchdog** closes the over when the keying session is idle past `tx_hang` (`TxSession.idle_elapsed`) even under continuous inbound; (3) a `write_timeout` on the DV Dongle serial port so a stuck write can't park forever; (4) `_rf_to_reflector` is **signal-gated by a per-link `AudioLevelGate`** (reusing the `audio.vad_*` thresholds, injected by `build_app`) so the crossband keys the reflector only on real RF audio — independent of the global `audio.squelch`; (5) `TxSlot` release only if actually held (never freeing a browser talker's slot) + a `_reap_stale_tx` backstop for a leaked "op" over. No new config, no canary change (gate reuses `audio.vad_*`; DI seam stays ungated). Sits above the ADR 0090 TOT. Verified on fakes (idle-watchdog close under failing decodes; `stop()` during a blocked decode drops PTT and returns; gate keys only on signal; slot accounting; op-over reap) — 1223 passed; re-enable gated on a bench proof (dummy-load reflector→RF + `doctor --dstar-browser-echo`) | Accepted |
| [0092](0092-dstar-parked-decode-ptt-safety.md) | **The parked decode can no longer hold PTT: an independent watchdog, a re-key guard, an unconditional unkey.** ADR 0091 was verified on fakes and merged — then the first **dummy-load** reflector→RF test on the real DV Dongle stuck-keyed anyway (`mode=rx`, PTT held ~40 s to the TOT). Two hardware realities the FakeVocoder/MockRadio couldn't reproduce: (1) ADR 0091's idle watchdog lived at the **top of the `_reflector_to_rf` loop**, but that loop `await`s each decode in the single-worker executor and a **wedged DV Dongle decode parks the loop there** — so the loop-top check never runs (`asyncio.wait_for` around `run_in_executor` doesn't help: it awaits the uncancellable thread to finish anyway); (2) `unlink` returned but PTT stayed up — `TxSession.close()` transmits a Part-97 sign-off **ID before** `ptt(False)`, that transmit **raised** on the wedged backend, and `_force_unkey`'s `suppress` swallowed it, skipping the unkey; and a decode resuming after the over closed could **re-key**. Four fixes, no config/canary change: (1) the PTT-safety watchdog runs as its **own `_rx_watchdog` task** (only ever `await`s `sleep`, never the executor) so it drops PTT even while the decode loop is parked; (2) `_play_ambe` drops its frame if `mode != "rx"` so a resumed decode can't re-key a closed over; (3) `TxSession.close()` wraps the sign-off ID `transmit` so a raise can never skip the `ptt(False)` beneath it — hardening **every** streaming keyer; (4) `_force_unkey` drops `radio.ptt(False)` **directly** as a path-independent backstop. Verified on fakes that now model the failure (a `_BlockingVocoder` parking in the executor proves the over closes on its own with no teardown; a late decode proves no re-key; a spy radio raising on the sign-off transmit proves close() still unkeys); re-enable still gated on the dummy-load proof that surfaced it | Accepted |
| [0093](0093-aioc-panic-unkey.md) | **The AIOC transmitter can never be stranded keyed: drop the line first, unconditionally, atomically.** After ADR 0090/0091/0092 hardened the D-STAR *bridge*, the crossband STILL left the AIOC transmitting dead air on real hardware — only a process kill (closing the serial port) dropped PTT. A controlled **carrier key-test** (bare `pyserial` DTR toggle on a dummy load, operator watching, no audio/dongle/bridge) isolated it: **`dtr=False` with the port open cleanly unkeys this AIOC** — so the hardware is fine and the stuck-key is a SOFTWARE failure to reach `dtr=False`. Three `AiocBaofeng` bugs, all in the stranding class: (1) `ptt(False)` was guarded on `self._keyed`, so a desynced flag made the watchdog's / teardown's / REST `/ptt off`'s safety lever **no-op** (matches the journal: `/ptt off`→200, `status.transmitting`=False, carrier still up); (2) `_key_on` asserted the line THEN wrote the TX lead-in — a lead-in write that raised propagated with the line up but `_keyed` never set True, stranding it under (1); (3) `_key_off` **drained the stream before dropping the line**, so a `stop()` that blocks/raises on an xrun'd stream (the DV Dongle write-timeout wedge was starving it) kept the line asserted. Fix: a single unconditional `_drop_line()` primitive (bare `setattr(line,False)`, never guarded, can't block); `_key_off` drops the line FIRST then tears down the stream best-effort (the RF-safety inversion of ADR 0029's drain-then-drop — costs a few ms of tail clip); `_key_on` atomic (drop line on any post-assert failure); `ptt(False)` unconditional (guard removed). Every keyer benefits (browser/Mumble/D-STAR/station-ID/`/transmit`). Verified on fakes that now model the failure (lead-in write raises → not stranded; stop() raises → line still dropped; line proven low BEFORE the stream is stopped; ptt(False) forces the line low when `_keyed` desynced) — 1230 passed. Does NOT re-enable D-STAR; crossband re-enable gated on a JOINT dummy-load re-proof; the dongle write-timeout wedge is a separate reliability follow-up | Accepted |
| [0094](0094-dvdongle-sleep-recovery.md) | **The DV Dongle recovers itself from the idle-sleep wedge: close+reopen+re-handshake on a frame timeout.** The crossband stuck-keys were *triggered* by the dongle wedging (`SerialTimeoutException: Write timeout` every decode); ADR 0092/0093 closed the safety half (PTT always drops), this closes the reliability half. A no-RF bench characterisation pinned the wedge: **sustained decode is rock-solid** (3000 back-to-back, 0 slow, ~36/s), but after ~2-3 s idle the **AMBE2000 sleeps and does not self-wake** — the next frame times out (`VocoderTimeout`) and, as the caller keeps feeding an unresponsive chip, the FTDI TX buffer fills and writes start timing out too (the incident signature). **A full close+reopen+re-handshake recovers it** (bench-proven). The ADR 0088 keepalive already prevents idle-sleep *while idle*; the gap is an idle stretch *during* an over (a stall between reflector talkers in `rx` mode, keepalive off). Fix: on `VocoderTimeout`, `_exchange` calls a new `_recover()` **once** and retries the frame (second failure propagates). `_recover` rebuilds the transport + session under the (now reentrant) io lock — signals stop, closes the old port, and **joins the old reader before reassigning** anything the reader reads by reference, then reopens + re-handshakes, retrying the flaky first open a few times exactly like cold bring-up; a dead dongle raises `VocoderUnavailable`. `_exchange` split into the recover-and-retry wrapper + the `_exchange_once` primitive; io lock → `RLock`. No public surface change. Verified on fakes modelling the wedge (handshakes-but-never-answers reopened healthy; flaky reopen; permanently wedged still propagates; un-openable → unavailable); the trigger + the close+reopen recovery are both bench-proven on the real dongle — 1230 passed. Does not change the D-STAR posture | Accepted |
| [0095](0095-gateway-remote-control-client.md) | **An ircDDBGateway remote-control client, landed as an isolated seam (unwired)** — PR 1 of 2 for DVAP support. radio-server is gateway module A over DSRP and links reflectors with in-band URCALL, so it can only steer its own module and only ever knows *believed* link state. The DVAP modules (B = 441.600, C = 441.000) are separate `dstarrepeater` endpoints it can't see; the gateway's **remote-control** UDP interface (compiled into the binary, currently off) links/unlinks *any* module and reports *confirmed* link state. This PR adds only the protocol client, consuming nothing — the ADR 0086/0087 discipline of proving a new wire protocol in isolation before it can touch the running radios. `dstar/remote_codec.py` (pure codec, the `dsrp.py` analogue): 3-byte ASCII tags, little-endian ints (wx `SWAP_ON_BE`); build `LIN` login / `SHA`+`SHA256(random_bytes ‖ password)` / `LNK`/`UNL`/`GCS`/`GRP`, parse (never raises) `RND`/`ACK`/`NAK`/`CAL`/`RPT` (the confirmed-state readback); `Reconnect`/`Protocol`/`Direction` enums from g4klx declared order. `dstar/remote_client.py` (transport seam, the `client.py` analogue): a `RemoteControlClient` Protocol, a `MockRemoteControlClient` that models a tiny gateway (link map, `fail_auth`), and a `UdpRemoteControlClient` (lazy cached `LIN→RND→SHA→ACK/NAK` login, bounded timeout + retries). Reimplemented clean from public g4klx `RemoteProtocolHandler.{h,cpp}` read as a spec. Nothing wired: no config/route/startup change. Verified on fakes (19 tests: codec byte-layouts, Mock link/status/unlink round-trip + fail-auth, Udp login handshake asserting the exact SHA over the injected random, NAK→auth-error, silence→timeout, close→logout) — 1253 passed; the LE-int convention, the SHA input and port 10022 are marked verify-against-the-live-gateway (guardrail 1). Next: the DVAP control surface (config + manager + `/dvap/*` + web tab) + the gateway deploy step (enable remote-control, stand up DVAP #2 as module C) | Accepted |
| [0096](0096-dvap-control-surface.md) | **DVAP control surface: config + a cached manager + `/dvap/*` over the remote-control client** — wires ADR 0095's client into the app so radio-server links/unlinks/monitors the DVAP gateway modules (B = 441.600, C = 441.000). Unlike D-STAR module A (a bridge + DV Dongle + PTT + *believed* state), the DVAP path is pure control-plane: no vocoder, no bridge, no PTT, and **confirmed** link state read over the gateway remote-control interface. Config: `[dvap]` host/port scalars + an array-of-tables `[[dvap.modules]]` (module letter / label / frequency_hz) modelled exactly on `[[mumble.servers]]` (`load_dvap_modules` + `resolve_dvap_modules`, fail-loud validation), with the remote-control password a secret (`dvap_remote_password` in radio-secrets.toml). `DvapManager` **caches** confirmed state — `status()` is I/O-free so `GET /status` never blocks; `refresh()` does the bounded UDP round-trips and marks an unanswered module `reachable:false` rather than failing the snapshot. Routes `/dvap/{status,link,unlink}` run the blocking client off the loop via `asyncio.to_thread`, publish the confirmed post-refresh block as a `dvap` WS event, and map 404 (unknown module) / 422 (bad reflector) / 503 (unconfigured or gateway-unreachable); the block embeds in `/status` and self-nulls when unconfigured (the config-driven-visibility pattern the web card keys off). Off by default (no `[[dvap.modules]]` → no manager, no socket); D-STAR module A untouched. Verified on fakes (`MockRemoteControlClient` modelling a tiny gateway): manager round-trip + cache + error handling, `resolve_dvap_modules` validation, and the routes' full flow / error codes / graceful degrade — 1278 passed. The web DvapPanel card is a follow-up PR; the gateway remote-control enable + DVAP-node standup is an operator deploy step | Accepted |
| [0088](0088-dstar-reflector-browser.md) | **D-STAR reflector control + browser talk/listen** — a hotspot you drive from a web page, extending the ADR 0087 link to the stated goal: pick a reflector on a screen, hit connect, talk/listen with the computer mic/speakers, no D-STAR radio. The D-STAR analogue of ADR 0050 (web-UI-as-Mumble-client). On `DStarBridge`: `send_link_command` (a synchronous, idle-gated URCALL burst — `NULL_AMBE` only, no vocoder — so it can't corrupt or be corrupted by an in-flight over) links/unlinks reflectors the standard D-STAR way; `send_operator_audio`/`end_operator_over` is the browser-mic TX seam (the `MumbleBridge.send_operator_audio` twin). A new `DStarLinkManager` parses `"REF001 C"` → URCALL `REF001CL` (REF/XRF/DCS/XLX differ only by prefix), tracks **believed** link state (the gateway gives no readback, surfaced honestly), and fires a `dstar` WS event. API: `WS /audio/dstar/{rx,tx}` (a dedicated `dstar_rx_hub` + a distinct `dstar_talk_slot`) and `POST /dstar/{link,unlink}` + `GET /dstar/status` + a `/status` block — the `/audio/mumble/*` + `/link` twins. Web UI: a `DStarPanel` reflector picker (free-text + presets) and a `dstarMode` that points Monitor/Transmit at the D-STAR endpoints. One new key `dstar.operator_tx` (browser-operator posture; `rx_to_reflector` off); `dstar.reflector` becomes the boot auto-link. `doctor --dstar-browser-echo` drives the real `send_operator_audio`→Echo→decode→`dstar_rx_hub` round trip. **Live-gateway-proven:** a header-only URCALL linked **REF030 C** (`D-Plus link established`); the browser round trip tracked the staircase (corr 0.88); the two production instances + the DVAP stayed at baseline PIDs (`NRestarts=0`). Found+fixed the AMBE2000 idle-sleep bug (chip goes unresponsive after ~2–3 s idle, losing the first inbound over) with an idle-gated keepalive decode. canary 74→75 | Accepted |
| [0087](0087-dstar-link.md) | The **D-STAR link**: radio-server as a homebrew-repeater endpoint on a G4KLX **ircDDBGateway**, the first live consumer of the ADR 0086 vocoder. New `radio_server/dstar/` package (sibling to the Mumble link): a pure I/O-free **DSRP** wire codec (`dsrp.py` — register/poll/header/data packets) + the 41-byte radio header with **CRC-16/X-25** (`header.py`), a `GatewayClient` seam with a `MockGatewayClient` + a `UdpGatewayClient` (socket, daemon reader, register/poll timers), and a **half-duplex bridge** (`bridge.py`) that keys reflector audio onto RF via the shared `TxSession`/`TxSlot`/`station_id` and encodes RF back to the reflector through the vocoder. A **mode latch (IDLE/RX/TX)** uses the single AMBE2000 chip one direction at a time — matching D-STAR's one-talker reality and the ADR 0086 no-interleave rule. The 48k⇄8k resample lives at the bridge edge, never in the vocoder. Protocol read from g4klx `RepeaterProtocolHandler.cpp` (GPL-2) as a **spec**, reimplemented clean. Config `[dstar]` group **off by default** (inert until `dstar.callsign` is set); wired in `create_app`/`build_app` behind that gate so existing deployments are untouched. Acceptance is a `doctor --dstar-echo` self-test round-tripping audio through the gateway's built-in **Echo** unit (a deterministic local loopback, no remote reflector/registration), reusing the vocoder staircase pitch metric. canary 66→74 | Accepted |
| [0086](0086-vocoder-seam.md) | The **vocoder seam**: PCM ⇄ AMBE, isolated and unwired from the live app. A future digital-voice/D-STAR path needs a vocoder (8 kHz speech PCM → compressed voice frame and back); it lands **alone**, before any framing/socket/bridge plumbing, because the vocoder is the one piece that can't be proven by inspection (same posture the reverted Codec2 seam took). New `radio_server/vocoder/` package: a device-independent `Vocoder` `Protocol` (`encode(pcm)->ambe` / `decode(ambe)->pcm`, one 20 ms frame) so a later **AMBE3000** or software codec (**Griffin**) drops in behind it, plus one concrete `DVDongleVocoder` driving the DV Dongle's DVSI **AMBE2000** over its FTDI link. **The seam operates at the native 8 kHz frame, not the app's 48 kHz canonical audio** — every real vocoder is 8 kHz, and the 48k⇄8k resample belongs at the future consuming backend's edge (a deliberate departure from the Codec2 seam, which resampled internally). Pure I/O-free wire codec (`frames.py`, DV Dongle 13-bit-length/3-bit-type framing + the verbatim AMBE2000 D-STAR config block) split from a kv4p-style serial transport (lazy pyserial behind `hardware`, daemon reader thread, `Condition` reply hand-off, idempotent `close`); v1 is a synchronous query/reply per frame. Protocol read from g4klx/DummyRepeater (GPL-2) as a **spec** and reimplemented clean (no ported code; AMBE vocoding stays on the licensed chip — no codec/patent exposure in-tree). Acceptance is a `doctor --vocoder-loopback` self-test (encode→decode→WAV + a lossy round-trip metric) proven on the actual dongle. **No config schema, no canary bump, no `radio.toml.example` change** (unwired); rides the existing `serial`/`hardware` extra | Accepted |
| [0085](0085-mumble-rx-guard.md) | A post-transmit **RX guard** that keeps the TX→RX turnaround transient off Mumble. AIOC symptom: talk from the phone (Mumble→RF), release PTT, the radio drops — then the phone hears a ~0.25 s buzz. It's the UV-5R receiver recovering at the turnaround (FM hash before squelch settles), captured by the AIOC card and relayed because `audio.squelch="off"` passes everything; kv4p is immune (SA818 hardware squelch keeps it off the wire). The arbiter resumes RX the instant TX releases with no guard — the very timing its docstring leaves unmodeled (ADR 0017, guardrail 1). Fix: on the arbiter's **TX→RX edge** (source-agnostic — a browser talker's release arms it too, not just the Mumble bridge), arm a short window that suppresses only the **RF→Mumble relay** (browser Listen + the recorder untouched — recording never loses audio). Reuses the ADR 0049 `DtmfMuteGate` latch, app-scoped and injected into the bridge; a new `rx_guarded` counter in `tx_stats`. Config `mumble.rx_guard_seconds` (default 0.4, advanced, verify-on-bench, **0 disables**). Backend-agnostic / kv4p-benign. Bundled UI tidy: all settings-screen collapsibles now default collapsed. canary 65→66 | Accepted |
| [0084](0084-kv4p-rx-continuous-silence.md) | Make the kv4p RECEIVE path a continuous stream (the RX mirror of the ADR 0082 TX pacer). Over Mumble, the tail of a *received* transmission looped ("Max Headroom") when a signal ended — not on the AIOC. The AIOC reads a continuous capture stream so `receive()` always returns full-length audio (silence between transmissions); the frame-push kv4p returned an **empty** frame on idle, which the shared `RxPump` skips *before* the activity gate — so the VAD gate's hang never published the trailing taper and the RF→Mumble feed stopped abruptly. Fix: on an idle-poll timeout `receive()` returns a full-length canonical **silence** frame (1920 zeros) instead of empty, so the stream stays continuous and every consumer (gate/recording/DTMF) treats kv4p identically to the AIOC — the hang tapers the silence then closes. `doctor measure_rx_levels` now skips all-zero frames so the ADR-0070 `--rx-level` calibration still measures real audio only. No AIOC/`tx_hang`/schema change | Accepted |
| [0083](0083-fixed-login-code.md) | A fixed over-RF login code option (opt-in, non-default) plus a settings-screen tidy. Auth becomes a derived mode — off / TOTP / fixed — via a new non-secret `auth.fixed_code` toggle (default false) beside the unchanged `auth.totp_enabled` gate. A `FixedCodeVerifier` (constant-time compare, **no single-use burn** — a fixed code is reused, so it's replayable by anyone who overhears it: the documented downgrade) drops into `AuthGate` in place of `TotpVerifier`, selected in `build_controller`; `build_app`'s controller gate builds in fixed mode when a code is set. The code is a credential on the secrets channel (`fixed_code` / `RADIO_FIXED_CODE`), never in `radio.toml`, set write-only via `POST /settings/secrets/fixed-code` (6-digit-validated); `GET /auth/totp` reports `{fixed:true}` and never echoes it; the masthead shows a locked chip. Warning surfaced in the toggle description and the Secrets control. Also: `MumbleServersPanel` now folds like the other settings groups (a `.settings-group` `<details>`). Non-breaking; canary 64→65 | Accepted |
| [0082](0082-kv4p-keyed-idle-tx-pacer.md) | Keep the kv4p transmitter fed while keyed-but-idle so the firmware never underruns and loops stale audio (the "Max Headroom" repeat over Mumble). The AIOC's continuous sounddevice output stream emits silence between writes; the frame-push kv4p sends nothing while keyed-idle, so when the Mumble bridge holds PTT across a `tx_hang` pause the SA818's TX buffer starves and loops its last ~0.5 s. Add a `_TxPacer` (new `backends/kv4p/pacer.py`): during a **held** key a daemon thread sends exactly one frame per ~40 ms slot — real audio buffered by `transmit()`, encoded silence (the key-up lead-in path; `tx_gain`-invariant) otherwise — the single coherent sender, so no window race or doubled frame. `transmit()` while keyed enqueues to a bounded drop-oldest jitter buffer instead of encoding inline; key-down stops the pacer, flushes the tail, drops PTT. One-shot `transmit()` (station ID, TTS, `/transmit`) is untouched — it never holds the key idle. Silence stays within the flow-control window (WINDOW_UPDATE refunds); moving writes off the event loop removes the old ~2 s backpressure stall (the buffer bound is now the limiter). No AIOC change, no `tx_hang` change, no schema change | Accepted |
| [0081](0081-remove-decorative-frequency-dial.md) | Remove the decorative frequency-dial scale (`DialScale`) from the control panel's face. It was a purely visual (`aria-hidden`) 144–148 MHz ruler + needle from the ADR 0044 retro refresh — hard-coded to the 2 m band, duplicating the authoritative `FreqLcd` numeric readout above it, adding clutter without function. Delete the component, its single mount, and its `.dial*` CSS; keep the `showDial` flag (still gates the LCD) and the shared `--tick`/`--ticksoft`/`--red` vars (used elsewhere). No server/endpoint/capability/setting change; no test change (nothing referenced the dial) | Accepted |
| [0115](0115-channel-presets.md) | **Channel presets: server-side named tuning entries.** Radio channels live host-side, not in radio memory (ADR 0111; the `CatRadio` backends omit `SET_CHANNEL`) — a "channel" is a `{frequency, tone?, mode}` **preset** the operator names in a top-level `[[presets]]` array-of-tables and applies through the existing `set_frequency`/`set_tone`/`set_mode` surface. v1 is **simplex** (repeater-output monitoring from the desk); split/offset is a named follow-on that would touch the `CatRadio` interface itself. New `radio_server/presets.py`: a frozen `Preset`, a self-contained EIA 38-tone `CTCSS_TONES` set + `FM`/`NFM` modes, `resolve_presets` (fail-loud like `resolve_mumble_entries` — bad tone / dup name (case-insensitive) / ≤0 frequency / unknown field / bad mode stop startup), the pure `split_preset_fields` (honoured/skipped in the `Capability` vocabulary), and `apply_preset` (anchor-first, capability-gated per field). Config rides the `[[mumble.servers]]` recipe: `load_presets` + a `_flatten` top-level peel-off; `save_settings` leaves the hand-authored block untouched; `render_example` ships commented examples. API: `GET /presets` (lists + per-backend honoured fields) and `POST /presets/apply` (`{name}` → `{applied, skipped, status}`, pushes a `status` event) with interlocks from existing discipline — **404** unknown name, **501** on an audio-only backend (like `/frequency`), **409** refused mid-TX (the arbiter's refuse-don't-key posture, ADR 0017), stop-scan-first (ADR 0028), **422** on an out-of-band frequency; no parallel state store (reuses `status_event`, ADR 0076/0077). No UI (next cycle), no new deps, backend suites untouched | Accepted |
| [0116](0116-channel-presets-web-ui.md) | **Channel presets: the web UI.** The browser surface for ADR 0115 — a **Channels** card (`web/src/components/PresetControl.jsx`) where tapping a named channel tunes the radio via `POST /presets/apply`. No server change, no new deps. Patterned on `DStarPanel` (tap-a-preset button row, self-hide, `role="alert"` errors) with the two existing hide gates and **no third state**: the `SET_FREQUENCY` capability gate is a conditional mount (`{hasCap("set_frequency") && …}`, the same predicate as `showDial`), and config-absence self-hides (empty `GET /presets` → `return null`, like DStarPanel/LinkPanel/DvapPanel). The active-channel highlight is **derived, never stored** — `activePresetName(presets, state, hasCap)` matches a preset's honoured fields against the live `state.frequency`/`tone`/`mode`, comparing tone/mode only where the backend advertises them, highlighting only on an **exactly-one** match (ambiguity → none; a manual tune-away clears it naturally). Skipped honoured fields from the apply response show as a `notice` (non-silent); a mid-TX **409** / out-of-band **422** / **404** surface as the `role="alert"` error a `/frequency` failure gets, a **501** greys via `onUnsupported`. Reactive across browsers for free — apply publishes a `status_event` (ADR 0076/0077), so every open tab's readout + highlight update with no polling. `api.js` gains `presets`/`applyPreset`; new `PresetControl.test.jsx` (Vitest) + an extended `ControlPanel.test.jsx` gating test; Python suites untouched. Out of scope: preset editing in the UI (file stays source of truth), split/offset | Accepted |
| [0117](0117-uvk5-tx-timeout-wiring.md) | **Wiring the UV-K5 into the transmitter time-out (the stuck-key gate).** ADR 0112 flagged that the docked UV-K6 full-control loop has no device-side TOT (unlike kv4p's firmware `RUNAWAY_TX_SEC ≈ 200 s` or a UV-5R's TOT menu) — the server is the only protection. Reconnaissance-first: the server-side TOT **already exists** (`TotRadio`, ADR 0090, wrapping every backend at `build_radio`), so this is **extension/wiring, not invention**. Placement **follows** ADR 0090's `Radio`-boundary decorator, not the arbiter (`RadioArbiter` is a timer-free latch that misses `/ptt` and one-shot `transmit()`). The correctness crux is already satisfied: `TotRadio._fire()` calls `ptt(False)`, which on the UV-K5 routes to the full `_key_off` (RX registers restored **first**, then audio teardown) — proven on the firmware-accurate `FirmwareFakeSerial` (`registers[0x30] == _reg30` after expiry), not a bare PTT drop. Three wirings: (1) a first-class **`"alarm"` event** (`{kind:"tx_timeout", tot}`) surfacing the forced unkey on the reactive `/events` path (the previously-unwired `on_timeout` hook), **thread-safe** via `loop.call_soon_threadsafe` since `TotRadio` fires on a `threading.Timer` thread — wired at construction for a swapped radio (`build_radio(on_tot_timeout=…)`) and post-construction for the initial one (`TotRadio.set_on_timeout`, the controller-`on_event` pattern); (2) a **mandatory per-backend `uvk5.tot`** (backend-declared `DEFAULT_TOT = 180`, `coerce_uvk5_tot` rejects 0/negative and any value above the default — may shorten, never disable/weaken) resolved per-backend in `build_radio` so a global `tx.tot=0` never disables the UV-K6's cap; other backends keep the global `tx.tot` + their device-side story. Residual named honestly: in-process covers logic bugs/runaway/leaks/`SIGTERM`, **not** `SIGKILL`/panic/power-loss (radio stays keyed till power-cycle) — an out-of-process supervisor is a named follow-on, documented in `uvk5-setup.md`. Canary 89→90 (`uvk5.tot`); no new deps, no web change | Accepted |
| [0118](0118-uvk5-v3-firmware-fork.md) | **UV-K5 V3 firmware fork: pin the base and prove the build (F1 — the no-change gate).** The bench radio is a **UV-K5 V3** (PY32F071 MCU), not the DP32G030 that nicsure's "Quansheng Dock" firmware (the ADR 0110 pin) targets — so that firmware **can never run on it**. Plan for the arc: add radio-server's four dock ops (0x0870/0x0871 enter/exit full-control, 0x0850 write, 0x0851 read → 0x0951 reply) to the **V3 community firmware ourselves**, wire-identical to the classic Dock so `radio_server/backends/uvk5/{frames,transport,radio}.py` and all 1487 tests change **zero**. F1 ports nothing — it forks `armel/uv-k1-k5v3-firmware-custom` (F4HWN Fusion) under `kbennett2000`, pins tag **`v5.7.0`** / commit **`3bd3ebb`** (Apache-2.0), and proves the build→flash→boot pipeline with an **unmodified** build. Headless Docker build (ARM GNU 13.3.Rel1): `docker build -t uvk1-uvk5v3 . && docker run … cmake --preset Fusion && cmake --build --preset Fusion` (upstream's `compile-with-docker.sh` needs `-it` stripped for no-TTY). **Byte-identity not achievable — recorded honestly:** the official `f4hwn.fusion.v5.7.0.bin` is a **committed artifact** (`archive/…`, embeds commit `0567f01`, 2 commits *before* the tag — a pin discrepancy à la ADR 0110), and even with identical source+config the build diverges 8867/103544 bytes (8.56%) scattered — the signature of `-flto=auto` codegen + a different release environment, not a config diff. Verified instead: clean build, valid `.bin/.elf/.hex`, size within 4 bytes, identical version/author/edition strings. `BENCH.md` (fork) documents the uvtools2 V3 flash path (DFU/FTDI/tab-conflict/calib-dump) with bench-specifics flagged `⚠ CONFIRM AT BENCH` (guardrail 1). Both trees Apache-2.0 → F2 may **port** nicsure's `uart.c` with attribution (unlike the GPL client, spec-only); fork `NOTICE` updated. F1 bin delivered as fork pre-release `radio-server-f1-v5.7.0`. **No radio-server code touched → suite stays 1487/5.** Acceptance (Kris, bench): flash the F1 bin over release Fusion, boots/RX/keypad identical → green-lights F2. Small cycles: F1 build gate → F2 protocol port → F3 bench loop | Accepted |
| [0119](0119-uvk5-v3-dock-protocol-port.md) | **UV-K5 V3 firmware: the dock protocol port (F2).** With F1 (ADR 0118) merged and bench-confirmed, port the dock control mode into the fork (`f2-dock-port` @ `32c600b`) so radio-server drives the V3 over the wire protocol it already speaks to the classic Quansheng Dock — **byte-compatible with the untouched `FirmwareFakeSerial`/`Uvk5Decoder`, so `radio_server/` and all 1487 tests change ZERO** (a genuine V3 wire difference would be STOP-and-report; none found — the V3 stock framing is already byte-identical). Surface is tiny (registers do everything): `0x0850` write / `0x0851` read → `0x0951 RegisterInfo`, `0x0870`/`0x0871` enter/exit full-control; no keypress/screen/scan/GPIO/AM/FSK/modulation. Design: a **pure, host-compilable** `App/app/dock.c`+`dock.h` (framing `AB CD`/`DC BA`, 16-byte XOR, CRC-16/XMODEM, command-CRC validation, dummy `obf(FF FF)` replies, register dispatch, streaming deframer) behind a thin `dock_hal_t`; `App/app/uart.c` binds it to `BK4819_Read/WriteRegister`+`UART_Send`, extends the existing dispatch `switch` and adds the `0x0870` blocking loop (re-enters dispatch for in-loop R/W, nicsure's shape) — surgical, `ENABLE_DOCK`-gated, on in Fusion. **The spec is the fake:** a `tests/host/` gcc harness mirrors `FirmwareFakeSerial`'s acceptance/dispatch/reply rules incl. a byte-exact `0x0951` vector — **19/19 pass** (pre-flash proof). Ported from nicsure's Apache-2.0 `uart.c` (@ `4375c3e`) **with attribution** (NOTICE + file headers), unlike the GPL client (spec-only). V3 derivation (from the tree): **no watchdog to feed**, no async ISR fights the BK4819 (blocking = the quiesce), 38400 already, encryption always-on (toggle never on radio-server's dock path). Flash fits: **104,116 B / 118 KB (86%), +572 B** over F1. Docs-only in radio-server (ADR + README + HANDOFF); `uv run pytest` still **1487/5**. F1 bin → pre-release `radio-server-f2-v5.7.0`. Acceptance (Kris, bench): flash → `doctor --backend uvk5` connect probe (the `ReadRegisters(0x30)` elicit answering IS the port working) → the four F1 gates → green-lights F3 (bench loop) | Accepted |
| [0120](0120-uvk5-v3-rx-audio-path.md) | **UV-K5 V3: RX audio was dead in dock mode — the audio-path amp (F3a).** Live-bench diagnostic: with the F2 build, received audio to the AIOC read the **noise floor** (~110 RMS) where an early HT-keyed run read 12431. Cause, proven with register dumps from a fresh power-cycle (never inferred): RX audio passes **two gates** — BK4819 `REG_47` (AF selector, MUTE `0x6042` / FM `0x6142`, reachable over the dock) **and `GPIOA8`, the external audio-amp enable, an MCU GPIO the dock CANNOT reach**. radio-server holds the `0x0870` full-control loop for its whole life, starving the firmware timeslice so `APP_StartListening()` never runs to raise GPIOA8 / unmute `REG_47`. Over the dock we forced `REG_30=0xBFF1` + `REG_47=0x6142` + `REG_48=0xB3A8`, **confirmed all three by read-back**, and the AIOC still read 109 RMS — BK4819 fully RX-alive yet silent ⇒ the dead gate is outside the BK4819 = GPIOA8. Only the firmware can raise it. **Fix (fork `f3-rx-audio-fix` @ `79f9b21`):** `Dock_EnterFullControl` now calls `GPIO_EnableAudioPath()` + `BK4819_SetAF(FM)` + `BK4819_SetRxAudioGain()` up-front, so RX audio flows the whole session; `0x0871` exit re-baselines (audio-path off, `REG_47`→MUTE — this also settles F2's resume-RX flag). `+28 B` (**104,144 B / 118 KB, 86.2%**); `dock.c` untouched → host harness stays 19/19. **radio-server change is the instrument only:** `doctor --rx-noise` — an HT-free RX self-test that force-opens the receiver, measures the AIOC, and restores every register (guardrail). The dock protocol + `backends/uvk5/*` change ZERO; `uv run pytest` **1492/4** (the 4 new tests). F3 bin → pre-release `radio-server-f3-v5.7.0` (sha256 `f9a3cc2d…`). Acceptance (Kris, bench): flash → `doctor --backend uvk5 --rx-noise` reads thousands (was floor) → live RX + the post-fix TX check | Accepted |
| [0121](0121-per-backend-squelch.md) | **Per-backend squelch mode (unbreak the mixed-radio box).** `audio.squelch` was one global mode, and ADR 0074 validates every configured backend against it — so `audio.squelch=cat` (which the docked UV-K6 needs post-F3) plus any configured audio-only block (`[baofeng]`) was **unstartable**: the inactive `[baofeng]` block was validated against the global `cat`, hit the "UV-5R has no busy line" guard, and boot aborted. Found live on the bench (stale `[baofeng]` block + uvk5). Fix mirrors the `uvk5.tot`/`tx.tot` pattern: uvk5 and baofeng get their own **`squelch_mode`** key with **backend-declared defaults** (`uvk5`=`cat` — dock mode force-opens the AF path so software VAD can't gate and `off` never segments; `baofeng`=`audio` — no busy line), and any backend without a key (kv4p/mock) falls back to the global. **Naming:** `squelch_mode`, not `squelch` — `[kv4p]` already owns `squelch` (an SA818 *level* 0-8); a flat registry rules out an `[audio.<backend>]` table. `resolve_squelch_mode(settings, backend)` (in `activity/gate.py`) is the one source of truth: `validate_backend_config` now checks each backend against ITS **effective** mode, so the stale-`[baofeng]`-blocks-`cat` failure disappears by construction (the guard still bites an *explicit* `baofeng.squelch_mode=cat`); misleading messages reworded to name the SECTION, never the active `server.backend`. `build_rx_gate` resolves the active backend and is **rebuilt in `RadioHolder.rebuild`**, so a live switch re-selects the gate type AND re-points `CatBusyGate` at the new radio (closing a latent stale-radio bug). The recording-`off` safety rail reads the effective mode too. Back-compat: realistic single-global configs resolve unchanged (documented divergence: a uvk5/baofeng box that relied on the global for a *non-default* mode now uses its backend default). No backend construction-arg change (`squelch_mode` is a gate/composition concern like `tot`). Canary 90→92; `radio.toml.example` regenerated; no new deps. `uv run pytest` green | Accepted |
| [0122](0122-uvk5-v3-f3-bench-loose-ends.md) | **UV-K5 V3 F3 bench loose ends (reproduce, fix, instrument).** Five host-side loose ends from the F3 bench, all RX/register-side, no TX, no firmware change. **(1) First-start dead RX** — the RX force-open is firmware-side (`Dock_ForceRxAudioAlive`, ADR 0120), triggered by radio-server's single **fire-and-forget, unverified** `send(EnterHwMode())` (0x0870); a 0x0870 lost in the reset-on-open boot race → dead RX until restart (radio leg), vs a capture stream opened against a settling USB device (host-audio leg). GPIOA8 is un-dockable, but the same firmware routine sets `REG_47`=FM — a host-visible **proxy**, so `REG_47` vs AIOC-RMS splits the legs. New `doctor --rx-firststart-loop N` (open→dump→measure→close, per-iter leg verdict + `dead/N`, step-0 F3 probe, cold-boot fidelity caveat); host-side fix `_enter_hw_mode_verified` (verify REG_47→FM, bounded re-send — shipped unconditionally as a strict robustness upgrade) + a default-OFF `capture_reopen_on_floor`. **(2)** WS handlers now swallow shutdown `CancelledError` (no teardown traceback). **(3)** `measure_rx_levels` primes one read before starting the clock, so stream-open latency no longer biases the true-rate estimate low — the kv4p **+2%** finding (pure formatter, hardcoded inputs) is untouched. **(4)** New `doctor --rssi` live meter (raw reg-0x67 counts + busy verdict vs `uvk5.squelch_threshold`). **(5)** The "HELLO not answered" message is reworded: the V3 fork is always-encrypted and drops the plaintext-0x0514 toggle (ADR 0119), so an unanswered plaintext HELLO is **expected**, not a fault (dock-alive is already proven by the register elicit). `frames.py`/`transport.py` unchanged (F2 invariant). **Live validation deferred:** the dev-PC HT was unresponsive over the dock (AIOC audio leg healthy, but zero bytes / register elicit timed out across 4 attempts — powered off or not on dock firmware); the live repro before/after counts (item 1) and live RSSI (item 4) are a bench acceptance for Kris. All five proven hardware-free — `FirmwareFakeSerial` extended with an F3 force-open + droppable-0x0870 model; `uv run pytest` **1528/4** (was 1510/4). **Live validation done 2026-07-24 (addendum):** item 3 PASS live (rate **47,999 Hz**/30 s; `--rx-noise` LOUD), item 4 PASS live → **`squelch_threshold` set from data 40→220** (idle ~159/max 163 vs keyed ~311); item 1's `_enter_hw_mode_verified` fix confirmed good (**10/10 completed opens ALIVE, REG_47=FM, F3 CONFIRMED, zero dead-RX**) but the loop can't reach 20-clean — a **reproducible harness defect** (construction-time reset-on-open `Uvk5Timeout` crashes the un-wrapped `_build_backend` at the ~6th rapid reopen), filed as a follow-up harness-fix cycle; no firmware implicated. **Item 1 fully accepted 2026-07-24 ([ADR 0123]):** the harness fix landed and `--rx-firststart-loop 20` ran **0 dead / 20** on the bench (F3 CONFIRMED, all ALIVE) | Accepted |
| [0123](0123-uvk5-v3-rx-firststart-loop-harness.md) | **UV-K5 V3 `--rx-firststart-loop` harness fix + the real 20-count.** ADR 0122 proved the first-start dead-RX **fix** works (10/10 completed opens ALIVE) but the **instrument** crashed at the ~6th open: `_build_backend` → `Uvk5Radio.__init__`'s non-retransmitting seeding reads hit the reset-on-open race, and construction sat **outside** the per-iteration `try`, so a `Uvk5Timeout` killed the whole run. Host-side (`doctor`) only, no firmware change. **(1)** Construction moved inside the `try`; a surviving construct-timeout is counted `DEAD/RADIO` (`REG_47=------  construct timeout after N attempts`), not a crash. **(2)** New `_build_backend_settled(cfg, *, attempts, interval, sleep)` catches **only** `Uvk5Timeout` and retries the whole build — the harness analogue of `connect()`'s probe retransmit (ADR 0111, same tolerance): `_RX_FIRSTSTART_CONSTRUCT_ATTEMPTS=3` mirrors `_ENTER_HW_MODE_RETRIES`, interval reuses `_ELICIT_RETRANSMIT_INTERVAL` (0.25 s); both the loop and the step-0 F3 probe build through it. **(3)** A `_RX_FIRSTSTART_SETTLE_S=1.0 s` inter-iteration settle (before every iteration but the first) makes each open a genuine single-reset first-start, not a rapid-reopen reset pile-up — a **realism** decision that defines what "N-clean" means (marked `VERIFY ON BENCH`; after the fix, any non-clean count is a real finding, not re-run to green). **Live: `0 dead / 20`** on the bench (F3 CONFIRMED, all ALIVE, `REG_47=0x6142`, peaks ~7.3–8.2 k) — the acceptance 0122 deferred. Transport/backend untouched (F2/ADR 0119 invariant). +5 tests (droppable construction); `uv run pytest` **1533/4** | Accepted |

[ADR 0123]: 0123-uvk5-v3-rx-firststart-loop-harness.md

## The live linking arc

Linking a repeater/base to a remote voice channel is done over **Mumble**, not the reverted M17 stack.
The arc is ADR **0041** (single link) → **0042** (multiple servers, DTMF-selectable, one active) →
**0043** (ungated disconnect) → **0045** (audio-correctness fixes) → **0049** (real-time DTMF mute +
Mumble→RF keying yield) → **0050** (the web UI doubles as a Mumble client). Config lives under
`[mumble]` / `[[mumble.servers]]`; see
[configuration.md](../configuration.md) and the annotated [`radio.toml.example`](../../radio.toml.example).
