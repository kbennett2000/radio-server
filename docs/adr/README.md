# Architecture Decision Records

This directory holds the ADRs for radio-server. Each ADR is a short, dated record of one significant
decision and its context (ADR-first is the project convention — see [CLAUDE.md](../../CLAUDE.md)). The
table below is the index: what each ADR decided, its current status, and any supersession. It is
maintained by hand — add a row when you add an ADR.

## Read this first — two things the numbering can't tell you

- **`0001` is used twice.** [`0001-cycle-model.md`](0001-cycle-model.md) is a *process/meta* ADR (the
  headless-cycle working model, no `Status` line); [`0001-two-backend-radio-abstraction.md`](0001-two-backend-radio-abstraction.md)
  is the first *technical* ADR. They are kept as-is rather than renumbered, so existing references stay
  valid; a future tidy could renumber the process ADR to `0000`.
- **`0035` and `0036` are missing, and an earlier `0049`–`0053` never existed on disk.** Those numbers
  belonged to a reverted **M17 / mrefd / Codec2** linking arc that was rolled back wholesale (commit
  `f39a750`, "Revert cycles 41-58"). That arc is **dead**. Its live successor is the **Mumble/Murmur
  link** (ADRs 0041→0049). The current `0049` below is a completely different, live ADR that happens to
  reuse the number. **Anything in the tree referencing M17, mrefd, Codec2, libcodec2, or reflectors is
  stale and does not describe the shipped system.**

## Index

| ADR | Title | Status |
|----|----|----|
| [0001](0001-cycle-model.md) | Cycle model (headless working process) | Accepted (process ADR) |
| [0001](0001-two-backend-radio-abstraction.md) | Two-backend radio abstraction, mock-first build | Accepted |
| [0002](0002-radio-protocol-shape.md) | Radio protocol shape (shared vs CAT-only split) | Accepted |
| [0003](0003-session-auth-state-machine.md) | Session / auth state machine | Accepted |
| [0004](0004-dispatch-service-tts.md) | Command dispatch, service interface, TTS shape | Accepted |
| [0005](0005-station-id-scheduling.md) | Station ID scheduling model | Accepted |
| [0006](0006-canonical-audio-format.md) | Canonical audio format, fail-loud frames | Accepted |
| [0007](0007-cw-id-encoder.md) | CW station-ID encoder (Morse, PARIS timing) | Accepted |
| [0008](0008-dtmf-decode-and-framing.md) | DTMF decode + framing grammar | Accepted |
| [0009](0009-piper-tts-engine.md) | PiperTts engine | Accepted |
| [0010](0010-voice-id.md) | VoiceId (phonetic spoken ID) + CW/voice mode | Accepted |
| [0011](0011-api-layer.md) | HTTP/WebSocket API layer, capability gating | Accepted |
| [0012](0012-scan-engine.md) | Software scan engine (clock-driven CAT loop) | Accepted |
| [0013](0013-controller-loop.md) | Controller loop: pure `step()` over `receive()` | Accepted |
| [0014](0014-rx-audio-streaming.md) | RX audio streaming (binary WS, demand pump) | Accepted |
| [0015](0015-activity-detection.md) | Activity detection (RMS + hysteresis + hang) | Accepted |
| [0016](0016-tx-audio-ingest.md) | TX audio ingest (binary WS in, single-talker) | Accepted |
| [0017](0017-duplex-arbiter.md) | Duplex arbiter (half-duplex TX priority) | Accepted |
| [0018](0018-event-log.md) | Event log (subscriber ledger over JSONL) | Accepted |
| [0019](0019-deferred-event-instrumentation.md) | Deferred-event instrumentation | Accepted |
| [0020](0020-audio-recording.md) | Audio recording (RX to WAV, passive tap) | Accepted |
| [0021](0021-recording-safety-and-tx.md) | Recording safety rails + TX recording | Accepted |
| [0022](0022-web-ui-architecture.md) | Web UI architecture (React SPA, same-origin) | Accepted |
| [0023](0023-rx-playback.md) | Live RX playback in the browser | Accepted |
| [0024](0024-tx-mic-capture.md) | Browser TX mic capture (getUserMedia) | Accepted |
| [0025](0025-config-system.md) | Config system (schema-driven TOML + secrets) | Accepted |
| [0026](0026-settings-api.md) | Settings REST API + secret rotation | Accepted |
| [0027](0027-settings-ui.md) | Web UI settings screen | Accepted |
| [0028](0028-async-scan-runner.md) | Async scan runner | Accepted |
| [0029](0029-aioc-baofeng-bringup.md) | AIOC/Baofeng backend bring-up (audio-only PTT) | Accepted |
| [0030](0030-controller-dtmf-buffering.md) | Buffer RX audio before DTMF decode | **Superseded in part by 0038** (buffered survives as `dtmf.decode_mode="buffered"`) |
| [0031](0031-single-capture-reader.md) | One capture reader for browser + controller | Accepted |
| [0032](0032-tx-lead-in.md) | TX lead-in (silence after key-up) | Accepted |
| [0033](0033-fetch-voice-services.md) | LAN-fetch voice services (quote/battery/bible) | Accepted |
| [0034](0034-pluggable-voice-services.md) | Pluggable voice-service architecture | Accepted |
| *0035* | *(gap — reverted M17 arc)* | — |
| *0036* | *(gap — reverted M17 arc)* | — |
| [0037](0037-web-ui-simplification.md) | Web UI simplification & approachability | Accepted |
| [0038](0038-streaming-dtmf-decode.md) | Streaming DTMF decode (one persistent multimon-ng) | Accepted (**supersedes 0030's default**) |
| [0039](0039-https-secure-context.md) | HTTPS secure context (phone live audio) | Accepted |
| [0040](0040-nonblocking-dtmf-feed.md) | Non-blocking DTMF feed off the RX loop | Accepted |
| [0041](0041-mumble-link.md) | Mumble/Murmur link (bridge RF to a channel) | Accepted |
| [0042](0042-multi-mumble-servers.md) | Multiple Mumble servers, DTMF-selectable | Accepted |
| [0043](0043-ungated-link-off.md) | Link-off combo works without a login | Accepted |
| [0044](0044-retro-ham-visual-refresh.md) | Retro-ham visual refresh (Day/Night themes) | Accepted |
| [0045](0045-link-audio-correctness.md) | Link audio correctness (DTMF mute; no busy-latch) | Accepted; **DTMF-mute mechanism superseded by 0049** (busy-latch fix live) |
| [0046](0046-web-session-open.md) | Open the OTA session from the web UI | Accepted |
| [0047](0047-web-restart.md) | Restart the server from the settings screen | Accepted |
| [0048](0048-cockpit-theme-meter-scale-totp-toggle.md) | Cockpit theme, meter dB scale, TOTP toggle | Accepted (extends 0044) |
| [0049](0049-realtime-dtmf-mute-and-yield.md) | Real-time DTMF mute + Mumble→RF keying yield | Accepted (**supersedes 0045's DTMF-mute mechanism**) |
| [0050](0050-web-mumble-client.md) | Web UI as a Mumble client (browser monitor/talk on the linked channel) | Accepted |
| [0051](0051-local-service-plugins.md) | Slim the shipped service set; local service plugins | Accepted (**supersedes 0034's in-tree-only scope**) |
| [0052](0052-freetext-mumble-names-demo-server.md) | Free-text Mumble names, per-entry password, shipped demo server | Accepted (amends 0042) |
| [0053](0053-bootstrap-installer.md) | One-command bootstrap installer (`scripts/install.sh` / `.ps1`) | Accepted |
| [0054](0054-native-dtmf-decode.md) | Native in-process DTMF decode (Goertzel), additive `native` mode | Accepted |
| [0055](0055-auto-decode-mode.md) | Auto-resolve DTMF decode mode by multimon-ng availability (`auto` default) | Accepted |
| [0056](0056-mumble-on-windows.md) | Mumble link on native Windows (tarball pin + vendored `opus.dll`) | Accepted |
| [0057](0057-installer-ships-the-link.md) | Installer ships the Mumble link by default; libopus via a bundled-wheel carrier (retires the vendored DLL) | Accepted |
| [0058](0058-posix-install-script.md) | `install.sh` is POSIX sh so `curl … \| sh` runs on dash; docs↔script contract test | Accepted |
| [0059](0059-plugin-migrations-and-examples.md) | Removed services ship as `examples/`; named migration errors for `[plugins.*]` and `local_services/` ids | Accepted |
| [0060](0060-native-is-the-default.md) | Resolve `auto` to `native` unconditionally (bench-verified); multimon-ng becomes optional | Accepted (**flips 0055's preference; closes 0054's A/B for decode**) |
| [0061](0061-kv4p-uart-backend.md) | kv4p HT UART/KISS backend shape (state reconciler, first real `CatRadio`, real busy line); pure wire codec only this cycle | Accepted |
| [0062](0062-kv4p-transport-handshake.md) | kv4p serial transport: connect by syncing `DeviceState.appliedSequence` (no HELLO dependency); hold DTR/RTS low on open (no reset-to-get-HELLO) | Accepted |
| [0063](0063-kv4p-backend-capabilities-and-units.md) | kv4p `Kv4pHt` backend: complete-state reconcile; advertise `SCAN` (software sweep) but omit `SET_CHANNEL`; unit mapping (Hz↔MHz, CTCSS Hz↔index, mode↔bandwidth) fails loud | Accepted |
| [0064](0064-kv4p-firmware-repin-shipped.md) | Re-pin the kv4p firmware reference to shipped v2.0.0.1 (`3f0e809`); the audio command ID (`0x07` vs `0x0C`), not `FIRMWARE_VER`, discriminates the two v17 builds | Accepted |
| [0065](0065-kv4p-opus-codec.md) | kv4p audio edge is Opus (48 kHz, 40 ms frames), replacing the dead IMA-ADPCM path | Accepted |
| [0066](0066-kv4p-connect-running-board.md) | kv4p `connect()` re-founded on shipped firmware: passive-first → elicit-with-retransmit → restore; de-clobber the NVS (no zero-write on connect/close) | Accepted |
| [0067](0067-extras-taxonomy.md) | Extras taxonomy: factor leaves (`serial`/`soundcard`/`opus`) and compose backends (`hardware`/`kv4p`/`mumble`) so a node installs only what it uses; `opuslib` named explicitly | Accepted |
| [0068](0068-kv4p-bringup-detections-and-docs.md) | kv4p bring-up: doctor detections for pre-KISS firmware (`de ad be ef` sniff) and band-mismatch (HELLO vs `kv4p.module_type`), shipped with the user docs (new `kv4p-setup.md`, fork by radio) | Accepted |
| [0069](0069-kv4p-tx-bringup.md) | kv4p TX bring-up: doctor TX telemetry rig (`TxStats`, key-up latency, `--tx-lead` sweep); fixes doctor to read `radio.toml`; first bench numbers (`tx_lead` 0.2→**0.5**, ~230 B/frame, window blocks are backpressure) | Accepted |
| [0070](0070-kv4p-rx-sample-rate-correction.md) | kv4p RX sample-rate correction: shipped firmware clocks the RX ADC ~2% fast (`rxAudio.h` `*1.02`) but labels it 48 kHz — broke DTMF and drifts every continuous consumer. Resample the true device rate → 48 kHz at the decode edge (soxr HQ); `kv4p.sample_rate_correction` knob + doctor `--rx-level` rate readout; connect timeout 2→10 s | Accepted |
| [0071](0071-kv4p-rx-dtmf-capture-analysis.md) | kv4p DTMF still fails after 0070: stop analysing, capture it. doctor `--rx-capture` (RX→WAV) + `--analyze-wav` read the DTMF tones straight out of the audio via FFT (independent of GoertzelStream) and return a verdict — clipping (firmware 16× gain) vs off-frequency vs clean-so-decode-wiring; tightens the `--rx-level` correction verdict to 0.2% | Accepted |
| [0072](0072-kv4p-dtmf-energy-floor.md) | kv4p DTMF cause found by reproducing the real capture through the live decoder: the native `NATIVE_ENERGY_FLOOR` (0.02) is ~10× too high for received audio (measured low-tone power ~0.012), so every block read as silence. Lower it to **0.002** (talk-off stays with the scale-invariant ratio gates); adds received-level + frame-size-invariance + talk-off regressions; realigns the 0070 offset regression to a received level | Accepted |
| [0073](0073-radio-holder-seam.md) | A `RadioHolder` seam (api/holder.py): one object owns the active radio (`.radio`, built via the extracted `build_radio(settings)`) and the lifecycle of the radio-bound pipeline — `start()` constructs the `RxPump`/`ScanRunner` against it, `stop()` tears them down (drop PTT if the arbiter holds TX, stop scan, halt pump, reap controller, close radio) idempotently. Pure behaviour-preserving refactor; the keystone for later in-app backend switching (`stop(); rebuild; start()`) | Accepted |
| [0074](0074-multi-backend-config.md) | radio.toml describes more than one backend: presence-based configured set (`[<backend>]` block present + active `server.backend`), each validated at load so a broken *inactive* switch target fails at startup, not at select time. New light `api/backend_config.py` (pure `validate_backend_config`/`validate_configured_backends`/`backend_kwargs`/`configured_backends` enumeration for the next cycle's select endpoint + UI); `doctor` validates the selected backend; no schema change (example/canary unmoved). No switching yet | Accepted |
| [0075](0075-configurable-dtmf-reverse-twist.md) | Make the native DTMF decoder's reverse-twist tolerance configurable via `audio.dtmf_reverse_twist_db` (default 4.0 = today's `NATIVE_REVERSE_TWIST_DB`, threaded into `GoertzelStream`). Bench finding: the UV-5R Mini sends its low group ~6.4 dB hotter than the high, tripping the −4 dB gate so it decodes nothing while a UV-5R decodes fine. Opt-in (bump to ~10) so compliant radios keep the tight, talk-off-safe default; dominance + second-harmonic gates carry talk-off, so the wider gate stays talk-off-clean. Reverse twist only, global (not per-backend) | Accepted |
| [0076](0076-live-backend-switch.md) | The live backend switch: `RadioHolder.rebuild(new_settings)` (atomic under a lock; stop → construct via `radio_factory` → start, rebuilding the controller via a `controller_factory`; **rolls back** to the previous working backend if the target fails to open, so a switch never bricks the server) + `POST /radio/select` (validates against `configured_backends`, 409 unconfigured, 503 on rollback) + `GET /radio/backends` (current + configured list). The select handler `nonlocal`-rebinds the route closures' `radio`/`rx_pump`/`scan_runner`/`controller` (the ADR 0073-deferred "routes follow the live radio" step), re-honors RX demand, re-emits a new `capabilities` event + a status snapshot, and persists `server.backend` through the schema. Endpoint + API only; the UI dropdown is the next cycle. No schema change | Accepted |
| [0077](0077-backend-selector-ui.md) | The backend selector in the web control panel: a `BackendPanel` dropdown over `GET /radio/backends` + `POST /radio/select` (self-hides with <2 backends; tracks the live active radio so a 503 snaps back and names the radio you're still on; "Switching…"/disabled in flight; warns switching drops PTT mid-transmit). The payoff — **reactive re-greying without a reconnect**: `reduceStatus` folds the re-emitted `capabilities` event into `state.caps` and `ControlPanel` prefers it over the one-shot login prop, so the CAT tuning/scan cards appear for the kv4p and vanish for the AIOC live; the additive `disabledCaps` clears on a switch. Bootstraps Vitest + Testing Library (the frontend had no JS test runner). No server/endpoint change | Accepted |
| [0078](0078-preserve-extra-channel-on-settings-patch.md) | Preserve the local-plugin (`[plugins.*]`) extra channel (ADR 0051) across a settings patch: `POST /radio/select` (ADR 0076) and `PATCH /settings` both rebuilt `Settings` from **schema keys only** and called `resolve_settings(...)` with no `extra=`, so a live backend switch (or any save) silently dropped every local plugin's config off `app.state.settings` — the rebuilt controller then gated weather/astronomy/quote/battery/bible **out of the service catalog** until a restart (the on-disk file was untouched, so a restart restored them). Add a public `Settings.extras()` whole-channel accessor and thread `extra=current.extras()` through both patch sites. Runtime-only; no schema change; the UI was faithful (the catalog really had shrunk) | Accepted |
| [0079](0079-persist-auth-session-across-backend-switch.md) | Persist the over-RF auth `Session` across a live backend switch (ADR 0076) — the runtime-state sibling of ADR 0078's config fix. `build_controller` minted a fresh `Session()` on every rebuild, so `holder.rebuild` logged out an authenticated operator the instant the station switched radios. The auth session belongs to the operator at the station, not the per-radio controller: give `build_controller` a `session=` param and have `build_app`'s `controller_factory` closure own **one** `Session` and inject it into every (re)build. Falls out correctly: the inactivity clock carries (neither reset nor extended), a mid-entry DTMF code is discarded (per-controller framer) while an open session stays open, and the rebuilt `StationId` re-arms erring toward ID-ing (Part 97). No schema change; "gated, not secure" makes persisting across a local switch fine | Accepted |
| [0080](0080-kv4p-tx-gain.md) | Give the kv4p a TX audio-level control, `kv4p.tx_gain` (float, default `1.0`). Bench symptom: kv4p announcements/voice are overmodulated — the firmware TX path applies no boost and the backend encodes to Opus with no attenuation, so near-full-scale TTS/CW over-deviates the SA818. The AIOC tames identical audio with `alsamixer`'s playback slider; the kv4p has no sound card and no such stage. Add a plain multiplier applied to the canonical int16 samples **before the Opus encoder** in `TxAudioEncoder` — the single choke point every TX byte (streaming + one-shot + lead-in) flows through — so it scales everything transmitted, not one source. Clamps to full-scale int16 (a `>1.0` value clamps, never wraps); `1.0` is an exact no-op. Threaded through both the initial build and the ADR 0076 rebuild like the other kv4p knobs; marked verify-on-bench (guardrail 1, ~`0.5` starting point). No AIOC change (OS mixer owns its TX level), no limiter/AGC, no RX change | Accepted |
| [0081](0081-remove-decorative-frequency-dial.md) | Remove the decorative frequency-dial scale (`DialScale`) from the control panel's face. It was a purely visual (`aria-hidden`) 144–148 MHz ruler + needle from the ADR 0044 retro refresh — hard-coded to the 2 m band, duplicating the authoritative `FreqLcd` numeric readout above it, adding clutter without function. Delete the component, its single mount, and its `.dial*` CSS; keep the `showDial` flag (still gates the LCD) and the shared `--tick`/`--ticksoft`/`--red` vars (used elsewhere). No server/endpoint/capability/setting change; no test change (nothing referenced the dial) | Accepted |
| [0082](0082-kv4p-keyed-idle-tx-pacer.md) | Keep the kv4p transmitter fed while keyed-but-idle so the firmware never underruns and loops stale audio (the "Max Headroom" repeat over Mumble). The AIOC's continuous sounddevice output stream emits silence between writes; the frame-push kv4p sends nothing while keyed-idle, so when the Mumble bridge holds PTT across a `tx_hang` pause the SA818's TX buffer starves and loops its last ~0.5 s. Add a `_TxPacer` (new `backends/kv4p/pacer.py`): during a **held** key a daemon thread sends exactly one frame per ~40 ms slot — real audio buffered by `transmit()`, encoded silence (the key-up lead-in path; `tx_gain`-invariant) otherwise — the single coherent sender, so no window race or doubled frame. `transmit()` while keyed enqueues to a bounded drop-oldest jitter buffer instead of encoding inline; key-down stops the pacer, flushes the tail, drops PTT. One-shot `transmit()` (station ID, TTS, `/transmit`) is untouched — it never holds the key idle. Silence stays within the flow-control window (WINDOW_UPDATE refunds); moving writes off the event loop removes the old ~2 s backpressure stall (the buffer bound is now the limiter). No AIOC change, no `tx_hang` change, no schema change | Accepted |
| [0083](0083-fixed-login-code.md) | A fixed over-RF login code option (opt-in, non-default) plus a settings-screen tidy. Auth becomes a derived mode — off / TOTP / fixed — via a new non-secret `auth.fixed_code` toggle (default false) beside the unchanged `auth.totp_enabled` gate. A `FixedCodeVerifier` (constant-time compare, **no single-use burn** — a fixed code is reused, so it's replayable by anyone who overhears it: the documented downgrade) drops into `AuthGate` in place of `TotpVerifier`, selected in `build_controller`; `build_app`'s controller gate builds in fixed mode when a code is set. The code is a credential on the secrets channel (`fixed_code` / `RADIO_FIXED_CODE`), never in `radio.toml`, set write-only via `POST /settings/secrets/fixed-code` (6-digit-validated); `GET /auth/totp` reports `{fixed:true}` and never echoes it; the masthead shows a locked chip. Warning surfaced in the toggle description and the Secrets control. Also: `MumbleServersPanel` now folds like the other settings groups (a `.settings-group` `<details>`). Non-breaking; canary 64→65 | Accepted |
| [0084](0084-kv4p-rx-continuous-silence.md) | Make the kv4p RECEIVE path a continuous stream (the RX mirror of the ADR 0082 TX pacer). Over Mumble, the tail of a *received* transmission looped ("Max Headroom") when a signal ended — not on the AIOC. The AIOC reads a continuous capture stream so `receive()` always returns full-length audio (silence between transmissions); the frame-push kv4p returned an **empty** frame on idle, which the shared `RxPump` skips *before* the activity gate — so the VAD gate's hang never published the trailing taper and the RF→Mumble feed stopped abruptly. Fix: on an idle-poll timeout `receive()` returns a full-length canonical **silence** frame (1920 zeros) instead of empty, so the stream stays continuous and every consumer (gate/recording/DTMF) treats kv4p identically to the AIOC — the hang tapers the silence then closes. `doctor measure_rx_levels` now skips all-zero frames so the ADR-0070 `--rx-level` calibration still measures real audio only. No AIOC/`tx_hang`/schema change | Accepted |
| [0085](0085-mumble-rx-guard.md) | A post-transmit **RX guard** that keeps the TX→RX turnaround transient off Mumble. AIOC symptom: talk from the phone (Mumble→RF), release PTT, the radio drops — then the phone hears a ~0.25 s buzz. It's the UV-5R receiver recovering at the turnaround (FM hash before squelch settles), captured by the AIOC card and relayed because `audio.squelch="off"` passes everything; kv4p is immune (SA818 hardware squelch keeps it off the wire). The arbiter resumes RX the instant TX releases with no guard — the very timing its docstring leaves unmodeled (ADR 0017, guardrail 1). Fix: on the arbiter's **TX→RX edge** (source-agnostic — a browser talker's release arms it too, not just the Mumble bridge), arm a short window that suppresses only the **RF→Mumble relay** (browser Listen + the recorder untouched — recording never loses audio). Reuses the ADR 0049 `DtmfMuteGate` latch, app-scoped and injected into the bridge; a new `rx_guarded` counter in `tx_stats`. Config `mumble.rx_guard_seconds` (default 0.4, advanced, verify-on-bench, **0 disables**). Backend-agnostic / kv4p-benign. Bundled UI tidy: all settings-screen collapsibles now default collapsed. canary 65→66 | Accepted |
| [0086](0086-vocoder-seam.md) | The **vocoder seam**: PCM ⇄ AMBE, isolated and unwired from the live app. A future digital-voice/D-STAR path needs a vocoder (8 kHz speech PCM → compressed voice frame and back); it lands **alone**, before any framing/socket/bridge plumbing, because the vocoder is the one piece that can't be proven by inspection (same posture the reverted Codec2 seam took). New `radio_server/vocoder/` package: a device-independent `Vocoder` `Protocol` (`encode(pcm)->ambe` / `decode(ambe)->pcm`, one 20 ms frame) so a later **AMBE3000** or software codec (**Griffin**) drops in behind it, plus one concrete `DVDongleVocoder` driving the DV Dongle's DVSI **AMBE2000** over its FTDI link. **The seam operates at the native 8 kHz frame, not the app's 48 kHz canonical audio** — every real vocoder is 8 kHz, and the 48k⇄8k resample belongs at the future consuming backend's edge (a deliberate departure from the Codec2 seam, which resampled internally). Pure I/O-free wire codec (`frames.py`, DV Dongle 13-bit-length/3-bit-type framing + the verbatim AMBE2000 D-STAR config block) split from a kv4p-style serial transport (lazy pyserial behind `hardware`, daemon reader thread, `Condition` reply hand-off, idempotent `close`); v1 is a synchronous query/reply per frame. Protocol read from g4klx/DummyRepeater (GPL-2) as a **spec** and reimplemented clean (no ported code; AMBE vocoding stays on the licensed chip — no codec/patent exposure in-tree). Acceptance is a `doctor --vocoder-loopback` self-test (encode→decode→WAV + a lossy round-trip metric) proven on the actual dongle. **No config schema, no canary bump, no `radio.toml.example` change** (unwired); rides the existing `serial`/`hardware` extra | Accepted |
| [0087](0087-dstar-link.md) | The **D-STAR link**: radio-server as a homebrew-repeater endpoint on a G4KLX **ircDDBGateway**, the first live consumer of the ADR 0086 vocoder. New `radio_server/dstar/` package (sibling to the Mumble link): a pure I/O-free **DSRP** wire codec (`dsrp.py` — register/poll/header/data packets) + the 41-byte radio header with **CRC-16/X-25** (`header.py`), a `GatewayClient` seam with a `MockGatewayClient` + a `UdpGatewayClient` (socket, daemon reader, register/poll timers), and a **half-duplex bridge** (`bridge.py`) that keys reflector audio onto RF via the shared `TxSession`/`TxSlot`/`station_id` and encodes RF back to the reflector through the vocoder. A **mode latch (IDLE/RX/TX)** uses the single AMBE2000 chip one direction at a time — matching D-STAR's one-talker reality and the ADR 0086 no-interleave rule. The 48k⇄8k resample lives at the bridge edge, never in the vocoder. Protocol read from g4klx `RepeaterProtocolHandler.cpp` (GPL-2) as a **spec**, reimplemented clean. Config `[dstar]` group **off by default** (inert until `dstar.callsign` is set); wired in `create_app`/`build_app` behind that gate so existing deployments are untouched. Acceptance is a `doctor --dstar-echo` self-test round-tripping audio through the gateway's built-in **Echo** unit (a deterministic local loopback, no remote reflector/registration), reusing the vocoder staircase pitch metric. canary 66→74 | Accepted |
| [0088](0088-dstar-reflector-browser.md) | **D-STAR reflector control + browser talk/listen** — a hotspot you drive from a web page, extending the ADR 0087 link to the stated goal: pick a reflector on a screen, hit connect, talk/listen with the computer mic/speakers, no D-STAR radio. The D-STAR analogue of ADR 0050 (web-UI-as-Mumble-client). On `DStarBridge`: `send_link_command` (a synchronous, idle-gated URCALL burst — `NULL_AMBE` only, no vocoder — so it can't corrupt or be corrupted by an in-flight over) links/unlinks reflectors the standard D-STAR way; `send_operator_audio`/`end_operator_over` is the browser-mic TX seam (the `MumbleBridge.send_operator_audio` twin). A new `DStarLinkManager` parses `"REF001 C"` → URCALL `REF001CL` (REF/XRF/DCS/XLX differ only by prefix), tracks **believed** link state (the gateway gives no readback, surfaced honestly), and fires a `dstar` WS event. API: `WS /audio/dstar/{rx,tx}` (a dedicated `dstar_rx_hub` + a distinct `dstar_talk_slot`) and `POST /dstar/{link,unlink}` + `GET /dstar/status` + a `/status` block — the `/audio/mumble/*` + `/link` twins. Web UI: a `DStarPanel` reflector picker (free-text + presets) and a `dstarMode` that points Monitor/Transmit at the D-STAR endpoints. One new key `dstar.operator_tx` (browser-operator posture; `rx_to_reflector` off); `dstar.reflector` becomes the boot auto-link. `doctor --dstar-browser-echo` drives the real `send_operator_audio`→Echo→decode→`dstar_rx_hub` round trip. **Live-gateway-proven:** a header-only URCALL linked **REF030 C** (`D-Plus link established`); the browser round trip tracked the staircase (corr 0.88); the two production instances + the DVAP stayed at baseline PIDs (`NRestarts=0`). Found+fixed the AMBE2000 idle-sleep bug (chip goes unresponsive after ~2–3 s idle, losing the first inbound over) with an idle-gated keepalive decode. canary 74→75 | Accepted |
| [0089](0089-dstar-folded-shared-dongle.md) | **D-STAR folded into the real radios: one shared DV Dongle, crossband + browser together** — the ADR 0088 browser reflector seam moved off its standalone MockRadio node onto the two real instances (AIOC 8090, kv4p 8091), so linking a reflector from an instance's own HTTPS web UI crossbands it to **that** radio's frequency **and** the browser, and you talk onto the reflector from **either** the PC mic **or** that FM radio. Four changes: (1) a **TX-owner latch** (`_tx_source` `rf`/`op`) so the crossband RF pump and the browser mic both run at once — whichever opens the over owns it, the other drops, no interleaving into one DSRP session — removing ADR 0088's `rx_to_reflector = not operator_tx` mutual-exclusion; (2) the **DV Dongle is acquired on link, released on unlink** — `DStarLinkManager.connect`→`bridge.start()` creates the vocoder from a factory opening the FTDI port **exclusively** (`pyserial exclusive=True`), `disconnect`→`stop()` closes it; the OS serial exclusive lock is the sole cross-process arbiter, so the two instances share one dongle and a second connect returns a clean **503 "in use by the other radio"**; (3) **browser audio follows link state** — `dstarMode` derived from `state.dstar.active`, not a config flag (linked → `/audio/dstar/{rx,tx}`, unlinked → RF), and `dstar.operator_tx` **removed**; (4) an **activity log** — inbound MYCALL (parsed but discarded before) + our own overs fire an `on_activity` callback → an `activity` WS event + a 30-entry `/dstar/status` ring + a `DStarActivityLog` card. Deploy: `[dstar]` on both instances (identical module A / port 20012), the 8092 node disabled, HTTPS inherited, **no gateway change** (module A already exists; DVAP untouched). The **DVAP tab** (its own gateway module, needs the remote-control interface) is the next cycle. canary 75→74 | Accepted |
| [0090](0090-tx-timeout-timer.md) | **Transmitter time-out timer: a hard cap on continuous key, on every path.** The ADR 0089 folded crossband could get **stuck keying the transmitter** (dead air on the air) with no path to drop PTT but a process kill — because every existing safeguard (`tx.idle_timeout`, D-STAR/Mumble `tx_hang`) is a *silence* timeout that resets per frame, so a **continuous** key is uncapped. New `TotRadio` (`tx/tot.py`): a `Radio`-protocol decorator wrapping the active backend at `build_radio` — the one composition root every keyer (browser TX, D-STAR/Mumble bridges, DTMF services, station ID, REST `/ptt`/`/transmit`) and every backend funnels through, on build and on live swap. Delegates the whole surface via `__getattr__`; intercepts only `ptt`/`transmit`. The watchdog is armed **once on key-up** (never reset per frame — a hard cap, not an idle reset) and **fires on its own `threading.Timer` thread**, so it force-drops PTT even when the caller is parked in a blocking decode (the exact stuck-key mode). A streaming time-out **latches TOT-locked** (drops further `transmit()` until an explicit re-key — stops a self-keying SignaLink); a one-shot overrun does not latch. Config `tx.tot` (seconds; default **180**, `0` disables) beside `tx.idle_timeout`; canary 74→75. The independent safety net beneath the ADR 0091 crossband-close fix. Verified on an injected fake timer (`ptt_log == [True, False]`), never by live-keying | Accepted |
| [0091](0091-dstar-crossband-safety.md) | **D-STAR crossband safety: the over always closes, PTT always drops, the reflector is signal-gated.** The ADR 0089 folded crossband could get **stuck keying the transmitter** — inbound DATA kept arriving (queue never idled) while decodes stopped feeding RF, so `_reflector_to_rf`'s only close paths (end-bit / queue-idle) never fired; and `_teardown` relied on that loop's `finally`, which can't run while the task is **parked in a blocking vocoder decode** (cancel undeliverable, join hangs, and the vocoder that would unblock it was closed *after* the join). Only a process kill freed PTT. Five `dstar/bridge.py` fixes: (1) `_teardown` closes the vocoder **first** (unblocking a parked `_exchange`), force-drops PTT **directly** via new `_force_unkey`, and **bounds** each task join; (2) an **independent RX watchdog** closes the over when the keying session is idle past `tx_hang` (`TxSession.idle_elapsed`) even under continuous inbound; (3) a `write_timeout` on the DV Dongle serial port so a stuck write can't park forever; (4) `_rf_to_reflector` is **signal-gated by a per-link `AudioLevelGate`** (reusing the `audio.vad_*` thresholds, injected by `build_app`) so the crossband keys the reflector only on real RF audio — independent of the global `audio.squelch`; (5) `TxSlot` release only if actually held (never freeing a browser talker's slot) + a `_reap_stale_tx` backstop for a leaked "op" over. No new config, no canary change (gate reuses `audio.vad_*`; DI seam stays ungated). Sits above the ADR 0090 TOT. Verified on fakes (idle-watchdog close under failing decodes; `stop()` during a blocked decode drops PTT and returns; gate keys only on signal; slot accounting; op-over reap) — 1223 passed; re-enable gated on a bench proof (dummy-load reflector→RF + `doctor --dstar-browser-echo`) | Accepted |
| [0092](0092-dstar-parked-decode-ptt-safety.md) | **The parked decode can no longer hold PTT: an independent watchdog, a re-key guard, an unconditional unkey.** ADR 0091 was verified on fakes and merged — then the first **dummy-load** reflector→RF test on the real DV Dongle stuck-keyed anyway (`mode=rx`, PTT held ~40 s to the TOT). Two hardware realities the FakeVocoder/MockRadio couldn't reproduce: (1) ADR 0091's idle watchdog lived at the **top of the `_reflector_to_rf` loop**, but that loop `await`s each decode in the single-worker executor and a **wedged DV Dongle decode parks the loop there** — so the loop-top check never runs (`asyncio.wait_for` around `run_in_executor` doesn't help: it awaits the uncancellable thread to finish anyway); (2) `unlink` returned but PTT stayed up — `TxSession.close()` transmits a Part-97 sign-off **ID before** `ptt(False)`, that transmit **raised** on the wedged backend, and `_force_unkey`'s `suppress` swallowed it, skipping the unkey; and a decode resuming after the over closed could **re-key**. Four fixes, no config/canary change: (1) the PTT-safety watchdog runs as its **own `_rx_watchdog` task** (only ever `await`s `sleep`, never the executor) so it drops PTT even while the decode loop is parked; (2) `_play_ambe` drops its frame if `mode != "rx"` so a resumed decode can't re-key a closed over; (3) `TxSession.close()` wraps the sign-off ID `transmit` so a raise can never skip the `ptt(False)` beneath it — hardening **every** streaming keyer; (4) `_force_unkey` drops `radio.ptt(False)` **directly** as a path-independent backstop. Verified on fakes that now model the failure (a `_BlockingVocoder` parking in the executor proves the over closes on its own with no teardown; a late decode proves no re-key; a spy radio raising on the sign-off transmit proves close() still unkeys); re-enable still gated on the dummy-load proof that surfaced it | Accepted |
| [0093](0093-aioc-panic-unkey.md) | **The AIOC transmitter can never be stranded keyed: drop the line first, unconditionally, atomically.** After ADR 0090/0091/0092 hardened the D-STAR *bridge*, the crossband STILL left the AIOC transmitting dead air on real hardware — only a process kill (closing the serial port) dropped PTT. A controlled **carrier key-test** (bare `pyserial` DTR toggle on a dummy load, operator watching, no audio/dongle/bridge) isolated it: **`dtr=False` with the port open cleanly unkeys this AIOC** — so the hardware is fine and the stuck-key is a SOFTWARE failure to reach `dtr=False`. Three `AiocBaofeng` bugs, all in the stranding class: (1) `ptt(False)` was guarded on `self._keyed`, so a desynced flag made the watchdog's / teardown's / REST `/ptt off`'s safety lever **no-op** (matches the journal: `/ptt off`→200, `status.transmitting`=False, carrier still up); (2) `_key_on` asserted the line THEN wrote the TX lead-in — a lead-in write that raised propagated with the line up but `_keyed` never set True, stranding it under (1); (3) `_key_off` **drained the stream before dropping the line**, so a `stop()` that blocks/raises on an xrun'd stream (the DV Dongle write-timeout wedge was starving it) kept the line asserted. Fix: a single unconditional `_drop_line()` primitive (bare `setattr(line,False)`, never guarded, can't block); `_key_off` drops the line FIRST then tears down the stream best-effort (the RF-safety inversion of ADR 0029's drain-then-drop — costs a few ms of tail clip); `_key_on` atomic (drop line on any post-assert failure); `ptt(False)` unconditional (guard removed). Every keyer benefits (browser/Mumble/D-STAR/station-ID/`/transmit`). Verified on fakes that now model the failure (lead-in write raises → not stranded; stop() raises → line still dropped; line proven low BEFORE the stream is stopped; ptt(False) forces the line low when `_keyed` desynced) — 1230 passed. Does NOT re-enable D-STAR; crossband re-enable gated on a JOINT dummy-load re-proof; the dongle write-timeout wedge is a separate reliability follow-up | Accepted |
| [0094](0094-dvdongle-sleep-recovery.md) | **The DV Dongle recovers itself from the idle-sleep wedge: close+reopen+re-handshake on a frame timeout.** The crossband stuck-keys were *triggered* by the dongle wedging (`SerialTimeoutException: Write timeout` every decode); ADR 0092/0093 closed the safety half (PTT always drops), this closes the reliability half. A no-RF bench characterisation pinned the wedge: **sustained decode is rock-solid** (3000 back-to-back, 0 slow, ~36/s), but after ~2-3 s idle the **AMBE2000 sleeps and does not self-wake** — the next frame times out (`VocoderTimeout`) and, as the caller keeps feeding an unresponsive chip, the FTDI TX buffer fills and writes start timing out too (the incident signature). **A full close+reopen+re-handshake recovers it** (bench-proven). The ADR 0088 keepalive already prevents idle-sleep *while idle*; the gap is an idle stretch *during* an over (a stall between reflector talkers in `rx` mode, keepalive off). Fix: on `VocoderTimeout`, `_exchange` calls a new `_recover()` **once** and retries the frame (second failure propagates). `_recover` rebuilds the transport + session under the (now reentrant) io lock — signals stop, closes the old port, and **joins the old reader before reassigning** anything the reader reads by reference, then reopens + re-handshakes, retrying the flaky first open a few times exactly like cold bring-up; a dead dongle raises `VocoderUnavailable`. `_exchange` split into the recover-and-retry wrapper + the `_exchange_once` primitive; io lock → `RLock`. No public surface change. Verified on fakes modelling the wedge (handshakes-but-never-answers reopened healthy; flaky reopen; permanently wedged still propagates; un-openable → unavailable); the trigger + the close+reopen recovery are both bench-proven on the real dongle — 1230 passed. Does not change the D-STAR posture | Accepted |
| [0095](0095-gateway-remote-control-client.md) | **An ircDDBGateway remote-control client, landed as an isolated seam (unwired)** — PR 1 of 2 for DVAP support. radio-server is gateway module A over DSRP and links reflectors with in-band URCALL, so it can only steer its own module and only ever knows *believed* link state. The DVAP modules (B = 441.600, C = 441.000) are separate `dstarrepeater` endpoints it can't see; the gateway's **remote-control** UDP interface (compiled into the binary, currently off) links/unlinks *any* module and reports *confirmed* link state. This PR adds only the protocol client, consuming nothing — the ADR 0086/0087 discipline of proving a new wire protocol in isolation before it can touch the running radios. `dstar/remote_codec.py` (pure codec, the `dsrp.py` analogue): 3-byte ASCII tags, little-endian ints (wx `SWAP_ON_BE`); build `LIN` login / `SHA`+`SHA256(random_bytes ‖ password)` / `LNK`/`UNL`/`GCS`/`GRP`, parse (never raises) `RND`/`ACK`/`NAK`/`CAL`/`RPT` (the confirmed-state readback); `Reconnect`/`Protocol`/`Direction` enums from g4klx declared order. `dstar/remote_client.py` (transport seam, the `client.py` analogue): a `RemoteControlClient` Protocol, a `MockRemoteControlClient` that models a tiny gateway (link map, `fail_auth`), and a `UdpRemoteControlClient` (lazy cached `LIN→RND→SHA→ACK/NAK` login, bounded timeout + retries). Reimplemented clean from public g4klx `RemoteProtocolHandler.{h,cpp}` read as a spec. Nothing wired: no config/route/startup change. Verified on fakes (19 tests: codec byte-layouts, Mock link/status/unlink round-trip + fail-auth, Udp login handshake asserting the exact SHA over the injected random, NAK→auth-error, silence→timeout, close→logout) — 1253 passed; the LE-int convention, the SHA input and port 10022 are marked verify-against-the-live-gateway (guardrail 1). Next: the DVAP control surface (config + manager + `/dvap/*` + web tab) + the gateway deploy step (enable remote-control, stand up DVAP #2 as module C) | Accepted |
| [0096](0096-dvap-control-surface.md) | **DVAP control surface: config + a cached manager + `/dvap/*` over the remote-control client** — wires ADR 0095's client into the app so radio-server links/unlinks/monitors the DVAP gateway modules (B = 441.600, C = 441.000). Unlike D-STAR module A (a bridge + DV Dongle + PTT + *believed* state), the DVAP path is pure control-plane: no vocoder, no bridge, no PTT, and **confirmed** link state read over the gateway remote-control interface. Config: `[dvap]` host/port scalars + an array-of-tables `[[dvap.modules]]` (module letter / label / frequency_hz) modelled exactly on `[[mumble.servers]]` (`load_dvap_modules` + `resolve_dvap_modules`, fail-loud validation), with the remote-control password a secret (`dvap_remote_password` in radio-secrets.toml). `DvapManager` **caches** confirmed state — `status()` is I/O-free so `GET /status` never blocks; `refresh()` does the bounded UDP round-trips and marks an unanswered module `reachable:false` rather than failing the snapshot. Routes `/dvap/{status,link,unlink}` run the blocking client off the loop via `asyncio.to_thread`, publish the confirmed post-refresh block as a `dvap` WS event, and map 404 (unknown module) / 422 (bad reflector) / 503 (unconfigured or gateway-unreachable); the block embeds in `/status` and self-nulls when unconfigured (the config-driven-visibility pattern the web card keys off). Off by default (no `[[dvap.modules]]` → no manager, no socket); D-STAR module A untouched. Verified on fakes (`MockRemoteControlClient` modelling a tiny gateway): manager round-trip + cache + error handling, `resolve_dvap_modules` validation, and the routes' full flow / error codes / graceful degrade — 1278 passed. The web DvapPanel card is a follow-up PR; the gateway remote-control enable + DVAP-node standup is an operator deploy step | Accepted |
| [0097](0097-crossband-content-liveness-and-over-cap.md) | **The crossband's first bring-up stuck-keyed the transmitter — content-gated liveness plus a hard per-over ceiling.** A supervised dummy-load test (2026-07-20) keyed the FM side off a real D-STAR over and then **held PTT on dead air well beyond the over**; it was safed by stopping the service. Two layered, DI-injected bounds on `DStarBridge`: an `AudioLevelGate` on the **decoded** reflector audio, so a dead-air frame neither keys a fresh over nor refreshes the idle deadline (the decode is published to the browser monitor *before* the gate, so a garbled decode stays diagnosable), and `dstar.max_over_seconds` (default 60 s), armed at key-up and **never reset per frame**, sitting below `tx.tot`. | Accepted; **liveness rule superseded by 0106** (the over-cap survives) |
| [0098](0098-dstar-reflector-decode-pipeline-alignment.md) | **The garbage decode was pipeline ordering, not bit format.** The same bring-up decoded real off-air AMBE (an ID-51A via DVAP → ircDDBGateway → DSRP) into **noise** in the browser *and* on FM, so the fault was the decode, not the FM path. The AMBE2000 is a **pipeline** — replies lag their input — and a single-value slot dropped and reordered them. Adds `open_decode_stream() -> DecodeStream` (`decode`/`flush`/`close`) backed by an **ordered FIFO**, with a fixed prime/flush of `decode_flush_frames` (≈8, marked tunable) rather than a fragile per-over `STOP/START`, and routes the crossband through it. The DVAP firmware already de-scrambles and de-interleaves, so **adding a transform to the 9 AMBE bytes would be wrong**. Later confirmed by ear in a listen-only phase. | Accepted |
| [0099](0099-crossband-vocoder-wedge-failsafe.md) | **The crossband must fail safe when the DV Dongle wedges.** The **second** joint dummy-load re-proof failed: real off-air D-STAR keyed the FM transmitter, put **dead air** on the air, **stayed keyed**, and the graceful `systemctl stop` then hung ~15 s before PTT dropped. Three coupled fixes: `_teardown` calls `_force_unkey()` **first** (the direct, codec-independent `radio.ptt(False)`) and only then closes the vocoder **off the event loop** under a bounded wait — reversing ADR 0091's ordering, which was itself the stall; `close()` no longer blocks ~15 s on a contended `_io_lock` but closes the port directly, which is what unwedges an in-flight `_recover`; and a wedged decode fails the over fast and heals at the **start of the next over**, never mid-over. | Accepted |
| [0100](0100-dvap-autoheal-usb-wedge.md) | **The DVAP autoheal must restart-until-the-log-confirms-the-dongle-opened.** The two DVAPs backing gateway modules B/C go **silently deaf** — still running, nothing logged. A bench session characterised two modes: a roughly hourly USB **re-enumeration** that leaves `dstarrepeater` on a stale fd, and a flaky **first open after any abrupt close** that *alternates* good/bad, so a single unverified restart heals ~50 % of the time **and can wedge a healthy dongle**. `scripts/dvap-autoheal.sh` and its `--user` unit now detect both (the instance's own log outcome, and `/proc/PID/fd` against the `by-id` target), restart, wait 6 s and **verify the open**, retrying up to 4 times. No USB reset is available (root-only node), so a silent same-`ttyUSB` re-enum stays undetected. | Accepted |
| [0101](0101-dsrp-registration-fix.md) | **DSRP registration corrected against a real gateway: the poll carries the callsign, and `0x0B` is inbound-only.** `rx_frames` stayed **0** through every bench test, so the ADR 0097/0098/0099 re-proof could not even begin. radio-server was sending a gateway→repeater `0x0B` **backwards**, and the gateway logged `Unknown packet from the Repeater` **249× in one afternoon**. `build_register()` is removed; `poll()` now carries the full 8-char `format_callsign()` name, one cadence replaces the register(30 s)+poll(60 s) pair, and the interval drops 60 → **10 s** to stay inside the gateway's repeater-inactivity timeout. Every earlier "proven on hardware" registration claim had used the Echo unit, which needs no registration. | Accepted |
| [0102](0102-aioc-playback-pacer.md) | **AIOC playback pacer: no blocking audio writes on the event loop.** The crossband decode was A/B-proven perfect on `backend="mock"` and unintelligible — clicking, fragmented, long FM tail — on `backend="baofeng"`, with the same dongle, pipeline, gateway and reflector, on a bench where every other suspect had been eliminated live. `AiocBaofeng.transmit` was performing blocking `RawOutputStream.write` calls on the loop. A per-keying daemon writer thread with a bounded drop-oldest FIFO now owns every write and producers enqueue without blocking. Deliberately a **parallel implementation, not a shared abstraction** with kv4p's Opus slot-timer pacer. | Accepted |
| [0103](0103-remote-control-reauth.md) | **The remote-control client re-authenticates after gateway session loss.** `UdpRemoteControlClient` (ADR 0095) cached `_authed = True` forever, but the gateway keeps a **single live login and a restart drops it** — afterwards queries are ignored (observed as a `sendto err 22` flood) and commands discarded, with nothing to notice. A `RemoteTimeout`/`RemoteAuthError` on an authenticated round-trip now clears the flag, logs in once and retries once; link/unlink go through `_ensure_auth()`. The DVAP panel's status poll heals the session within one interval, with no radio-server restart. A bad-password NAK still raises immediately. | Accepted |
| [0104](0104-shutdown-stop-budget.md) | **Shutdown fits the stop budget — bounded, concurrent joins everywhere.** Stopping radio-server repeatedly overran the deployed unit's `TimeoutStopSec=10`, so systemd escalated to **SIGKILL, which severs the DV Dongle mid-operation and wedges it** for the next session. The D-STAR bridge joined up to four cancelled tasks *sequentially*; they now join **concurrently** under one `wait_for` (worst case ~12 s → ~3 s), the Mumble link bridge and RX pump get the same bounded treatment, and the deployment unit ships **`TimeoutStopSec=20`** with the rationale recorded in it: the stop timeout must never be what fires first. | Accepted |
| [0105](0105-dstar-reheader-continuity.md) | **Same-stream re-headers must not cut the over (the 12-fragment shredder).** DSRP superframes re-send the header mid-stream, and the bridge treated each one as a new over — shredding a single transmission into 12 keyed fragments. It now tracks the inbound session id: a re-header carrying the **same** id is absorbed (no `_end_rx`, no re-key, no new decode stream) and counted as `rx_reheaders`, while a **different** id ends the old over with its tail flushed, so a genuine talker change no longer strands the previous stream's last frames. | Accepted |
| [0106](0106-dstar-stream-liveness.md) | **Over liveness follows the stream, not the silence (the phrase-chopper).** ADR 0097's content-gated liveness cut the carrier in the pauses between words, chopping 43 s of speech into 9 overs. Liveness becomes **frame arrival** — `TxSession.touch()` on every accepted DATA frame — so a pause holds the carrier with silence, which is normal repeater behaviour, while a drain loop parked in a wedged decode still starves the deadline and the ADR 0092 watchdog still fires. The gate keeps governing *keying and feeding* only, so dead air never keys a fresh over, and content silence gets its own longer bound: `dstar.dead_air_seconds` (default 10 s, `0` disables). | Accepted |
| [0107](0107-decode-throughput.md) | **Decode keeps up with the stream — a non-blocking drain and honest loss counters.** The decode stream blocked mid-over and the counters hid ~14 % self-inflicted frame loss. `decode()` now drains opportunistically, with the wire itself pacing the pipeline (input arrives at 50/s, the wire drains ~62/s, so the write side cannot run away) while `flush()` stays bounded-blocking for the tail; `rx_seq_lost` moves to the enqueue side so wire loss and queue loss are no longer conflated. | Accepted; **stall-detection claim amended by 0108** (its throughput numbers should be read with suspicion) |
| [0108](0108-decode-starvation-alarm.md) | **A decode-stream starvation alarm, and the bench end-to-end self-test.** ADR 0107 claimed mid-over stall detection survived the non-blocking drain. It did not — a dead pipeline returned `[]` forever with every counter healthy. The stream now stamps a progress clock and raises `VocoderTimeout` if nothing has emerged for a full `reply_timeout` past priming, restoring the loud-failure contract without restoring the block. `scripts/bench/dstar_decode_selftest.py` injects a synthetic DSRP over while counting decoded PCM on `/audio/dstar/rx`, and is **mandatory before handover** for any deploy touching `radio_server/dstar/` or `radio_server/vocoder/`. Its `real` mode **keys the crossband** — dummy load only. | Accepted |
| [0109](0109-dvap-qualified-unlink.md) | **DVAP Disconnect: a blank `LNK` drops a fixed link; `UNL` cannot.** Disconnect was a silent no-op. Proven against the live gateway: a defaulted `UNL` matched nothing, and a fully-qualified one was refused — *"Cannot unlink REF030 C because it is fixed"* — because every link this manager makes is `Reconnect.FIXED`. `unlink()` now reads the module's confirmed state and, when a link is live, sends `link(callsign, "", Reconnect.NEVER)`. `UNL` is not used at all. | Accepted |
| [0110](0110-uvk5-quansheng-dock-wire-codec.md) | **UV-K5 (Quansheng Dock): the wire codec, and nothing else.** Opens the UV-K5 backend arc with the pure, I/O-free layer: `crc16` (CRC-16/XMODEM) and the self-inverse XOR `obfuscate`/`deobfuscate` — the divergence from kv4p, which has neither — `build_frame()` mirroring the client's `SendCommand2` byte-for-byte, and `Uvk5Decoder`, a stateful streaming deframer. Imports only the stdlib and nothing from `radio_server.*`. | Accepted |
| [0111](0111-uvk5-dock-transport-and-control-path.md) | **UV-K5 (Quansheng Dock): the serial transport, and the control-path decision.** Control is **BK4819 register-write tuning**, confirmed viable against the pinned firmware source with no hard blocker: `0x0870` enters hardware-control mode, which clears the display and sits in a loop servicing only serial commands until `0x0871` restores the radio — so the radio's own logic is suspended and host register writes are not fought. Channels are therefore **server-side presets**, not radio memory, which is the premise ADR 0115 builds on. | Accepted |
| [0112](0112-uvk5-radio-class-and-keying.md) | **UV-K5 (Quansheng Dock): the `Uvk5Radio` CatRadio class and register keying.** The byte-exact derived sequences, tested against a fake: frequency in **10 Hz** units with the band bit in `reg33`; `FM`/`NFM` as `(0x43, 18856)`/`(0x43, 18440)`; CTCSS as `code = ((round(tone*10) * 206488) + 50000) // 100000`. Keying is a register write **confirmed by read-back**, never a CAT `TX` command (guardrail 2). | Accepted |
| [0113](0113-uvk5-audio-shared-soundcard-seam.md) | **UV-K5 (Quansheng Dock): AIOC audio via a shared sound-card seam.** Extracts `backends/soundcard.py` — the ADR 0102 daemon-thread TX pacer, the canonical 48 kHz s16le mono capture/playout openers, and the lazy `sounddevice` import/injection seam — all of it PTT-independent and moved verbatim, so the change is behaviour-preserving. Both the AIOC and UV-K5 backends then ride it. | Accepted |
| [0114](0114-uvk5-config-factory-doctor.md) | **UV-K5 (Quansheng Dock): the config block, factory, doctor and setup docs.** The `[uvk5]` block mirrors `[kv4p]` with two **REQUIRED, fail-loud** fields rather than invented defaults: `uvk5.serial_port`, because the AIOC enumerates as an ambiguous `/dev/ttyACM*` and a guessed default would point at the wrong device on any bench with two ACM adapters; and `uvk5.frequency`, because in full-control mode the host owns tuning and there is no radio-side value to preserve. Adds the factory entry, doctor coverage, and `docs/uvk5-setup.md`. | Accepted |
| [0115](0115-channel-presets.md) | **Channel presets: server-side named tuning entries.** Radio channels live host-side, not in radio memory (ADR 0111; the `CatRadio` backends omit `SET_CHANNEL`) — a "channel" is a `{frequency, tone?, mode}` **preset** the operator names in a top-level `[[presets]]` array-of-tables and applies through the existing `set_frequency`/`set_tone`/`set_mode` surface. v1 is **simplex** (repeater-output monitoring from the desk); split/offset is a named follow-on that would touch the `CatRadio` interface itself. New `radio_server/presets.py`: a frozen `Preset`, a self-contained EIA 38-tone `CTCSS_TONES` set + `FM`/`NFM` modes, `resolve_presets` (fail-loud like `resolve_mumble_entries` — bad tone / dup name (case-insensitive) / ≤0 frequency / unknown field / bad mode stop startup), the pure `split_preset_fields` (honoured/skipped in the `Capability` vocabulary), and `apply_preset` (anchor-first, capability-gated per field). Config rides the `[[mumble.servers]]` recipe: `load_presets` + a `_flatten` top-level peel-off; `save_settings` leaves the hand-authored block untouched; `render_example` ships commented examples. API: `GET /presets` (lists + per-backend honoured fields) and `POST /presets/apply` (`{name}` → `{applied, skipped, status}`, pushes a `status` event) with interlocks from existing discipline — **404** unknown name, **501** on an audio-only backend (like `/frequency`), **409** refused mid-TX (the arbiter's refuse-don't-key posture, ADR 0017), stop-scan-first (ADR 0028), **422** on an out-of-band frequency; no parallel state store (reuses `status_event`, ADR 0076/0077). No UI (next cycle), no new deps, backend suites untouched | Accepted |
| [0116](0116-channel-presets-web-ui.md) | **Channel presets: the web UI.** The browser surface for ADR 0115 — a **Channels** card (`web/src/components/PresetControl.jsx`) where tapping a named channel tunes the radio via `POST /presets/apply`. No server change, no new deps. Patterned on `DStarPanel` (tap-a-preset button row, self-hide, `role="alert"` errors) with the two existing hide gates and **no third state**: the `SET_FREQUENCY` capability gate is a conditional mount (`{hasCap("set_frequency") && …}`, the same predicate as `showDial`), and config-absence self-hides (empty `GET /presets` → `return null`, like DStarPanel/LinkPanel/DvapPanel). The active-channel highlight is **derived, never stored** — `activePresetName(presets, state, hasCap)` matches a preset's honoured fields against the live `state.frequency`/`tone`/`mode`, comparing tone/mode only where the backend advertises them, highlighting only on an **exactly-one** match (ambiguity → none; a manual tune-away clears it naturally). Skipped honoured fields from the apply response show as a `notice` (non-silent); a mid-TX **409** / out-of-band **422** / **404** surface as the `role="alert"` error a `/frequency` failure gets, a **501** greys via `onUnsupported`. Reactive across browsers for free — apply publishes a `status_event` (ADR 0076/0077), so every open tab's readout + highlight update with no polling. `api.js` gains `presets`/`applyPreset`; new `PresetControl.test.jsx` (Vitest) + an extended `ControlPanel.test.jsx` gating test; Python suites untouched. Out of scope: preset editing in the UI (file stays source of truth), split/offset | Accepted |
| [0117](0117-uvk5-tx-timeout-wiring.md) | **Wiring the UV-K5 into the transmitter time-out (the stuck-key gate).** ADR 0112 flagged that the docked UV-K6 full-control loop has no device-side TOT (unlike kv4p's firmware `RUNAWAY_TX_SEC ≈ 200 s` or a UV-5R's TOT menu) — the server is the only protection. Reconnaissance-first: the server-side TOT **already exists** (`TotRadio`, ADR 0090, wrapping every backend at `build_radio`), so this is **extension/wiring, not invention**. Placement **follows** ADR 0090's `Radio`-boundary decorator, not the arbiter (`RadioArbiter` is a timer-free latch that misses `/ptt` and one-shot `transmit()`). The correctness crux is already satisfied: `TotRadio._fire()` calls `ptt(False)`, which on the UV-K5 routes to the full `_key_off` (RX registers restored **first**, then audio teardown) — proven on the firmware-accurate `FirmwareFakeSerial` (`registers[0x30] == _reg30` after expiry), not a bare PTT drop. Three wirings: (1) a first-class **`"alarm"` event** (`{kind:"tx_timeout", tot}`) surfacing the forced unkey on the reactive `/events` path (the previously-unwired `on_timeout` hook), **thread-safe** via `loop.call_soon_threadsafe` since `TotRadio` fires on a `threading.Timer` thread — wired at construction for a swapped radio (`build_radio(on_tot_timeout=…)`) and post-construction for the initial one (`TotRadio.set_on_timeout`, the controller-`on_event` pattern); (2) a **mandatory per-backend `uvk5.tot`** (backend-declared `DEFAULT_TOT = 180`, `coerce_uvk5_tot` rejects 0/negative and any value above the default — may shorten, never disable/weaken) resolved per-backend in `build_radio` so a global `tx.tot=0` never disables the UV-K6's cap; other backends keep the global `tx.tot` + their device-side story. Residual named honestly: in-process covers logic bugs/runaway/leaks/`SIGTERM`, **not** `SIGKILL`/panic/power-loss (radio stays keyed till power-cycle) — an out-of-process supervisor is a named follow-on, documented in `uvk5-setup.md`. Canary 89→90 (`uvk5.tot`); no new deps, no web change | Accepted |
| [0118](0118-uvk5-v3-firmware-fork.md) | **UV-K5 V3 firmware fork: pin the base and prove the build (F1 — the no-change gate).** The bench radio is a **UV-K5 V3** (PY32F071 MCU), not the DP32G030 that nicsure's "Quansheng Dock" firmware (the ADR 0110 pin) targets — so that firmware **can never run on it**. Plan for the arc: add radio-server's four dock ops (0x0870/0x0871 enter/exit full-control, 0x0850 write, 0x0851 read → 0x0951 reply) to the **V3 community firmware ourselves**, wire-identical to the classic Dock so `radio_server/backends/uvk5/{frames,transport,radio}.py` and all 1487 tests change **zero**. F1 ports nothing — it forks `armel/uv-k1-k5v3-firmware-custom` (F4HWN Fusion) under `kbennett2000`, pins tag **`v5.7.0`** / commit **`3bd3ebb`** (Apache-2.0), and proves the build→flash→boot pipeline with an **unmodified** build. Headless Docker build (ARM GNU 13.3.Rel1): `docker build -t uvk1-uvk5v3 . && docker run … cmake --preset Fusion && cmake --build --preset Fusion` (upstream's `compile-with-docker.sh` needs `-it` stripped for no-TTY). **Byte-identity not achievable — recorded honestly:** the official `f4hwn.fusion.v5.7.0.bin` is a **committed artifact** (`archive/…`, embeds commit `0567f01`, 2 commits *before* the tag — a pin discrepancy à la ADR 0110), and even with identical source+config the build diverges 8867/103544 bytes (8.56%) scattered — the signature of `-flto=auto` codegen + a different release environment, not a config diff. Verified instead: clean build, valid `.bin/.elf/.hex`, size within 4 bytes, identical version/author/edition strings. `BENCH.md` (fork) documents the uvtools2 V3 flash path (DFU/FTDI/tab-conflict/calib-dump) with bench-specifics flagged `⚠ CONFIRM AT BENCH` (guardrail 1). Both trees Apache-2.0 → F2 may **port** nicsure's `uart.c` with attribution (unlike the GPL client, spec-only); fork `NOTICE` updated. F1 bin delivered as fork pre-release `radio-server-f1-v5.7.0`. **No radio-server code touched → suite stays 1487/5.** Acceptance (Kris, bench): flash the F1 bin over release Fusion, boots/RX/keypad identical → green-lights F2. Small cycles: F1 build gate → F2 protocol port → F3 bench loop | Accepted |
| [0119](0119-uvk5-v3-dock-protocol-port.md) | **UV-K5 V3 firmware: the dock protocol port (F2).** With F1 (ADR 0118) merged and bench-confirmed, port the dock control mode into the fork (`f2-dock-port` @ `32c600b`) so radio-server drives the V3 over the wire protocol it already speaks to the classic Quansheng Dock — **byte-compatible with the untouched `FirmwareFakeSerial`/`Uvk5Decoder`, so `radio_server/` and all 1487 tests change ZERO** (a genuine V3 wire difference would be STOP-and-report; none found — the V3 stock framing is already byte-identical). Surface is tiny (registers do everything): `0x0850` write / `0x0851` read → `0x0951 RegisterInfo`, `0x0870`/`0x0871` enter/exit full-control; no keypress/screen/scan/GPIO/AM/FSK/modulation. Design: a **pure, host-compilable** `App/app/dock.c`+`dock.h` (framing `AB CD`/`DC BA`, 16-byte XOR, CRC-16/XMODEM, command-CRC validation, dummy `obf(FF FF)` replies, register dispatch, streaming deframer) behind a thin `dock_hal_t`; `App/app/uart.c` binds it to `BK4819_Read/WriteRegister`+`UART_Send`, extends the existing dispatch `switch` and adds the `0x0870` blocking loop (re-enters dispatch for in-loop R/W, nicsure's shape) — surgical, `ENABLE_DOCK`-gated, on in Fusion. **The spec is the fake:** a `tests/host/` gcc harness mirrors `FirmwareFakeSerial`'s acceptance/dispatch/reply rules incl. a byte-exact `0x0951` vector — **19/19 pass** (pre-flash proof). Ported from nicsure's Apache-2.0 `uart.c` (@ `4375c3e`) **with attribution** (NOTICE + file headers), unlike the GPL client (spec-only). V3 derivation (from the tree): **no watchdog to feed**, no async ISR fights the BK4819 (blocking = the quiesce), 38400 already, encryption always-on (toggle never on radio-server's dock path). Flash fits: **104,116 B / 118 KB (86%), +572 B** over F1. Docs-only in radio-server (ADR + README + HANDOFF); `uv run pytest` still **1487/5**. F1 bin → pre-release `radio-server-f2-v5.7.0`. Acceptance (Kris, bench): flash → `doctor --backend uvk5` connect probe (the `ReadRegisters(0x30)` elicit answering IS the port working) → the four F1 gates → green-lights F3 (bench loop) | Accepted |
| [0120](0120-uvk5-v3-rx-audio-path.md) | **UV-K5 V3: RX audio was dead in dock mode — the audio-path amp (F3a).** Live-bench diagnostic: with the F2 build, received audio to the AIOC read the **noise floor** (~110 RMS) where an early HT-keyed run read 12431. Cause, proven with register dumps from a fresh power-cycle (never inferred): RX audio passes **two gates** — BK4819 `REG_47` (AF selector, MUTE `0x6042` / FM `0x6142`, reachable over the dock) **and `GPIOA8`, the external audio-amp enable, an MCU GPIO the dock CANNOT reach**. radio-server holds the `0x0870` full-control loop for its whole life, starving the firmware timeslice so `APP_StartListening()` never runs to raise GPIOA8 / unmute `REG_47`. Over the dock we forced `REG_30=0xBFF1` + `REG_47=0x6142` + `REG_48=0xB3A8`, **confirmed all three by read-back**, and the AIOC still read 109 RMS — BK4819 fully RX-alive yet silent ⇒ the dead gate is outside the BK4819 = GPIOA8. Only the firmware can raise it. **Fix (fork `f3-rx-audio-fix` @ `79f9b21`):** `Dock_EnterFullControl` now calls `GPIO_EnableAudioPath()` + `BK4819_SetAF(FM)` + `BK4819_SetRxAudioGain()` up-front, so RX audio flows the whole session; `0x0871` exit re-baselines (audio-path off, `REG_47`→MUTE — this also settles F2's resume-RX flag). `+28 B` (**104,144 B / 118 KB, 86.2%**); `dock.c` untouched → host harness stays 19/19. **radio-server change is the instrument only:** `doctor --rx-noise` — an HT-free RX self-test that force-opens the receiver, measures the AIOC, and restores every register (guardrail). The dock protocol + `backends/uvk5/*` change ZERO; `uv run pytest` **1492/4** (the 4 new tests). F3 bin → pre-release `radio-server-f3-v5.7.0` (sha256 `f9a3cc2d…`). Acceptance (Kris, bench): flash → `doctor --backend uvk5 --rx-noise` reads thousands (was floor) → live RX + the post-fix TX check | Accepted |
| [0121](0121-per-backend-squelch.md) | **Per-backend squelch mode (unbreak the mixed-radio box).** `audio.squelch` was one global mode, and ADR 0074 validates every configured backend against it — so `audio.squelch=cat` (which the docked UV-K6 needs post-F3) plus any configured audio-only block (`[baofeng]`) was **unstartable**: the inactive `[baofeng]` block was validated against the global `cat`, hit the "UV-5R has no busy line" guard, and boot aborted. Found live on the bench (stale `[baofeng]` block + uvk5). Fix mirrors the `uvk5.tot`/`tx.tot` pattern: uvk5 and baofeng get their own **`squelch_mode`** key with **backend-declared defaults** (`uvk5`=`cat` — dock mode force-opens the AF path so software VAD can't gate and `off` never segments; `baofeng`=`audio` — no busy line), and any backend without a key (kv4p/mock) falls back to the global. **Naming:** `squelch_mode`, not `squelch` — `[kv4p]` already owns `squelch` (an SA818 *level* 0-8); a flat registry rules out an `[audio.<backend>]` table. `resolve_squelch_mode(settings, backend)` (in `activity/gate.py`) is the one source of truth: `validate_backend_config` now checks each backend against ITS **effective** mode, so the stale-`[baofeng]`-blocks-`cat` failure disappears by construction (the guard still bites an *explicit* `baofeng.squelch_mode=cat`); misleading messages reworded to name the SECTION, never the active `server.backend`. `build_rx_gate` resolves the active backend and is **rebuilt in `RadioHolder.rebuild`**, so a live switch re-selects the gate type AND re-points `CatBusyGate` at the new radio (closing a latent stale-radio bug). The recording-`off` safety rail reads the effective mode too. Back-compat: realistic single-global configs resolve unchanged (documented divergence: a uvk5/baofeng box that relied on the global for a *non-default* mode now uses its backend default). No backend construction-arg change (`squelch_mode` is a gate/composition concern like `tot`). Canary 90→92; `radio.toml.example` regenerated; no new deps. `uv run pytest` green | Accepted |
| [0122](0122-uvk5-v3-f3-bench-loose-ends.md) | **UV-K5 V3 F3 bench loose ends (reproduce, fix, instrument).** Five host-side loose ends from the F3 bench, all RX/register-side, no TX, no firmware change. **(1) First-start dead RX** — the RX force-open is firmware-side (`Dock_ForceRxAudioAlive`, ADR 0120), triggered by radio-server's single **fire-and-forget, unverified** `send(EnterHwMode())` (0x0870); a 0x0870 lost in the reset-on-open boot race → dead RX until restart (radio leg), vs a capture stream opened against a settling USB device (host-audio leg). GPIOA8 is un-dockable, but the same firmware routine sets `REG_47`=FM — a host-visible **proxy**, so `REG_47` vs AIOC-RMS splits the legs. New `doctor --rx-firststart-loop N` (open→dump→measure→close, per-iter leg verdict + `dead/N`, step-0 F3 probe, cold-boot fidelity caveat); host-side fix `_enter_hw_mode_verified` (verify REG_47→FM, bounded re-send — shipped unconditionally as a strict robustness upgrade) + a default-OFF `capture_reopen_on_floor`. **(2)** WS handlers now swallow shutdown `CancelledError` (no teardown traceback). **(3)** `measure_rx_levels` primes one read before starting the clock, so stream-open latency no longer biases the true-rate estimate low — the kv4p **+2%** finding (pure formatter, hardcoded inputs) is untouched. **(4)** New `doctor --rssi` live meter (raw reg-0x67 counts + busy verdict vs `uvk5.squelch_threshold`). **(5)** The "HELLO not answered" message is reworded: the V3 fork is always-encrypted and drops the plaintext-0x0514 toggle (ADR 0119), so an unanswered plaintext HELLO is **expected**, not a fault (dock-alive is already proven by the register elicit). `frames.py`/`transport.py` unchanged (F2 invariant). **Live validation deferred:** the dev-PC HT was unresponsive over the dock (AIOC audio leg healthy, but zero bytes / register elicit timed out across 4 attempts — powered off or not on dock firmware); the live repro before/after counts (item 1) and live RSSI (item 4) are a bench acceptance for Kris. All five proven hardware-free — `FirmwareFakeSerial` extended with an F3 force-open + droppable-0x0870 model; `uv run pytest` **1528/4** (was 1510/4). **Live validation done 2026-07-24 (addendum):** item 3 PASS live (rate **47,999 Hz**/30 s; `--rx-noise` LOUD), item 4 PASS live → **`squelch_threshold` set from data 40→220** (idle ~159/max 163 vs keyed ~311); item 1's `_enter_hw_mode_verified` fix confirmed good (**10/10 completed opens ALIVE, REG_47=FM, F3 CONFIRMED, zero dead-RX**) but the loop can't reach 20-clean — a **reproducible harness defect** (construction-time reset-on-open `Uvk5Timeout` crashes the un-wrapped `_build_backend` at the ~6th rapid reopen), filed as a follow-up harness-fix cycle; no firmware implicated. **Item 1 fully accepted 2026-07-24 ([ADR 0123]):** the harness fix landed and `--rx-firststart-loop 20` ran **0 dead / 20** on the bench (F3 CONFIRMED, all ALIVE) | Accepted |
| [0123](0123-uvk5-v3-rx-firststart-loop-harness.md) | **UV-K5 V3 `--rx-firststart-loop` harness fix + the real 20-count.** ADR 0122 proved the first-start dead-RX **fix** works (10/10 completed opens ALIVE) but the **instrument** crashed at the ~6th open: `_build_backend` → `Uvk5Radio.__init__`'s non-retransmitting seeding reads hit the reset-on-open race, and construction sat **outside** the per-iteration `try`, so a `Uvk5Timeout` killed the whole run. Host-side (`doctor`) only, no firmware change. **(1)** Construction moved inside the `try`; a surviving construct-timeout is counted `DEAD/RADIO` (`REG_47=------  construct timeout after N attempts`), not a crash. **(2)** New `_build_backend_settled(cfg, *, attempts, interval, sleep)` catches **only** `Uvk5Timeout` and retries the whole build — the harness analogue of `connect()`'s probe retransmit (ADR 0111, same tolerance): `_RX_FIRSTSTART_CONSTRUCT_ATTEMPTS=3` mirrors `_ENTER_HW_MODE_RETRIES`, interval reuses `_ELICIT_RETRANSMIT_INTERVAL` (0.25 s); both the loop and the step-0 F3 probe build through it. **(3)** A `_RX_FIRSTSTART_SETTLE_S=1.0 s` inter-iteration settle (before every iteration but the first) makes each open a genuine single-reset first-start, not a rapid-reopen reset pile-up — a **realism** decision that defines what "N-clean" means (marked `VERIFY ON BENCH`; after the fix, any non-clean count is a real finding, not re-run to green). **Live: `0 dead / 20`** on the bench (F3 CONFIRMED, all ALIVE, `REG_47=0x6142`, peaks ~7.3–8.2 k) — the acceptance 0122 deferred. Transport/backend untouched (F2/ADR 0119 invariant). +5 tests (droppable construction); `uv run pytest` **1533/4** | Accepted |
| [0124](0124-aioc-alsa-card-id-device-resolution.md) | **AIOC sound-card addressing: resolve ALSA card ids, not just PortAudio names.** `doctor --rx-noise` on the LAN server failed with `No input device matching 'AIOC_K6'` while the connect probe passed ALL. The udev rule naming the card off its USB serial had **already applied** (`/sys/class/sound/card2/id -> AIOC_K6`; `udevadm test` shows it firing) — it had merely been authored *after* the reboot it was tested against. The real defect is a **three-layer name mismatch**: udev's `ATTR{id}` sets the ALSA card **id** (`AIOC_K6`), but the card **name** comes from the USB product string (`All-In-One-Cable`) and is not settable by udev, and PortAudio device names — the only thing `sounddevice` substring-matches — are derived from that *name*. So the config could never resolve and **no udev rule could ever fix it**; the error text is sounddevice's own, which is why it appears nowhere in this repo. Re-pointing `radio.toml` at the working PortAudio name was rejected: it names the *product*, not the *cable*, and a second AIOC (2026-07-25, UV-5R) presents an identical name — the serial is the only per-cable discriminator, and the udev rule already projects it into a stable card id; nothing could *consume* it. **Decision:** `resolve_device(sd, device, *, kind, sysfs_root)` in the shared `backends/soundcard.py` seam both backends already funnel through (`open_capture_stream`/`open_playout_stream`), plus `doctor._check_audio` (which now *reports* the mapping). Order is **existing behaviour first** — int/None, then PortAudio name substring, and only then card id → `/sys/class/sound/card*/id` → index → the `(hw:N,…)` device with channels in the direction being opened; unresolvable strings are handed back so sounddevice raises its own familiar error. A seam without `query_devices` (the backends' injected test doubles) and a `query_devices` blow-up both pass through — regression guards, not defensive noise. Completes the two-AIOC scheme: `ATTRS{serial}` → card id → index, so Saturday is one uncommented rules line plus `AIOC_UV5R` in config. Rule also tidied host-side (`KERNEL=="card*"` scoping; second line commented). **Bench:** post-fix `doctor --rx-noise` with `radio.toml` **unchanged** → **peak 5299 RMS (-15.8 dBFS), avg 4123**, RX ALIVE; connect probe ALL PASS. Recorded for next time: a bare `arecord` on this card reads **floor (67 RMS)** and is *not* a valid RX test — only the backend runs the dock's `REG_47`→FM un-mute (ADR 0120/0122). `radio.toml` untouched; no firmware change. +16 tests; `uv run pytest` **1548/5** | Accepted |
| [0125](0125-uvk5-v3-rx-pump-cat-gate-decouple.md) | **UV-K5 V3 RX pump: decouple the CAT squelch read from the audio reader.** F4 server-deployment triage — the uvk5 stack passed on the dev PC (0120–0124) but degraded on the LAN server into four symptoms; **measure first.** Split into two chains: **Chain A — RX (choppy browser + no DTMF)**, this ADR; **Chain B — TX (no audio + no service)**, the ADR 0112/0113 acceptance gate, still **open** pending the bench `--tx-tone` run. **Bench, live:** `/audio/rx` for 30 s delivered **0 bytes**; a faithful pump-loop repro measured **14.3 %** duty with the CAT gate vs **98.5 %** without, `status()` p50 **100.2 ms** (pinned to `transport._READ_TIMEOUT`). **Root cause:** ADR 0121 made `cat` the uvk5 default squelch **one day earlier**, so `RxPump` now calls `CatBusyGate` — a ~100 ms serial `ReadRegisters(0x67)` — **once per 20 ms frame** on the single capture reader; it reads ~1 frame in 7, overruns the 60 ms ALSA ring, and since `busy` never returned True `hub.publish` never ran (0 bytes to the browser); DTMF, fed the raw pre-gate frame, gets only 7 frames/s → shredded. **One root cause, both RX symptoms.** Why the dev PC "passed": every 0120–0124 check ran through `doctor`, which bypasses the pump+gate. **Fix shape validated on hardware BEFORE writing it** (first hypothesis — inline caching — was **refuted**: tops at 84.4 %, still overflows, because any inline 100 ms read stalls the reader past the 60 ms ring): **decouple.** New **`PolledGate`** (activity/gate.py) runs the inner gate on a **background daemon thread** every `DEFAULT_CAT_POLL_INTERVAL`=0.2 s and caches the verdict; `__call__` returns a cached bool with **zero serial** in the audio path (bench: **10.5 % → 100.2 %** duty). `build_rx_gate`'s `cat` branch returns `PolledGate(CatBusyGate(radio))` (`.inner` is the CAT gate the live-switch rebuild re-points); `off`/`audio` untouched. **`RxPump`** owns the poller's lifecycle (`gate.start()`/`stop()` if present, guarded, restartable — maps to the demand-driven pump) and now **sleeps only when the read was empty** (`sleep(0)` yield on a real frame, `poll` only on idle — the old unconditional `sleep(poll)` was 50×20 ms/s = zero headroom, matching the docstring's long-standing intent). **Thread-safety:** the transport's reader-thread + per-register waiter matching already supports concurrent `status()`; during TX `status()` short-circuits without serial. **Instrumentation (the fault was invisible):** uvk5 `receive()` **discarded** the ALSA `_overflowed` flag — why the journal showed "zero xruns" — now a rate-limited (≤1/5 s) `WARNING`. A separate **ops** fault (clipping knob → DTMF shredded at the decoder) was fixed on the box (knob-down; `doctor --dtmf` then read `1234#1234#` clean) and recorded in `server-notes.md`, not here. Non-CAT backends byte-identical. No config/deps change. +18 tests; `uv run pytest` **1566/5** | Accepted |
| [0126](0126-uvk5-v3-dock-force-tx-chain-b-close.md) | **UV-K5 V3: dock-mode TX had no PA — closed by a firmware fix (Chain B / F5).** The F4 Chain B counterpart to ADR 0125's Chain A: browser TX / services not heard. **Measured with the kv4p as an objective RF sniffer** (both radios 445.800, UV-K5 inches away): software keys reliably (`tx_key_up` 5.64 s, `REG_30` CONFIRM passes); a dummy-load run saw the chip's modulated carrier (near-field), but the antenna run on a confirmed 5.7 s key saw kv4p carrier **False throughout** (`dual_tx_watch.py`: 9 polls keyed-WITHOUT-RF, 0 with) while RX still worked — the **external PA never engages**. **Root cause:** radio-server keys by writing only `REG_30` (TX_DSP); stock TX also drives the PA-enable GPIO + PA bias + `REG_50/37/52` un-mute, which a bare `REG_30` write skips, so the modulator lights up and nothing radiates. **Corrects the F4 "un-dockable MCU GPIO" framing:** on this PY32F071 port the PA-enable/antenna-switch/LNAs are **BK4819 GPIOs via `REG_33`** and PA bias is `REG_36` — dockable in principle, just never written by radio-server (not un-dockable like ADR 0120's `GPIOA8`). **Fix = firmware** (fork `f5-dock-force-tx` @ `79d522a`, fork ADR `adr/0001`): `dock.c` edge-detects the `REG_30` TX-enable bit and calls a new optional `dock_hal_t.tx_set` callback before completing the write; `uart.c` binds it to `Dock_ForceTx`/`Dock_EndTx`, adding exactly the stock PA steps and dropping them in stock order on un-key (plus re-opening the F3a RX audio path). PA slaved to `REG_30`, forced off at `0x0870`/`0x0871`; TOT untouched. **Zero wire-byte change → radio-server byte-identical** (no `frames.py`/`transport.py`/backend edit; `uv run pytest` **1566/5**, unchanged). Fork host dock tests 19→31; Fusion build 104,340 B / 118 KB (86.35%), +196 B; pre-release `radio-server-f5-v5.7.0` (sha256 `881ca3fd…`). Bench acceptance (Kris): kv4p carrier **True** while keyed where F4 showed none → browser TX + service heard → antenna range proof. | Accepted |
| [0127](0127-bounded-graceful-shutdown.md) | **`systemctl stop` never completed — bound uvicorn's graceful shutdown.** Every stop sat the full `TimeoutStopSec=20` in "Waiting for background tasks to complete", then got SIGKILLed (`Failed with result 'timeout'`) — **7 times in 14 h**, and one of them left the service down for **8 minutes** (the ConnectionRefused that started the stabilization mission). `uvicorn.run()` had no `timeout_graceful_shutdown`, whose default is *wait forever*; a browser holding `/audio/rx` + `/events` open keeps `server_state.tasks` non-empty indefinitely (confirmed in `ss` at the moment of a stop). Worse, uvicorn never got past the wait, so the **bounded lifespan teardown (ADR 0104) never ran at all** — no unkey, no decoder reap, no log flush, and SIGKILL is exactly what ADR 0104 warns wedges a USB vocoder. A `py-spy` dump 6 s into a hung stop also caught the blocking sound-card read executing **on the asyncio event loop** (`RxPump.run` → `radio.receive()` → `sounddevice._raw_read`), the open bring-up question flagged at `rx/pump.py:190` — degrading, but not the hang; left for its own evidence. **Fix:** `timeout_graceful_shutdown=5.0`, which cancels stragglers and *still* runs the lifespan teardown (`force_exit` is only set by a second SIGINT); plus `SuccessExitStatus=143` in the unit, because a clean SIGTERM stop legitimately exits 143 — uvicorn re-raises the captured signal on purpose. Declaring 143 is truthful only *after* the hang is fixed; before, the result was `timeout`/`status=9/KILL`, which 143 never matched. **Measured: 20.0 s + SIGKILL + no teardown → 5.48 s + `Result=success` + teardown runs.** | Accepted |
| [0128](0128-dock-tx-measured-pa-owned-by-firmware.md) | **Dock TX measured end-to-end: Chain B closed, and the PA belongs to the firmware.** The bench acceptance ADR 0126 deferred, plus two corrections only measurement could produce. New instrument `scripts/bench/uvk5_tx_regs.py` snapshots the BK4819 TX path idle/keyed/un-keyed (register state is ground truth: two radios inches apart means a PA-less modulator still couples a "carrier"). Keyed on 445.800: `0x30=0xC1FE`, **`0x33=0x9028` → PA_ENABLE `0x20` SET, RX_ENABLE cleared**, `0x37` → stock `PrepareTransmit` value. radio-server writes none of those — **so the F5 firmware is flashed and firing, and no flash was needed.** End-to-end via the deployed API with the kv4p as receiver: **4 carrier polls with RF** (F4: 0 with / 9 without), RMS 7615, 1000 Hz recovered at 0.990; a voice service heard at 4.8 s / RMS 2761 / 0.68 speech-band. **Chain B is closed by measurement.** **Correction to 0126:** the backend *did* write the PA bias (`0x36`) — but it is dead code, overwritten by `Dock_ForceTx`'s calibrated `SetupPowerAmplifier` on the `0x30` edge. Read-back proves it: **bias 12**, never the computed 255. Removed, along with `tx_power_pct` (internal-only: no config key, no docs, no test) — the host cannot read the SPI-flash calibration, so it can never compute a correct bias; the lever is the radio's own OUTPUT_POWER. **Answers 0126's verify-on-bench item: dock TX radiates at bias 12.** `uv run pytest` 1566/5 unchanged. | Accepted |
| [0129](0129-bench-acceptance-runner.md) | **A permanent, self-serving bench acceptance runner.** Every prior acceptance was a one-off in `/tmp` (wiped on reboot — which is why proofs kept evaporating), or a `doctor` run needing the service *stopped* (the ADR 0127 anti-pattern), or a human reporting what they heard ("Kris hears himself" is not a number). `scripts/bench/acceptance.py`: eight stages driving **the live HTTPS APIs of both deployed units**, exit 0 only if all pass, following the `dstar_decode_selftest.py` precedent. The two radios (445.800, inches apart) are each other's instrument — **no human keys anything**. Stages: `systemd`, `web`, `presets`, `rx`, `dtmf`, `auth` (real TOTP login over RF), `tx`, `services`. Measurement decisions: RX duty over the **active span** not the listening window (window-relative duty only reports the *transmitter's* duty cycle), paired with inter-frame gap + "whole over received" so a smooth *fragment* still fails; tone recovery as a **normalised** spectral ratio so thresholds survive a power/geometry change; DTMF digit shape from a **sweep** (120/60 ms decodes nothing — too short to hold the RX gate open; **200/100 ms @ 0.4** decodes `123456#`), which also proved this hardware genuinely needs `audio.dtmf_reverse_twist_db = 10.0` — the same capture decodes as only `14` at the stock 4 dB limit. On that evidence the **stashed `dtmf.py` hardcode was dropped**: superseded by the config knob, and it would have broken `test_native_dtmf.py:335`. `auth` forces logged-out before *and* after so consecutive runs stay independent. Watchers moved `scratchpad/` → `scripts/bench/` with env-driven credentials. | Accepted |
| [0130](0130-rx-read-off-the-event-loop.md) | **Read the sound card off the event loop, and stop counting expected overruns.** `RxPump.run()` called `radio.receive()` directly in the coroutine — its own docstring had flagged thread-vs-loop as an open bring-up decision since ADR 0029. The bench decided it twice over: a **`py-spy` dump of the deployed server** caught the asyncio loop parked in `sounddevice._raw_read` via `rx/pump.py:225`, and the card logged **33 overruns in 40 min** (~2.1/min under load) — literally the "the RX reader fell behind the card … a blocking call on the reader thread" warning the backend emits (ADR 0125 fixed the *other* such call; this is the read itself). Symptom: a fixed 5.0 s over arrived **5.70 → 3.88 → 3.28 s** across three consecutive acceptance runs — smooth (duty 100.5 %, max gap 40 ms) but progressively *short*, because dropped audio is absence, not a gap. **Fix:** `await asyncio.to_thread(self._radio.receive)`; the awaits are sequential so exactly one read is in flight and the backend still sees a single reader. **Plus** a dedicated `max_workers=1` reader executor (the shared default pool also serves the DVAP/D-STAR refreshes the UI polls; one run showed a read queued behind one — 94.9 % duty, 321 ms gap). **Plus** an overrun is only a fault if somebody was reading: the ring fills on wall-clock time, so it is *guaranteed* to overrun wherever reading legitimately stops (keyed over, the demand-driven pump halting when the last listener leaves, stream open, restart). Enumerating those missed the listener case, so the backend now measures the **gap since the previous `receive()`** and excuses the drain — and the warning states the gap, which is what cracked the residue: `xrun after 0.022 s since the previous read` — one block period, i.e. reading continuously, no stall. Verdict therefore **scoped to the receiving window** (whole-run count printed as information, not a verdict) — widening it counted the runner's own restart and every keyed over, the same mistake inverted. **Measured: span 3.28 s → 5.06/5.38/5.14 s stable, duty 100.4–100.8 %, max gap 40–41 ms, 0 xruns while receiving.** 12 pump tests moved from a single-turn `sleep(0)` to a bounded `settle_pump()` helper. `uv run pytest` 1568/5. | Accepted |
| [0131](0131-dock-link-robustness.md) | **The dock link drops frames, and three places assumed it never would.** With TX working (0128) and RX smooth (0130) the acceptance runner still failed ~1 run in 3, never the same stage twice. Running it repeatedly turned three intermittent faults into reproducible ones, all sharing a cause: **`Dock_EnterFullControl` is a *blocking* loop, so while the firmware is inside `Dock_ForceTx` (~25 ms of PA-sequence delays) it is not servicing its UART — a request arriving then is *dropped*, and writes are fire-and-forget so a lost write is silent.** (1) The key-up read-back raced that window and got the RX word for a transmitter coming up fine — this **is** the F4 "first-key settle flake" ADR 0126 carried forward, and it failed a live announcement with HTTP 500 and nothing on air. (2) Retrying the *read* made it worse — polling into the busy window gets the request dropped and burns the transport's full `Uvk5Timeout: no matching reply within 2.0s`. (3) Once the **write** is the casualty, reg 0x30 reads `0xBFF1` forever; no amount of re-reading finds a bit that was never set. (4) `status().busy` answered `False` on a timed-out RSSI read — and it drives the CAT squelch gate, so a dropped poll closed the gate on an over *in progress*: **4.19 s of a 6.0 s transmission**, perfectly continuous (98.2 % duty, 119 ms gap, zero overruns), just missing its head. Every symptom pointed at the receiver; the fault was missing data read as silence. **Fix:** settle 50 ms before confirming (wait the firmware out, don't poll into it); re-send the idempotent key-up up to 3× confirming each (the only thing that recovers a lost write); treat a timed-out read-back as one failed attempt, not an aborted over; hold the last known squelch state across a failed poll, bounded. **The ADR 0112 invariant is intact** — a radio that never keys still exhausts every attempt, still raises, still unwinds; a dropped frame merely stops masquerading as a broken radio. **Measured: 3 consecutive full acceptance runs PASS (exit 0), first straight off a cold boot**, vs ~1-in-3 failures before. `uv run pytest` 1574/5, one regression test per fault. | Accepted |
| [0132](0132-dock-band-and-the-register-model.md) | **The dock keys the band the RADIO is on, and the backend believed whatever it found.** Everything in 0127-0131 was proven on 445.800; moved to **147.555** the station went dead both ways while the API reported success throughout (200s, real `tx_key_up`/`tx_key_down` in the operating log). Four faults, only two about bands. (1) **`Dock_ForceTx` sets the PA up from `gCurrentVfo`** — the radio's own boot VFO — and deliberately never tunes the synthesiser, which the host owns via reg 0x38/0x39 (`uart.c:729-732`); the fork's own ADR flagged this ⚠ verify-on-bench and guessed it "would only mis-scale power". Measured keyed on 147.555: carrier correctly at 147 555 000, but reg 0x36 = `0x0CA2` (**UHF** gain byte) and reg 0x33 with the **UHF** LNA. (2) **`Dock_EndTx` never puts the LNA back** (`uart.c:740-746`) and `set_frequency` was the only thing that ever wrote reg 0x33 — so VHF receive worked until the first transmission and was deaf after: `0x9046` before keying, `0x9048` after. (3) **The backend adopted whatever state it found, for ever** — `_reg30` was seeded from a bare read at connect and written back on every un-key and every retune, so a radio found broken *stayed* broken. Reproduced: writing `0x30 = 0` (where a lost un-key leaves it) drops RSSI **157 → 0**, and a service started against that radio reported `rssi 0 / busy false` and passed **zero bytes** indefinitely — band-independent, and the better candidate for "the server doesn't hear me". (4) The reg-0x33 clear mask `& 0xFFE7` never cleared the VHF bit, so VHF→UHF left **both** LNA paths on; inherited verbatim from ADR 0112's self-inconsistent prose, and uncatchable by tests that all seed `reg33 = 0`. **Fix:** correct the band *after* the firmware's sequence rather than before (which is why 0128's write lost), **keeping the firmware's bias** — the calibration is in flash the dock cannot read, and inventing one is the mistake 0128 removed; restore the receive path on key-off; repair an impossible reg-0x30 seed with the measured stock word and *write* it; assert the receiving shape instead of replaying the seed; refuse to retune while keyed; expose the raw RSSI so the squelch threshold can be sized on a *running* station (floors **107 on 445.800, 154-156 on 147.555** — one scalar still covers both, so no per-band setting was added on speculation). **Measured on the deployed tree: all three VHF band checks ✘→✔, 445.800 unchanged, rx still 100.5 % duty / 1.000 tone.** `uv run pytest` 1585/5. **Not proven:** the bench has no 2 m receiver — the kv4p is a single-module SA818-UHF board — so VHF is proven at the register level only; `scripts/bench/rf_listen.py` covers it with a human, and the runner's RF stages now **skip with the reason** instead of emitting zeros that look exactly like a broken receiver (that output cost real time this session). | Accepted |
| [0133](0133-repeater-split-and-chirp-import.md) | **Repeater split, and 37 channels imported instead of retyped.** Presets were simplex by explicit decision (ADR 0115 named the follow-on: "a new `set_split`/offset concept every backend must implement"); this is that follow-on. `CatRadio.set_split(tx_hz)` + `Capability.SET_SPLIT` + `RadioStatus.tx_frequency`; overloading `set_frequency(hz, tx_hz=)` was tried and rejected — the repo's duck-typed `Radio` doubles take one argument, so a keyword they don't accept turns a `ValueError` path into a `TypeError` and a 422 assertion into a 500. **`set_frequency` CLEARS the split**: a TX leg outliving a retune would let an unattended station ID key a repeater uplink from a frequency nobody chose. The ordering is the substance and it is byte-order tested on a firmware-accurate fake: key-up **prepends** 0x38/0x39 to the existing key batch, safe because one `_write_registers` is one frame and a corrupt frame is dropped *whole* — so the tune and the TX-enable cannot land independently; key-down is **two frames**, the first byte-identical to the simplex un-key and the second literally `set_frequency`'s batch (the one write shape with a citation, ADR 0112), landing strictly after `Dock_EndTx` so the window where the PA ramps down on the repeater's *output* is structurally zero. The fakes had to be fixed first or none of it was tested: `ForceTxFake` evaluated the Dock_ForceTx/Dock_EndTx edge once per serial WRITE, after every pair of a frame had landed, so it would have sampled the un-key edge with the synthesiser already back on RX and **reported the hazardous ordering as normal**; the firmware's hook runs per register pair and the fake now does too. `tx_hz` is validated harder than `hz` (>10 MHz offset, crossband) because unlike a receive frequency it radiates. CHIRP CSV importer (`python -m radio_server.chirp`) whose posture is *never guess*: **TSQL takes `cToneFreq`, not `rToneFreq`** — 26 of the operator's 37 rows leave `rToneFreq` at 88.5 while the real tone is in `cToneFreq`, so the inverted mapping transmits 88.5 at repeaters wanting 100.0/103.5/107.2/123.0/141.3 and **not one would key**; DTCS/TSQL-R/split/off are skipped and printed; the one `Cross` row cannot be known from a 9-column export so it is assumed **and named on stdout**. All arithmetic integer — `(145.145-0.600)*1e6` is `144545000.00000003` and float subtraction lands 1 Hz low in **2160** cases across 2m/70cm, an off-raster 422 for one repeater in forty. **Measured on the bench:** CTCSS survives the witness's audio path (0.000026 → **0.109**, a 4250x ratio) so the tone is checked at RF, not just in a register; keyed on a split the kv4p hears **RMS 8188 / 1000 Hz 0.882 / CTCSS 0.109 on 446.400** and **RMS 0 on 445.800** — the carrier moved completely, no near-field bleed at 600 kHz. The runner **caught a real leak**: a tone-less preset did not clear the previous channel's CTCSS (`services` failed at speech-band 0.31), a pre-existing fault invisible until a channel list carried tones. `uv run pytest` 1653/5. | Accepted |
| [0134](0134-repeater-keyup-in-the-field.md) | **No repeater keys up in the field — four hypotheses killed with evidence, and the two survivors made visible.** ADR 0133 shipped split TX and 37 imported repeaters; not one keys up, while simplex TX is good on the air. The suspicion was a duplex-sign error, because 0133's only RF proof was ONE preset at a **positive** +600 kHz offset while 34 of the 37 real repeaters are negative. **The measurements said otherwise.** Read-only against the live station: all 41 presets audited, **0 sign mismatches** (34 negative, 3 positive, all correct) and **0 tone mismatches**; `/capabilities` advertises `set_split`; and every keying path — `/audio/tx`, `POST /transmit`, `POST /ptt`, the automatic station ID — converges on `_key_on`, which reads the split and tone at key time, so there is no split-unaware key path. So the fault is not in the data and not in the split plumbing. **What was missing was any way to see the two survivors.** `RadioStatus` gains `pa` (bias / gain / `band_matched` / tx leg) recorded at key-up from the reg-0x36 read `_correct_tx_band` already did — the `rssi` argument again: a station reaching nothing is indistinguishable over the API from one that works, every call returns 200. `band_matched` is load-bearing: `Dock_ForceTx` sets the PA up from the radio's **own front-panel VFO**, and `_correct_tx_band` deliberately keeps the firmware's bias byte (per-band calibration in a flash the dock cannot read — inventing one was the ADR 0128 mistake), so transmitting outside the radio's VFO band uses the **other band's** calibration. The VFO is on 445.800, the 37 repeaters span both bands, so **at most one band can ever be correctly biased**; the log escalates INFO → **WARN** and names it. `TalkControl` states where it will transmit **both ways** — the `SIMPLEX` half is the point, because the armed split is process-local and a scan hop, a manual retune or a `systemctl restart` each clear it silently, and the only prior signal was a status row that *disappears*. Tests: all **37** CHIRP rows recomputed independently (only 4 were pinned before, and a sign/column fault is uniform — it lands on all 37 or none), verified by mutation; a `split-minus` RF stage, the exact mirror of `split` with the legs swapped, both now one parameterised proof; and a key-up carrying a tone **and** a split, which nothing covered — deleting `_tone_pairs()` from `_key_on` fails that test and **no other test in the suite**. **Claims no root cause and measures no RF** (no shell on the bench box this cycle); the ranked survivors are the radio's front-panel power setting, the wrong-band bias, and a split cleared before key-up. `uv run pytest` 1661/5, `npm test` 50. | Accepted |
| [0135](0135-ctcss-deviation-and-the-instrument-gap.md) | **Retracts 0134's leading hypothesis, and builds an instrument that could actually see the fault.** 0134 ranked the radio's front-panel TX power first, on the strength of reading `L1` off a *photograph of the radio's screen*. The operator then opened these same repeaters through this same antenna with an **ID-51A and a Baofeng Mini at microwatts** — retiring power, feedline, antenna and the repeaters at once, and retiring the reasoning that produced it. Four more hypotheses died to the **compiled** driver (`bk4829.c` ships; `bk4819.c` is in the tree but not in `CMakeLists.txt`, so earlier analysis quoting it read dead code): `Dock_ForceTx` is `ExitBypass`+`ExitTxMute`+`TxOn_Beep` and calls **no** `ExitSubAu` and **no** `SetFrequency`, so it clobbers neither the tone nor the synthesiser; our CTCSS code word is byte-identical to the firmware's. Nine hypotheses are now dead across two cycles, five to source reading — **reading more source will not find this.** **The real finding is that the bench could not have caught this class of fault at all.** The only over-the-air CTCSS check is `tone_power()`, a *dimensionless fraction*; measured against synthetic signals, a tone at **one tenth** of correct deviation reads **0.998757** against a pass floor of 0.01 (a correct one reads 0.999987). It would pass the bench and open nothing in the field. On a dead carrier the fraction saturates near 1.0 by construction, and the check only ran inside split stages that currently skip. So: `deviation_probe.py` measures the **absolute** band amplitude (`band_rms`, verified linear to three decimals over 10:1) as a **ratio against a transmitter known to open these repeaters** — three legs (handheld / UV-K5 front panel / UV-K5 via dock) because two cannot localise a fault, at **141.3 Hz** because Opus/SILK's high-pass corner moves through 60–100 Hz with content. It refuses to report through two traps: a gated silent capture is not a zero-deviation measurement, and a sweep whose modulator never compressed is INCONCLUSIVE, not a refutation. `uvk5_tx_regs.py` gains `0x51`/`0x07`/`0x40`/`0x43` + `--tone`, because "source says the tone survives" is what was wrong about `0x33`/`0x36` in 0132. The keying guard gains a preset **deny-list** on top of its allow-list — unioned API+file sources (a restarted server answers `200 []`, a successful read of nothing), exemption per **preset** not per frequency (else a repeater imported onto 445.800 exempts its own leg) and never by **name**; unreadable presets refuse. First-ever tests for `scripts/bench/`, all four mutations caught — one of which exposed a test passing for the wrong reason. **Measures no RF** (still no shell on the bench box) but unlike 0134 does not fill the gap with reasoning; `radio_server/` is untouched. The REG_51 gain discrepancy (ours 74, firmware 64) is documented and deliberately **not** fixed, because under-deviation is live and lowering our gain would move the wrong way. `uv run pytest` 1670/5. | Accepted |
| [0136](0136-the-probe-measured-the-window-not-the-transmission.md) | **The instrument 0135 shipped was biased toward confirming 0135's own hypothesis.** Found by watching the operator try to use it. (1) `--reference` printed `KEY NOW` and started a 6 s capture on the same line — a window sized to the measurement, not to a human who has to read a prompt and pick up a handheld; it is now a 45 s window (`--window`) whose loudest `--seconds` is measured, so there is no cue to hit. (2) The real defect: the probe's output is a **ratio** between an operator-keyed leg and a script-keyed leg, valid only because the receive chain's unknowns are common to both — but each leg carried a *different* amount of dead air (the operator's reaction time; `key_and_listen`'s 0.3 s lead-in and 1 s tail). `band_rms` is an RMS over whatever it is handed, so dead air drags a leg down, and **a leg dragged down reads as under-deviation — exactly the finding the probe exists to test for.** It was not even reproducible: `band_rms` applies a Hann window, so a transmission near either end is attenuated by the taper *on top of* the dilution — identical transmissions measured **1726 early / 7687 mid / 1726 late**, a **4.5x spread from reaction time alone**, against a verdict threshold of 0.5x. `loudest_slice()` (cumsum energy, linear over a long window) picks the transmission out of the capture and `measure_transmission()` is now the ONLY path to a recorded number, so both legs get identical treatment. 10 tests pinning both properties as numbers; all four mutations caught. Still **no RF measured** — same blocker, no shell on the bench box — but the procedure is now one a human can execute and the answer no longer depends on their reflexes. `radio_server/` untouched. `uv run pytest` 1680/5. | Accepted |
| [0137](0137-let-the-radio-be-a-radio.md) | **The deviation hypothesis is dead, measured — and the operator was right that the architecture is inverted.** First cycle in this arc to measure anything, because the "no shell on the bench box" blocker every prior cycle recorded was **false**: `ssh kb@192.168.1.62 true` returns 0 and always did (`ssh kb@home` was failing a *host-key* check read as an auth failure; the box's pre-auth ASCII MOTD was read once as success and once as failure, and the **exit code** was never checked). Measured on the air: CTCSS tone ON vs OFF at 141.3 Hz = **149x** over a floor whose two bracketing controls agree (19.11 / 18.79); an earlier run read 527x. Synthetic bench split puts the carrier on the TX leg at 2872.7 RMS with a **0.0 null** on the RX leg. All **41 presets** apply and read back with every RX, TX leg and tone correct (software only, never keys — safe against live repeater inputs). Tone frequency reads high by a *constant* +0.372% across all six tones (spread 0.00054) — a constant percentage is a **clock**, and the witness has an uncalibrated one; 0.54 Hz at 141.3 is inside any decoder's capture range anyway. So four hypotheses die by measurement and none is ruled in. The first attempt at the tone control had its two controls disagree 17x (tone not clear of the chip after 0.5 s) — the script now **voids** such a run instead of reporting the unstable bench as a weak transmitter, the same error class as 0136. Next is **Gate 0** (`aioc_ptt_gate0.py`): does the AIOC's serial PTT line key this radio? If yes the dock register path is optional and `AiocBaofeng` (exists, ADR 0029) already does it — the brief's "Baofeng mode". Refuses the literal "write channels over serial" plan: stock `0x051B`/`0x051D` each arm a **6-second TX lockout**, and no channel-select opcode exists in this firmware. **Not measured: absolute RF power** (witness is inches away, would hear a microwatt) — now the obvious gap. Corrects **ADR 0132**: its `0x100D0 > 0xFFFF` "cannot be expressed" arithmetic is wrong — `eeprom_compat.c:69` maps it to EEPROM `0xB0D0`, so TX calibration is readable with a stock `0x051B`. `uv run pytest` 1680/5. | Accepted; **EEPROM refusal overturned by 0141, lockout account corrected by 0142** |
| [0138](0138-baofeng-mode-proven-on-the-uvk5.md) | **The AIOC keys the UV-K5, and Baofeng mode talks through it — the dock is optional for repeater work.** Executes ADR 0137's gates against hardware. **Gate 0:** DTR keys the radio (witness RMS **1926.8**, and **1970.5** on repeat, against a **0.0** baseline); RTS does nothing, confirming `DEFAULT_PTT_LINE = DTR` (ADR 0029) empirically on this radio rather than by inheritance. The witness is what makes it an answer — the plan it came from proposed watching the TX LED, and an LED says "something happened", not "RF appeared on the right frequency". **Gate 1:** the real `AiocBaofeng` class — the one `server.backend="baofeng"` instantiates — transmits a 1000 Hz tone through the AIOC sound card and the witness recovers it at **910–927 across four runs** against an untransmitted 1600 Hz control band (5–66x; only the noise band moves). `transmit()` blocked 5.03 s on a 5 s clip, so the blocking contract holds. So the radio's own firmware did the frequency, deviation, CTCSS and PA — **every register question this arc chased for four cycles is moot for repeater work.** **The finding that nearly faked a failure:** the first Gate 1 run put NO RF on the air with no exception and a correct 5 s block. Cause — radio-server holds the radio in the dock's `0x0870` full-control loop, which blocks the firmware main loop, and **the main loop is what samples the hardware PTT pin** (`app.c:1441`, ADR 0120 starvation). A stop landing mid-handshake leaves the radio wedged with nothing left to send `0x0871`, deaf to its own PTT input. Measured: stopped ~8 s after a restart → **deaf at +2/+8/+20 s**; stopped after ~40 s → **keys first try, twice**. A wedged radio and an AIOC that cannot key are identical at the witness, so both scripts now refuse a stop before `SETTLED_SECONDS`, and Gate 1 re-tests a bare PTT assert and reports **INCONCLUSIVE** rather than failure when that is silent too — the same error class as 0136's window and 0137's disagreeing controls. **Dock mode and Baofeng mode are therefore mutually exclusive** (also only one AIOC serial interface exists). Costs, all source-confirmed: arbitrary frequency/split/tone/mode become 501, no read-back of anything, no `_confirm_keyed`, no frequency-addressed scanning. **Not claimed: no repeater has been opened yet**, absolute RF power is still unmeasured, nothing on 2 m. `uv run pytest` 1680/5. | Accepted; **RF numbers qualified as single-shot by 0140** |
| [0139](0139-opening-a-real-repeater.md) | **Building an instrument honest enough to say a repeater opened — and the pre-flight that stopped it lying.** Executes the acceptance ADR 0138 named. `scripts/bench/repeater_openup.py`: the witness watches the repeater's **output** while the station transmits on its **input**, and **the verdict comes from the window after our own carrier drops** — a witness inches from a keyed HT can be desensed 5 MHz away, so `busy` while we transmit is exactly what a dead repeater also shows; `busy` after we stop cannot be us. `busy` is the SA818's SQ pin (hardware carrier detect, ~560 Hz polling), `t0` is the transmit request's *return* (late on purpose — a bias toward failing is the one you may believe when it passes), and the over **is** the Part 97 station ID via `StationId.identify()`. Three overs, ≥2 tails, each with its own quiet check. **The pre-flight is the finding:** ADR 0138 recorded that nothing can read the front-panel VFO and left it there, which silently gives every Baofeng-mode result three causes and one output — so this points the witness at the repeater's **input** and keys once first. First run: **0.00 s** on 443.525 → **INCONCLUSIVE**, refused to proceed. Splitting the causes by keying per candidate found the radio transmitting fine on **445.800** (**1.21 s** carrier, audio RMS **3816.5**) — it was simply never moved to K0PRA. Without the pre-flight this would have published a confident false **NO RESPONSE** about somebody's repeater: the ADR 0136/0137/0138 error class a fourth time, caught before the output for the first time. Two things fall out: **`POST /radio/select` un-wedges the radio** (it reaches `Uvk5Radio.close()` → `ExitHwMode` `0x0871`, so the ADR 0138 wedge is specific to a *service stop*, not the mode switch operators will use), and `transmit()` blocked **1.51 s** = 1.0 s TX lead + 80 wpm CW `AE9S`, i.e. the audio path was perfect while the run produced nothing. Also: the bench box's parked `[baofeng]` block is now active on the **same** AIOC as `[uvk5]` (one cable, one radio, two mutually exclusive drivers), and the UI stops hiding where it transmits — `TuneControls` names the contract instead of saying "audio-only backend", and `StatusPanel` gains a **Transmits on — set on the radio, not readable from here** warning row, because `set_frequency` being absent also hides the face's LCD. Baofeng mode additionally retires all three of ADR 0134's surviving field hypotheses by construction (wrong-band PA bias, cleared split, unsettable TX power) — the radio's own firmware sets up TX. **Still open:** no repeater opened yet, absolute RF power unmeasured, nothing on 2 m (kv4p is UHF-only). | Accepted; **acceptance cancelled and RF numbers qualified by 0140** |
| [0140](0140-the-first-key-is-always-lost.md) | **The first key after idle is always lost, and a dock tune never survives the handoff.** ADR 0139's hand-tuned K0PRA acceptance is **cancelled**: a repeater opens on carrier plus CTCSS, both generated by the K5's own firmware from its front panel, so it would have measured the operator's antenna and tone rather than radio-server. What replaced it found something worse. **The station keys 0/4 from cold and 4/4 warm** — and an 8 s continuous hold from cold radiates exactly as much as a 1.5 s one: nothing, so length is not the variable and it is not a sleep window. Ruled out by measurement, not argument: not a frequency change (`carrier_hunt` holds PTT and sweeps all 48 candidates in the kv4p's 400-480 MHz — on a silent over the carrier is **nowhere**); not USB autosuspend (`power/control: on`, never suspended); not the witness (a direct 20 Hz poll saw busy 21/27) nor the poller (719 samples in-window, 0 errors); not the server (200 every time, `transmit()` blocks 1.51 s = TX lead + CW ident, every time); not a 0.5 s wake pulse (0/3 treated vs 0/3 controls). **Retroactive cost: every RF number in ADRs 0138 and 0139 is single- or two-shot**, taken while that coin-flip was live — not wrong, but over-stated. `scripts/bench/trials.py` makes every RF claim N-of-M with N stated, runs the full count after a failure (clustering is what found this), and keeps 1=failure distinct from 2=inconclusive. **Mechanism, separately:** `Dock_EnterFullControl` ends in `RADIO_SetupRegisters(true)` (`uart.c:774`) → `BK4819_SetFrequency(gRxVfo->pRX->Frequency)` (`radio.c:807-809`), so `0x0871` retunes from the radio's OWN VFO and no dock write can survive it — which is why 37 presets applied and read back correctly and never keyed a machine. **Decision:** fork opcode **`0x0873`** hands the radio a whole channel and runs the firmware's own `RADIO_ApplyOffset` → `RADIO_ConfigureSquelchAndOutputPower` → `RADIO_SetupRegisters`, so `0x0871` stops being something to survive and becomes the thing that applies it. `0x0873` not `0x0872` (the latter is stock `CMD_0872_t`); dispatched outside the full-control loop so the main loop that samples the PTT pin is never starved; **both** VFOs written so dual watch cannot land on a stale one; malformed frames refused not clamped, CTCSS matched against the radio's real table (123.4 Hz is in range and is not a tone it has). Host tests 48/0, image builds clean (FLASH 57%). **Also:** `POST /radio/select` baofeng→uvk5 **segfaults the server** (139, 49 s in; `close()` never runs so `0x0871` is never sent) — 0139 called that path safe on the other direction alone. `uv run pytest` 1719/5. **Not claimed:** the cold-key fault is neither fixed nor explained; the firmware has never been flashed; no repeater opened; absolute RF power still unmeasured; nothing on 2 m. | Accepted |
| [0141](0141-one-byte-over-the-wire.md) | **Dual watch was the fault, and one byte over the wire fixed it.** ADR 0140 had the shape but not the cause: key-up success was a **function of the gap between keys** (9/10 at 6 s, 5/12 at 19 s, strict alternation at ~21 s) while `TIOCMGET` showed DTR high 16/16 samples on 10/10 overs — aliasing against a periodic switch, which no cable, connector or battery can produce. **The firmware already on the radio dispatches EEPROM read/write and a soft reset** (`0x051B`/`0x051D`/`0x05DD`), so ADR 0137's refusal of that path was wrong on both counts: the 6-second TX lockout only matters if you key immediately, and "no channel-select opcode" is true and beside the point — *you do not select a channel, you write the VFO*. `settings.c:173` loads `DUAL_WATCH` as `(Data[4] < 3) ? Data[4] : DUAL_WATCH_CHAN_A`, so an **unprogrammed `0xFF` loads as ON** — never configured, shipped that way. With it on, `RADIO_SelectCurrentVfo` points `gCurrentVfo` at `gRxVfo`, which dual watch alternates on a timer, so **which VFO the station transmits from was decided by a clock**; half the overs left on the other VFO, inaudible to an SA818-**UHF** witness — which retro-explains `carrier_hunt` finding the carrier "nowhere" across 48 UHF candidates. **Two wrong addresses, caught by reading before writing:** the classic `0x0E70` settings block is, on this V3 tree, inside the identity-mapped *channel* region, so the probe returned all `0xFF` — indistinguishable from a settings block full of defaults, and writing on it would have put a byte into memory channels. The real block is `0xA000`, `DUAL_WATCH` at `0xA00C`. **Result: 10/10 at 6 s, 19 s and 21 s = 30/30**, carrier 1.17–1.27 s, gap-dependence gone — verified with the same measurement that exposed the fault, because a fix visible at only one spacing is indistinguishable from luck. No flash; nobody touched the radio. **Not claimed:** no repeater opened, the other VFO's frequency never read, absolute RF power unmeasured. | Accepted |
| [0142](0142-the-server-picks-the-repeater.md) | **The server picks the repeater — 41 presets stopped being decoration.** Everything was already built: `POST /presets/apply`, `apply_preset`, 38 repeater splits in `radio.toml`. None of it did anything, because `AiocBaofeng.capabilities()` returned `SHARED_CAPS` and every tuning field was reported skipped. The UV-K5's UART rides the **same AIOC cable** already held for PTT — measured 18/18 that dock frames survive capture *and* playback streaming — so a tuner runs in-process with nothing stopped. Two strategies behind one seam, `baofeng.uvk5_tuner` (default `off`, so a UV-5R is untouched): `setvfo` (`0x0873`, instant, needs F6) and `eeprom` (write the channel, soft-reset onto it, **works on stock firmware**). `tuning_batch` makes a whole preset cost exactly one tune instead of four reboots. **Three bugs found by building the reply rather than by flashing:** the wire spoke **Hz** into a VFO that stores **10 Hz units** (and `FREQUENCY_GetBand()` *clamps*, so 4.485 GHz would have resolved to band 7 and mis-calibrated the PA in total silence); the power byte's `0/1/2` was written raw into `{USER, LOW1..LOW5, MID, HIGH}`, so **"high" meant `LOW2`** — a correct tune, a keyed radio and a repeater that stays shut; and `0x0873` had **five silent rejections**, including a missing tone falling through to *no tone*. `0x0874` now reports status plus the frequencies and power read back out of the radio's own VFO after `RADIO_ApplyOffset`. **Corrects ADR 0137:** the 6-second TX lockout is armed by `CMD_0514` (Hello) and `CMD_051B` (**read**) too, not just writes — the bench said so exactly, every carrier row failing attempt #1 and passing after. **Acceptance is differential** (ADR 0139's repeater-tail test retired as the operator argued): apply a preset, then check where the carrier is **and where it is not** — a radio stuck on one frequency passes the carrier rows by accident and fails the silence rows. Split row proves the mechanism: carrier on the **transmit** leg (446.400), silence on the receive leg. RX measured by `tone_power`, **0.96 tuned vs 0.000 detuned** — `busy` is hardwired `False` on this backend and the first probe wrongly polled it. **Not claimed:** no real repeater opened by the server yet; **2 m unverified** (36 of 38 splits are 145/144 MHz, the witness is UHF-only); `setvfo` unproven on hardware until the radio is flashed; `/radio/select` still segfaults. | Accepted; **`setvfo` since proven on hardware by 0145; tuner docstring corrected by 0144** |
| [0143](0143-a-tune-must-know-it-has-a-session.md) | **A tune must know it has a session — ADR 0142 passed 80/80 and the operator still could not set a frequency.** Every `POST /presets/apply` returned **500**, `TuneError: no answer to an EEPROM read at 0x8808`. Measured out, not argued: the AIOC symlink was unmoved, the fd still held, no re-enumeration, both addresses correct — and a direct probe got `F4HWN v5.7.0` back on the first ask. **The firmware answers EEPROM frames only inside a session and refuses silently** (`if (pCmd->Timestamp != Timestamp) return;`), so "no answer" *is* the radio saying we have none — indistinguishable from a dead cable unless the host asks. Our half: `_hello()` used `send()` instead of `request()` and ignored the `0x0515` reply that was already decoded, then **latched `_hello_sent` for the life of the process** — so one HELLO fired into a radio mid-flash wedged every tune for three hours, including long after the radio came back. **Why 80/80 missed it:** the gate ran as one continuous session against a warm, already-HELLO'd radio and re-established the session after its own resets, so it never entered the case where the session dies for a reason the server did not cause — a firmware flash, a battery swap, the power switch. *A gate that cannot fail the way the product fails is not a gate.* **Fixes:** the handshake is verified (and a locked radio with a custom AES key refused, since it answers reads with zeros); silence costs **one** re-handshake and retry, not a service restart; `apply()` pre-flights before a sequence that reboots the radio; the post-reset HELLO is retried because boot time is not a constant. **And the operator sees a sentence:** new `RadioUnavailable` (hardware could not, vs `UnsupportedCapability`'s mode cannot) rendered by one app-wide handler as **503 with the message**, so a switched-off radio stops reading as `Internal Server Error`. Bench row 2 — tune, `Reset()` out-of-band, tune again — is the incident, and the old code fails it every time. | Accepted |
| [0144](0144-instant-and-persistent.md) | **Instant and persistent — a channel change goes from ~14 s to 1.1 s and still survives the power switch.** Almost all of the 14 s was a soft reset whose only job was making the firmware *load* the record; `Dock_SetVfo` ends in `RADIO_SelectVfos(); RADIO_SetupRegisters(true)`, so `0x0873` retunes the synthesiser in place — but it writes `gEeprom.VfoInfo[]`, which is **RAM**. `HybridTuner` does both: RF now, storage after, no reboot. **The enabling fact, read out of the firmware:** the six-second TX lockout is armed at exactly four sites in `uart.c` — 355 (HELLO), 393 (EEPROM **read**), 447 (write), 586 — and **the dock opcodes do not arm it**, while `SerialConfigInProgress()` both refuses a key-up and *terminates an over in progress*. That lockout is now **published rather than slept through**: `tx_ready_at` on the tuner, `_key_on()` waits out the remainder, so a listener is not charged for a talker's wait. Re-selecting a stored channel writes nothing — **7.6 ms**. The UI stops inviting a dead press: `RadioStatus.tx_ready_in` (relative, never a stale absolute) and a `TalkControl` button that disables itself with a locally-ticking countdown, since nothing pushes a status event while the radio sits muted; Mumble/D-STAR untouched. **The gate caught itself:** the persistence row was written to fail under `setvfo` and *passed 2/2*, because storage already held that channel from an earlier hybrid run — "it persisted" and "it was already there" are indistinguishable with one channel. Rewritten as decoy-then-test across two reboots, it now fails setvfo **0/6** (carrier 0.00 s) and passes hybrid **6/6**. Also corrects ADR 0142's docstring claim that the server re-applies on connect — it never did. Differential 16/16 in hybrid; eeprom cold+recovery 4/4, no regression. **Still not claimed: 2 m TX** (witness is UHF-only). | Accepted; **lockout arming rule corrected by 0145** |
| [0145](0145-instant-by-default.md) | **Instant tuning as a live choice, and the key-up that makes it safe.** Storing is the only thing that costs the radio's six-second TX lockout, so whether to pay it is the operator's call and changes with the afternoon — `HybridTuner.persist` (default **off**), `POST /tuning/persist`, `RadioStatus.tune_persist`, a switch in the Channels card. `baofeng.uvk5_tuner` **keeps its `off` default**: the operator's own question — would a UV-5R still work as before — is the reason, and defaulting the global to instant would fire dock frames at a jack with no UART. **The hazard instant creates, and the fix:** a RAM-only tune is gone when the radio is switched off, but `status()` still reports the channel the server chose and the UI still highlights it — so the next over goes out on a stale frequency, silently. Tuners now declare `volatile`, and `_key_on()` re-asserts with one `0x0873` **before** the line goes high, refusing cleanly (503, ADR 0143) with nothing opened and nothing keyed. It is available precisely here: `0x0873` needs **no HELLO**, and a HELLO arms six seconds of mute at `uart.c:355` — so *re-tuning the radio costs strictly less than asking where it is*. `EepromTuner.reassert` is an empty body on purpose (its `apply` reboots). **Corrects ADR 0144:** the lockout was armed only when flash changed, but the HELLO and every read arm it too and `write_channel` always does both — a store that wrote nothing reported a muted radio as ready. **The fail-first, re-measured not cited:** the persistence row on merged `master` with `setvfo` **0/6, carrier 0.00 s**; on this branch **6/6** — same radio, same config, one code change. That row can no longer tell storage from re-assert, so a new `run_storage_contrast` does: storing arms the lockout 5/5, instant arms none 5/5, after a reboot the radio hears the **stored decoy** (0.96) and is deaf on the instant channel (0.00) **yet the key-up still lands there** (2.18-2.23 s). **Two rows were wrong before the code was** — one read a draining lockout as a fresh one and failed a correct implementation; one was contaminated by the probe before it (0.22 against a 0.05 floor) and was fixed with a settle, not a wider threshold. Drops the `rx_tone` notice from the UI (29 of 41 channels, every tap, never actionable, and it said "this radio" meaning "this software") — the API still reports it. **Still not claimed: 2 m TX.** | Accepted |
| [0146](0146-transmit-power.md) | **Transmit power becomes something the operator can set.** It was plumbed to the radio from the first tuner and hardcoded above the `VfoImage` seam — measured first, not inferred: the radio read back **`power=7` on all 186 tunes**. Now `low`/`mid`/`high` via `baofeng.uvk5_power` (default **high**, so nothing moves for anyone who does not touch it), an optional per-`[[presets]]` `power`, `POST /power`, `RadioStatus.power` and a Tune-card selector. **Absent on a preset is deliberately NOT "off"** — the split and the tone belong to the channel, so omitting them means none (ADR 0133), but a power level belongs to the *station*, so omitting it means "however I am set" and forcing a default would undo the operator's choice on every tap. A preset that names one moves the station level, so there is exactly one visible level. `status.power` reports what the radio **confirmed** — `None` before the first tune and for a front-panel level (`LOW2..LOW5`, `USER`) this server did not choose, because naming one would be inventing a number. **Baofeng mode is the *calibrated* path**: the firmware runs `RADIO_ConfigureSquelchAndOutputPower` per band from its own flash, the exact thing the dock's raw `0x36` bias write cannot do (ADR 0128/0134). `SetVfoTuner` now **checks** the `0x0874` power read-back instead of logging it — `out->power` comes from `gEeprom.VfoInfo[0].OUTPUT_POWER` after the firmware applied it, and the scale is a trap that already bit once (raw 0/1/2 lands "high" on `LOW2`). **Measured: low/mid/high → OUTPUT_POWER 1/6/7, 3 passes, exit 0**; the same gate against a build that swallows the request scored `low → high`, **exit 1**. **Claim 2 — that less power radiates less — was NOT measured**: the witness reported RSSI 0 including its idle floor, which ADR 0132 had already established this kv4p firmware does, and which had already produced two wrong "FLAT" verdicts. The gate now says NO MEASUREMENT in those words rather than dressing a dead meter as a null result. **Watts are claimed nowhere.** | Accepted |
| [0147](0147-the-docs-drifted.md) | **The docs drifted, and nothing was watching — lock what a test can check.** A full audit found **four documents still telling the operator to set the frequency by hand on a Baofeng** (the exact capability ADRs 0142-0146 deliver, over the exact cable `install.md` said could not carry it), **18 ADRs (0097-0114) with no index row** in an ADR-first project, ADR 0139's row **missing its Status column**, `deployment.md` naming **three** WebSockets where the app registers **seven** (an nginx config from that list silently kills browser Mumble and D-STAR audio), and `using-it.md` promising *"you're never left with a dead station"* about a backend switch that **SIGSEGVs**. Cause is structural: docs are hand-updated at cycle end and **nothing fails when they aren't** — the two contracts that *were* locked (ADR 0058's README↔`install.sh`, `radio.toml.example`↔`spec.py`) had not drifted at all. So `tests/test_docs_contract.py` locks every ADR file to an index row, every internal link to a real file, and every REST path / WebSocket / `Capability` to `docs/api.md`, off `app.openapi()` over the existing `create_app(MockRadio())` seam. **Fail-first: 5 of 7 failed** (18 / 1 / 1 / 10 / 2) — and the capability check **passed when I predicted it would fail**, recorded as a miss rather than dressed up, because a substring search cannot see a prose list that named five of seven. **The locks catch absence, never wrongness**: "there is no CAT" would have passed all seven. Also ships `docs/dstar-setup.md`, the first narrative doc for a feature area with two UI cards and eight endpoints — written warning-first, because the crossband **stranded the transmitter keyed at least five times**, its joint dummy-load re-proof has **never passed**, and since ADR 0089 removed `operator_tx` there is **no browser-only mode**: the link IS the arming switch. | Accepted |
| [0148](0148-the-firmware-is-a-product-too.md) | **The firmware is a product too — the setup guide pointed V3 owners at firmware that cannot run on their radio, and the firmware we wrote was invisible.** `docs/uvk5-setup.md` pinned nicsure's Dock `0.32.21q` (**DP32G030**) for a bench radio that is a **PY32F071** — a fact ADR 0118 established eight cycles earlier and never told the guide. ADR 0147's locks could not catch it, exactly as predicted: every link resolved and every endpoint was named; the page was simply **false**. Measured on the fork: `main` was **still pristine upstream at `3bd3ebb`** (all ten dock commits on stacked branches — *cloning it got you none of the work*), the README was **byte-identical to upstream's**, there was **no F6 release** though F6 is what is flashed, and the operational knowledge lived only in *this* repo's ADRs. The guide also assumed flashing is mandatory: since ADR 0141 the `eeprom` tuner drives a UV-K5 on **stock** firmware, so **three paths existed and one was documented**. Restructured around all three with the no-flash path leading; the V3-vs-classic tell states only what is confirmed (USB DFU vs the PTT+flashlight bootloader, bootloader 7.00.07) and marks the rest verify-on-hardware. Fork `main` fast-forwarded to F6 (clean, zero divergence, no rewrite), `PROTOCOL.md` written, F6 released. **The doctor gains an F-level probe: an EMPTY `0x0873`**, safe because `dock.c` checks length in the *first* branch — `ERR_SHORT` before a field is decoded, before the VFO binding is called, frequencies blanked. **The refusal is the affordance**; a firmware that clamped instead could not be probed without tuning the radio. **Fail-first: a probe that always claims F6 failed 2 of 3 gates.** Every golden vector cross-checked against `dock.c` *and* `frames.py`; the release binary is the flashed one and a clean rebuild differs in **5 bytes of 105,336** — the build timestamp. **Left open: nothing checks that the two repos' protocol docs stay in step** — ADR 0147's problem one level up. `uv run pytest` 1884/5. | Accepted |
| [0149](0149-a-new-opcode-is-cheaper-than-a-changed-one.md) | **A new opcode is cheaper than a changed one — the radio gains AM, and a tune stops silently forcing FM.** The dock could tune the radio and not change its demodulator: `Dock_ApplyVfo` wrote `vfo->Modulation = MODULATION_FM` on every `0x0873` (and `vfo.py:339` does the same host-side), so AM was not gated or degraded but **absent**. Adds firmware `0x0877`/`0x0878` set-modulation — **a new opcode, not a 14th byte on `0x0873`**, because an added field changes bytes on a frame radio-server already sends and breaks BOTH directions **silently**: an old host's 13-byte frame is refused `ERR_SHORT` and reads as a tuning failure, a new host's 14-byte frame decodes fine on old firmware which then tunes on a modulation nobody set. ADR 0148 named that drift and nothing checks it; this declines to widen it. **The opcode was not free, and neither was the last one:** ADR 0111:52 records the classic Dock's extras as *"0x0872 modulation, **0x0873/4 backlight, 0x0875/6 AM emulation**"* — so the obvious `0x0875/6` is claimed, **and so was `0x0873/4`, which ADR 0140 took after checking `0x0872` alone**. That one is shipped, on hardware, in 186 measured tunes, and cannot be walked back; the census still lives in three places that disagree. Hence `0x0877/8`, checked first. **The modulation is sticky and `0x0873` applies it** — not a convenience: **ADR 0131 established this link DROPS frames**, so "tune, then set modulation" as two frames can leave the radio on the right channel in the wrong demodulator, reporting success. Seeded FM **from a constant, never read back off the radio** (that is ADR 0132's fault), and **a refusal never moves it** — committing during decode would let a refused `ERR_BUSY` become what every later tune applies. `0x0873`'s wire bytes are untouched; one literal became a table lookup whose default is that literal. **A successful set-AM stops the transmitter**, traced not assumed: `GENERIC_Key_PTT` → `gFlagPrepareTX` → `RADIO_PrepareTX` → `VFO_STATE_TX_DISABLE` without `ENABLE_TX_WHEN_AM` — the path the **PTT pin** drives, where the `baofeng` backend keys from via AIOC DTR. Dock REG_30 keying bypasses it, so one state means "dead transmitter" on one backend and "normal" on another; `0x0878` reports it as a flag. **`ENABLE_TX_WHEN_AM` stays off — AM is receive-only and the flag reports the condition, it does not remove it.** Refusals blank to **`0xFF`, not `0`** — copying `0x0874`'s zero-blanking would answer a refusal with a plausible claim of FM. Wire values are the wire's own, because `MODULATION_UKNOWN` **moves with a build flag**. **Fail-first twice:** an always-succeeds stub scored **15 failures**, a build keeping only the `0x0873` FM hardcode scored **exactly 2** — the stickiness pair and nothing else. Green: **98 checks, 0 failures** (66 before), with the `0x0874` golden unmoved as the gate on `0x0873`. Goldens cross-checked against an independent framer that reproduces the two published vectors first. FLASH **105,576 B / 118 KB (87.37%)**, +240 B. **Not flashed; no hardware claim** — AM audio and the PTT refusal are new `⚠ CONFIRM AT BENCH` items. | Accepted |
| [0150](0150-the-host-learns-to-listen-to-am.md) | **The host learns to listen to AM — F7 shipped the command and nothing here could speak it.** ADR 0149 gave the radio `0x0877`/`0x0878`, so the capability existed on the hardware and was unreachable from the server: airband impossible, presets unable to name a demodulator. Adds the codec, a capability, a preset field and `POST /modulation`. **The wire contract was read out of `dock.h`/`dock.c` at the merged F7 commit, not out of ADR 0149's prose** — ADR 0148's cross-repo drift is still unguarded, so reading the source *is* the check; goldens are transcribed from the fork's own harness and then **re-derived from an independent reference framer** in the test file, matching byte-for-byte both ways. **`SET_MODULATION` is its own capability, not a widening of `SET_MODE`** — that is wide/narrow *bandwidth* (`narrow` inside `0x0873`), this is *demodulation*; both spell one value `"FM"` and folding them would make one route mean two things. Forced cost, named: `len(FULL_CAPS) == len(Capability)` is asserted, so it joins `CAT_CAPS` and `MockRadio` implements it — the `SET_POWER` path. **`TUNING_CAPS` stops being one shared frozenset:** `EepromTuner` writes a hardcoded-FM record into *stock* firmware with no `0x0877` case, so it keeps the five and `setvfo`/`hybrid` get `SETVFO_CAPS`; it also **raises** rather than returning, because not advertising gives the 501 while raising is what stops a direct call reporting success for a radio still on FM. **Absent `modulation` means FM — the opposite of `power` (ADR 0146):** a level belongs to the *station*, a demodulator to *what you are listening to*, so it is written on every apply — which is also the **assert-at-connect `dock.h` requires**, since the firmware's sticky value outlives a host restart. **Decode by explicit value, never position** (`raw` moves with `ENABLE_BYP_RAW_DEMODULATORS`; `0xFF` is a sentinel, and `.name` returns `None` rather than folding onto FM). **`tx_ok` is reported on every backend AND refuses a key-up:** AIOC-DTR keying runs into `VFO_STATE_TX_DISABLE`, dock REG_30 keying does not, so one radio state means "dead transmitter" on one backend and "normal" on another — and an unenforced flag would leave AM key-ups as silence with `status()` reporting `transmitting`, swallowing the **station ID** guardrail 5 makes mandatory. Only a *measured* `False` refuses; `None` never blocks. The key-path re-assert now carries the modulation **first**, so the flag is a measurement, not a memory. **Fail-first three times:** wrong command byte → **2** failures; `tx_ok` never reading the flag → **5** across three files; `EepromTuner` silently returning → **exactly 1**, with the capability-split test **staying green** (recorded so nobody cites the split as proof of the refusal). `uv run pytest` **1939 passed, 5 skipped** (1884/5 before). **No UI, no firmware change, no bench claim** — the radio is not flashed with F7 and AM audio remains `⚠ CONFIRM AT BENCH`. | Accepted |
| [0151](0151-a-failed-key-up-must-give-the-radio-back.md) | **A failed key-up must give the radio back — one refused over took the whole station off the air.** ADR 0150 taught the AIOC backend to refuse a key-up in AM, which was the first time `ptt(True)` could routinely raise — and it exposed a latent bug directly upstream. `TxSession.feed` claimed the shared arbiter and *then* keyed, so a raising `ptt(True)` left it latched `TRANSMITTING` with `_keyed` still `False`; `close()` guards its release on `_keyed`, so **nothing ever gave the radio back**. The RX pump, the scan engine and the D-STAR bridge all consult that latch and stood down for the life of the process — no RX audio, no DTMF decode, no controller. Reproduced in memory before anything was written. **Key-up is now all-or-nothing** (the ADR 0093 `_key_on` shape, one layer up): before the line is up, release and re-raise; after it is up, `close()` unwinds — including the paired `on_key(False)` the ledger needs to close its record, and including a failed **key-up ID**, because `key_up_id` only returns audio when an ID is *due* and carrying on would put an un-ID'd over on the air. **The audit found more than the shape it was sent for:** a **ninth** station-keying site in `services/dispatch.py` (every DTMF voice service — the most-travelled keying path in normal operation, invisible to a controller-shaped search), and a Part 97 defect with no guarding involved — `StationId.transmit` advanced `_last_id` *before* the radio call, so a failed announcement **marked the ID as sent** and suppressed the next for ten minutes. **The state invariant lives in `StationId`, the guard in the controller:** no caller can repair state it cannot see, and the event channel is the controller's. `strict` re-raises on `trigger`/`open_session` because those callers *can* report a fault (the ADR 0143 app-wide 503) while the DTMF loop cannot — the `tx_failed` event is emitted before the re-raise either way, so the record is never traded away. It reaches the **JSONL ledger**, the durable Part 97 artifact: absence of a `station_id` record is not evidence of anything. **Fail-first three times:** the arbiter leak → **8 failures across 4 files** (the RX-resume proof *hung* rather than failed — the blast radius stated plainly); the `_last_id` ordering → red by construction; a guard that swallows silently → **7 failures**, with the survival assertions and **three return-value honesty tests staying green** (recorded so nobody cites `id_sent is False` as proof the failure is visible). `uv run pytest` **1961 passed, 5 skipped** (1939/5 before). **Dependency, not a note: the two bridge relay loops must be hardened before AM is selectable by an operator, i.e. before the UI cycle** — a raising key-up still kills them, which is pre-existing, orthogonal, and unfirable while F7 is unflashed. **No UI, no firmware change, no bench claim.** | Accepted |
| [0152](0152-a-login-is-not-its-announcement.md) | **A login is not its announcement — ADR 0151 applied `strict` to one API path too many.** `open_session` re-raised on a refused login announcement, but `gate.open()` commits *before* the announcement is attempted — so the gate was left open and the station ID armed with **neither `auth_accepted` nor `session_open` emitted**, and the caller got a 503. An authenticated session that existed in memory and **not in the ledger**, reported as a failure that the very next `GET /status` contradicted. ADR 0151 also shipped a test asserting the raise, so the suite *pinned* the defect rather than catching it — recorded, not quietly rewritten. **`step()` was checked, not assumed:** its ACCEPTED branch never passed `strict`, so the DTMF login always kept its session and its records; only the API path lost them. **The line is not API-vs-over-the-air — it is whether the caller's request NAMES a transmission.** `trigger` *is* "transmit this", so a 200 with nothing on the air would be a lie and it keeps re-raising, untouched. `open_session` is "open my session"; the announcement is a side effect of the open. Restores the invariant the docstring already claimed ("identical to a DTMF-accepted auth"), stops a 503 contradicting observable state, and keeps the audit trail complete. **Rollback was considered and rejected:** a retrying caller would be fine, but rolling back only in `open_session` would break the same equivalence in the opposite direction — and ADR 0151's all-or-nothing protects a *hardware* resource where a half-state strands a transmitter, which a session is not. **`announced` is tri-state** (`tx_ok`'s precedent, ADR 0150): `None` is *not attempted*, `False` is *refused* — documented as PAIRS with `opened`, since `null` means "already open" beside `opened: false` and "nothing configured" beside `opened: true`. The failure-only shape was rejected as the silent-failure class ADRs 0149/0150/0151 exist to close. **Fail-first** asserts the *invariant* (gate closed OR both events recorded) rather than the chosen remedy, so it survives a change of mind; red on master with `Session(state=AUTHENTICATED)` and neither event seen. `test_trigger_still_raises` stays green and untouched — the proof this did not overcorrect. `uv run pytest` **1965 passed, 5 skipped** (1961/5 before). **No UI, no firmware, no bridges, no bench claim.** | Accepted |
| [0153](0153-one-frame-must-not-take-the-relay-down.md) | **One frame must not take the relay down — closing the gate ADR 0151 recorded before AM can be operator-selectable.** ADR 0151 made a refused key-up unwind cleanly *inside* `TxSession`, but it still propagates OUT of `session.feed()` — and `link/bridge.py` caught only `AudioFormatMismatch` while `dstar/bridge.py` was guarded for that plus `ArbiterStateError`. One refused frame killed the Mumble→RF task, or the drain loop and with it the whole crossband. **The argument was already written in the repo**, in the `ArbiterStateError` catch: "an unhandled raise here would kill the drain loop — the whole crossband — over one contended frame". Same sentence, different exception; that catch is the shape copied rather than a new one invented. **Two counters, never one:** an AM refusal is a *standing condition* recurring every frame until the demodulator changes, an `OSError` from a yanked cable is a *fault* that is rare and individually meaningful — one shared counter lets the former bury the latter. **Narrow-only was rejected and not for diff size:** a yanked cable kills the loop identically, so catching one exception type is a partial fix, not a smaller one. **Except ORDER is load-bearing and pinned by tests** — the broad backstop goes last, and a malformed frame and a held arbiter must each still take their named path with both new counters at zero, or the backstop silently eats ADR 0102's drop-and-retry. Refusal logs are throttled (standing condition); the backstop logs a full traceback every time (rare, and the traceback is the only identification). **D-STAR ends the over where the arbiter path retries** — contention is transient, a refusal is a standing station condition, and holding the slot and the `rx` latch for an over that cannot happen blocks the browser talker; the new causes stay outside `("end", "teardown")` so a still-flowing stream re-latches. **Fail-first 7 red across both bridges**, asserting loop SURVIVAL (a frame after recovery must reach RF), and `overs_keyed` came back **1 for zero overs** — independently confirming a counter that was already lying, now moved to fire on a successful feed. One self-inflicted failure recorded: the first fix called `log.warning` in a file with **no module logger**, and the `NameError` inside the `except` killed the loop exactly as the original bug did. `uv run pytest` **1973 passed, 5 skipped** (1965/5 before). Carried forward as the named next finding: **three websocket talker-slot leaks** (`app.py:1873`/`:1995`/`:2095` — `try_acquire()` then `await websocket.accept()` outside the try/finally). **No UI, no firmware, no bench claim.** | Accepted |
| [0154](0154-two-controls-must-not-both-say-fm.md) | **Two controls must not both say FM — the AM/FM split reaches a screen, and the card would have said FM twice meaning two things.** ADR 0153 closed the gate; this is the UI cycle it unblocked. `ModeControl` said "Mode" over a select that POSTs `/mode` — wide/narrow **bandwidth** — and a demodulation control offers FM and **AM**. **The argument was already in the repo, twice**, in `Capability.SET_MODE`'s docstring ("*they will be confused otherwise, because both spell one of their values `"FM"`*") and in `docs/api.md`; third cycle running where the reasoning predates the fault. **Both labels change so neither survives with its meaning altered:** `Bandwidth` (`Wide (FM)`/`Narrow (NFM)`) and `Demodulation` (`FM`/`AM`). Keeping "Mode" for the demodulator is the ham convention and was rejected as the *only* option where muscle memory lands on the wrong control. The parentheticals are deliberate — the sole join back to the raw `FM`/`NFM` still spelled in `radio.toml`, in preset records and on the face LCD. **The bandwidth select already offered AM**, this cycle's trap already shipped: it listed AM/USB/LSB/CW, all four of which every real backend raises on, trimmed to the two `POST /mode` accepts. **Only a *measured* `tx_ok: false` locks Talk out** — `null` never blocks, the backend's own rule — and **no backend-name check went into the browser** even though `docs/api.md` says the refusal only bites the `baofeng` keying path: `uvk5/radio.py` passes neither field, so the dock reports `null` and the tri-state rule *is* the backend gate, structurally. Recorded with its expiry condition. The lockout folds inside the existing `source === "rf" && !talking` guards — **the parentheses are load-bearing and pinned by tests**, since `&&` binding tighter than `\|\|` would dead-button Talk-on-Mumble and strand a key mid-over. The standing reason is named before the countdown (one clears itself, AM does not), and the sub-line is *actionable* and names the cost: the station ID Part 97 requires and every voice service, not just the over. **The face LCD had to change too — a correct select beside a misleading LCD is not a fixed collision:** it printed a bare `state.mode`, so a radio demodulating AM read "FM" in the largest text on screen; tokens now say which setting they are (`DEMOD AM` / `BW NFM`), warn-red on a refusal, absent rather than defaulted when unknown. **The new control has no Set button and no draft state**, because `apply_preset` rewrites the modulation on *every* apply — a draft would show the operator's last pick while the radio was moved out from under it. **Fail-first three times:** the lockout and Spacebar → **6 red**; treating `null` as `false` → **5 red**, and the blast radius is the finding — it also broke **four pre-existing `tx_ready_in` tests**, because an unmeasured field would lock out essentially every radio. **Shipped beyond the brief and recorded, not blamed:** the Spacebar keydown never consulted the lockout at all, so a greyed Talk button has always still keyed from the keyboard — cosmetic at six seconds, permanent once AM can cause one. The keyup was verified and deliberately left unconditional: refusing to stop is the dangerous direction. `npm test` **101 passed** (71 before); `uv run pytest` **1973 passed, 5 skipped, unchanged**. Findings carried: `POST /mode` returns **500** not 422 on a bad value (verified empirically, precedent one route above), `MockRadio.set_mode` accepting what every real backend rejects (which is *why* nothing caught it), `GET /presets` omitting `modulation` so the preset highlight will now lie, and a `TuneControls` test header claiming coverage that never existed. **No backend change, no firmware, no bench claim.** | Accepted |
| [0155](0155-a-restart-must-not-inherit-the-demodulator.md) | **A restart must not inherit the demodulator — the AM arc's last open path was process start.** The UV-K5 keeps its modulation in the **dock session**, RAM a host restart does not touch and only the radio's own power switch reseeds. `AiocBaofeng.__init__` sent **no frame to the radio at all**, and `_reassert_channel` returns early on `self._tuned is None` — so a server restarted against a radio left on AM reported `modulation=null, tx_ok=null`. Every layer above then behaved **correctly** and the fault still landed: `_refuse_if_tx_disabled` refuses only a *measured* `False`, the ADR 0154 UI never locks on `null`, and the first key-up drove DTR into a transmitter `VFO_STATE_TX_DISABLE` had already disabled — silence over the air, `status()` reporting `transmitting`, and the transmission it ate was the **station ID**. The rule was already written in `presets.py` ("a reconnecting server must state what it wants rather than assume FM") — true only of the path through `apply_preset`, which **boot does not run through**. **Write beats read, and the losing option's cost is recorded:** there is no get-modulation opcode (a firmware fork, a flash and a bench proof to *learn* what one frame can *make* true), inferring from BK4819 over `0x0851` reads the firmware's leftover rather than its VFO truth (the argument already written for `_pa` and for split), and even a *successful* read is ADR 0132's "take whatever state you find" — a cycle whose goal is a true belief must not get there by reversing the rule that made the belief honest. **In the constructor, not the lifespan**, even though lifespan is the house pattern for best-effort boot work: `RadioHolder.rebuild` and `_restore` each construct a fresh backend, so a lifespan step would let a **live backend swap reopen the same gap**. **Best-effort, unlike the fail-loud `apply_port_settings` twenty lines above, and the asymmetry is the point** — that one fails when the serial *handle* is unusable and there is nothing to degrade to; this one fails when a good handle has a radio switched off at the far end, an ordinary Tuesday, and the failure is **representable** as the `None` the tri-state was built for. **The exception tuple is argued member by member**, including two exclusions: `Uvk5Timeout` is already converted upstream and is pinned by a test instead, because widening the tuple would *hide* a refactor that dropped the conversion; `ValueError` is loudly out, since the only way to get one is a typo'd constant. **No config key, and the residual is stated as accepted cost rather than oversight:** a restart always pulls the radio out of AM with no opt-out. A modulation-only key was rejected as **the sole member of an incomplete set** — no boot frequency, no boot tone, so an unattended airband monitor would return on the right demodulator and the wrong frequency, which is not a working station; the whole-shaped successor is a boot preset reusing `apply_preset`. **A failed assert logs and nothing more, for correctness not economy:** `null` renders as `—`, and `—` is **true**; a UI channel would explain why the value is unknown, it would not make it known. **Fail-first 1 red** — `assert None == 'AM'`, the defect in one line, asserting the belief **equals the radio's state** rather than equals `"FM"`, which would pass on a host that guessed right by luck. Two of the three repaired tests reach `None` via a *failed* assert rather than by resetting a fake, because resetting would fabricate a state production can no longer produce. `uv run pytest` **1981 passed, 5 skipped** (1973/5 before); `npm test` **101 passed, unchanged**. Findings named: `doctor` builds no tuner so `--key-test` keys with zero modulation knowledge, and **the EventHub does not exist at backend construction**, which blocks any boot-time diagnostic, not just this one. **No firmware, no UI, no API change, no bench claim.** | Accepted |
| [0156](0156-a-deaf-station-can-still-transmit.md) | **A deaf station can still transmit — broadcast FM over the dock (F8).** The UV-K5 carries a **second receiver**: a BK1080 commercial-FM chip, 64-108 MHz, on the same I2C bus as the BK4819 every other opcode drives, sharing the antenna front end and the audio amplifier and nothing else. `0x0850`/`0x0851` cannot reach it — those are BK4819 register access and the BK1080's registers are not in that space — so it has been in the Fusion image, working, and unreachable from the server. Zero prior mentions in this repo: **`BK1080`, "broadcast FM" and "88-108" have no hits anywhere**. Adds firmware `0x0879`/`0x087A`. **The cycle's expectation was wrong in the dangerous direction and the ADR is named after it:** turning this on puts the BK1080 on the speaker line the AIOC listens on, so the station stops hearing its own channel — and it **does not stop transmitting**. Traced rather than assumed: `RADIO_PrepareTX` has no broadcast-FM term at all, `MAIN_ProcessKeys` whitelists `KEY_PTT` **by name**, and `GENERIC_Key_PTT` is an explicit `goto start_tx` on the FM screen. So the radio transmits normally into a channel it cannot monitor, including the station ID guardrail 5 requires — strictly worse than the AM fault, where the over was at least silent. **`tx_ok` is carried and is ORTHOGONAL**, which is the point of carrying it: it reports the BK4819 demodulator, which the BK1080 does not touch, so a host must be able to read `state=ON` and `tx_ok=true` off one frame because that combination is real. **The census was re-run before a number was picked** — all three disagreeing sources, union of every claimed opcode — and `0x0879/A` appear in none; it also found the `frames.py:113-140` citation carried in 0149, 0150 and HANDOFF is now **stale** (`96-165`). **Hz on the wire, and off-raster frequencies REFUSED not rounded:** `0x0873` truncates sub-10 Hz because 10 Hz of a repeater channel is nothing, but 100 kHz of the broadcast band is **a whole adjacent station**. Band is on the wire because `FM_Band` is a **two-bit field** that turns a 4 into a 0 inside the assignment with no diagnostic; out-of-band also **underflows** `channel = freq - loLimit` in uint16. **Blanking uses three different sentinels**, because `0` is a real reading of `state` (OFF) and of `band` (87.5-108) but not of `freq_hz` — 0x0878's lesson applied field by field rather than copied. **Refused while keyed, one interlock with two halves:** `ACTION_FM`'s own guard is mirrored **including MONITOR**, but it cannot see the dock's REG_30 key (`Dock_ForceTx` never enters `FUNCTION_Select`), so `dock.c` refuses on `ctx->tx_on` too — the one new refusal a host test can prove. Not an exception to `0x0873`/`0x0877`: their state survives an over and this one does not, so applying anyway would read back ON for a receiver already being torn down. **The command path writes no flash, and the OFF leg does — stated as a correction, not an exception granted:** the brief's blanket no-EEPROM-write rule was written without `app.c:1761-1767`, which calls `FM_Start()` **write and all** ~5 s after any squelch close, armed by `functions.c:106-108`. The firmware persists FM behind the host and no command path can stop it; if OFF also declined, flash would sit at `CURRENT_STATE=3` and the radio would boot deaf for ever. Clearing a bit the firmware set is not routing a command through `SETTINGS_Save*`. **The screen follows the radio because TX keys off `gScreenToDisplay`** — leaving it alone would split host- and operator-initiated FM into two states that only look identical, which ADR [0137](0137-let-the-radio-be-a-radio.md) forbids; the dead keypad is the second reason. `gRequestDisplayScreen` was checked and is a **no-op from a UART handler** (consumed only at the end of `ProcessKey`), and `GUI_SelectNextDisplay`'s scan-clearing side effects are load-bearing. **`RADIO_SetupRegisters` mutes a running BK1080** and all four dock paths end in it, so one shared `Dock_RestoreFmAudio` helper — **no risk, not a mitigated risk**: the state needing it cannot exist until this opcode creates it. Its guard is **a grep, said plainly**, because `uart.c` is not host-compiled by design. **Fail-first twice:** an always-succeeds stub scored **18 failures**, and the goldens, binding-reached-once and read-back checks **stayed green** — recorded so nobody cites them as evidence; plus a free compile-time red from the eighth `dock_hal_t` member under `-Wextra -Werror`. One failure in that run was a **defect in the new tests**, recorded rather than quietly fixed. Green **144 checks, 0 failures** (98 before); goldens derived from an independent framer that reproduces both published F7 vectors first; `0x0874`/`0x0878` pinned byte-identical. FLASH **106,136 B / 118 KB (87.84%)**, +560 B, 14,696 B free; the `ENABLE_FMRADIO=off` build links clean, so `ERR_NO_HAL` is a real firmware shape. `uv run pytest` **1981 passed, 5 skipped, unchanged — no `radio_server/` code changed.** **Not flashed; no hardware claim** — three new `⚠ CONFIRM AT BENCH` items, including the OFF-leg erase duration, whose only source is a firmware comment. | Accepted |
| [0157](0157-a-station-that-cannot-hear-itself.md) | **A station that cannot hear itself — the server clears broadcast FM and knows it did.** [0156](0156-a-deaf-station-can-still-transmit.md) made a state *reachable* the server could not leave: the BK1080 holds the speaker line the AIOC listens on, so the station hears **nothing** on its own channel and **transmits normally anyway** (`RADIO_PrepareTX` has no broadcast-FM term — re-verified, the only `gFmRadioMode` in `App/radio.c` is the VOX gate). The firmware persists it to flash **behind the host** (`app.c:1761-1767` calls `FM_Start()` ~5 s after any squelch close), so a crash leaves the radio booting deaf with automatic ID still keying. **Verified first, and it shapes the cycle: F8 is NOT merged** — fork `main` is `4e1d9dc`, `merge-base --is-ancestor` says no, and F8 lives only on `5f4c581` behind open PR #6. All goldens were read from that commit; reading `main` would have produced *nothing*, not an error. So **no radio on earth runs F8**, which is why **the capability is EARNED, not configured**: `CLEAR_BROADCAST_FM` appears only after a radio has answered `0x087A`, because a static member in `SETVFO_CAPS` would have every station asserting a firmware generation nobody is running (guardrail 1). Any reply earns it **including a refusal** — a refusal proves the opcode exists; only silence earns nothing. **Re-earned on every reply, not once at boot**, because a boot-only probe would leave a radio switched off at startup missing the capability *for ever* with **no operator remedy** — a preset tap cannot conjure one back. It is the **first capability with no route**, said plainly in `api.md`: it reports what a radio has *proved*, not what the API will gate. **The request can only build OFF** — no action parameter exists — so \"cannot turn broadcast FM on\" is a property of the code, while the **reply parses every state including ON**: you can always learn the radio is deaf, you can only tell it to stop. Only the reply is in `_DISPATCH`, and that is not bookkeeping — without it `0x087A` decodes to `RawMessage` and every clear times out against a radio that answered. **Status is a tri-state BLOCK** (`PaState` precedent): `null` = the server does not know, `{on:false}` = it knows and the station can hear — rendering those the same is how a deaf station gets trusted. **`ERR_NO_HAL` maps to a definitive `on:false`**, not unknown: no `ENABLE_FMRADIO` means no BK1080 driver, so it *cannot* be deaf — the one status on this wire that is a certainty. **`tx_ok` is on the reply and deliberately thrown away**, because recording it would break ADR 0155's `modulation is None ⟹ tx_ok is None` invariant and have the key-up refusal read a flag from a different frame than the demodulator it names. **Two asserts, broadcast-FM first, each with its OWN try/except** — [0153](0153-one-frame-must-not-take-the-relay-down.md)'s lesson where it would bite *universally*, since `0x0879` times out on every radio today and a shared handler would silently cost every station the [0155](0155-a-restart-must-not-inherit-the-demodulator.md) assert as collateral. **Two ordering arguments were rejected on evidence and are recorded so they are not re-invented** — including the cycle's own first one (`Dock_RestoreFmAudio` restores audio for a BK1080 *already* running; it preserves deafness, it cannot create it) and a plausible dropped-frame theory (`dock.c` replies as its **last** statement, after the HAL returns, and the tuner is synchronous request/reply, so the erase is absorbed by the round trip). **Boot and backend-switch are different costs and get different sentences:** `build_app` is plain `def` and runs before uvicorn's loop exists, so boot is *startup latency*; `rebuild` called the factory synchronously inside `async def` and `_restore` again on rollback, so that one **froze the loop, twice** — both now `asyncio.to_thread`, the `apply_preset` idiom. Timeout not shortened and no config key added: any number is a guess and would truncate exactly the case that matters. **No UI, and not for scope** — the server never re-reads, so a row would say \"off\" while an operator's own FM keypress left the station deaf. **Fail-first twice, with the weak run labelled weak:** run 1 was 3 *collection errors* (import-level, proves names are absent, not that behaviour is wrong); run 2 after the wire layer was **18 failed / 1996 passed** and behavioural. **Four new tests that passed VACUOUSLY in the red run are named as not evidence**, as are every frames golden. Green **2014 passed, 5 skipped** (1981/5 before, +33); vitest **101 passed, unchanged**. Both exact-set capability assertions pass **untouched** — predicted by the earned design and verified by diff, so the honest option was also the smaller one. **No firmware, no flashing, no bench claim, no ON path.** | Accepted |
| [0158](0158-the-host-refuses-to-key-a-station-that-cannot-hear-itself.md) | **The host refuses to key a station that cannot hear itself — the transmit interlock, host half.** [0157](0157-a-station-that-cannot-hear-itself.md) built the instrument: a tri-state `broadcast_fm` block saying whether the radio's second receiver — the BK1080 that holds the speaker line — is running. This spends it. `RADIO_PrepareTX` has no `gFmRadioMode` term, so a deaf radio transmits normally and the automatic station ID goes out into a channel nobody is monitoring; the firmware will not stop it, so the host must. **One predicate, `refuse_if_deafened` in `base.py`, and the shared helper is the deliverable** rather than an accident of factoring: the message is what ships, and two copies of a message is exactly how two causes collapse into one undiagnosable "cannot transmit". `AiocBaofeng` and `MockRadio` both consult it. Only a definitive `on=true` refuses — a null block never does, the `tx_ok` rule for the `tx_ok` reason, pinned both ways at the predicate, the backend, the API, both bridges and the browser. It goes **first** in `_key_on`, ahead of the channel re-assert: cheaper than the step it displaces, severity-first as `__init__` already argued for the boot asserts, and — the deciding reason — a re-assert that raises its own `TuneError` must not be able to mask the worse fault. Nothing else changed to carry it: `RadioUnavailable` already reached the 503 handler, both bridges' except ladders, `Controller._keying` and `TxSession`'s unwind, and that those worked untouched is asserted rather than assumed. **Two things this records that the brief did not expect.** F8 **merged** to fork `main` at `d086a23` 75 minutes before ADR 0157 landed, so that ADR's "no radio in existence runs F8" was true when run and stale when merged — the earned-capability decision is unaffected, but the sentence must not be quoted forward, and the firmware successor is unblocked rather than blocked. And this interlock **latches**: nothing re-reads the block, so clearing broadcast FM at the radio does not clear the refusal until the server restarts. First refusal here an operator cannot fix from the front panel; stated in the message, the docs and the UI. Finding 7's recorded fix was a no-op — the protocol already declared the method; **nothing ever checked the protocol** — so the boot assert now guards on `isinstance(tuner, Uvk5Tuner)` (verified empirically on 3.12.13, data members included), skipping to `null` and a WARNING naming the type, never to "off", and deliberately NOT applied to the demodulator assert, where an all-or-nothing fat-protocol check would resurrect ADR 0153's skipped-sibling bug. TalkControl gains the cause and, in its own sub-line, its own limit: this cannot see broadcast FM switched on at the radio's keypad, so an enabled Talk button is not proof the radio is hearing. 13 behavioural reds across two bridges, a controller, both REST key paths, `TxSession` and the backend, plus 5 of 9 browser reds; the vacuous passes are named, not counted. **No firmware, no flashing, no bench claim, no ON path.** | Accepted |
| [0159](0159-the-radio-refuses-to-transmit-while-deaf.md) | **The radio refuses to transmit while deaf — the transmit interlock, firmware half (F9).** [0158](0158-the-host-refuses-to-key-a-station-that-cannot-hear-itself.md) built a host gate that cannot see the front panel and dies with the process; this is the half that works with no host at all. Three claims were re-read rather than inherited, and one overturned the plan's worry: the AIOC's DTR line drives `GPIO_PIN_PTT`, the **same pin** the rubber button drives, so `GENERIC_Key_PTT` -> `gFlagPrepareTX` -> `RADIO_PrepareTX` gates both — and the FM screen's `goto start_tx` is **not** a bypass, it lands on that same flag. Dock `REG_30` keying **is** a bypass and is **not covered**; a term in `Dock_ForceTx` was *rejected, not deferred*, because `0x0850` sends no reply and a silent no-RF is worse than the diagnosis it replaces (0158 R3 stays open, and is now the only ungated keying authority). One clause in `RADIO_PrepareTX`; `0x087A` `flags` gains **bit 1** — a bit, never a ninth byte, because three consumers hard-require the 8-byte reply and one is the deployed `frames.py` this cycle may not touch. It reports **blocking, not readiness**, since `flags` blanks to 0 and pre-F9 firmware answers 0: an unmeasured field must never lock a transmitter. Bit 0 keeps its published meaning; read `will_key = TX_OK && !FM_BLOCKS_TX`. **One predicate, three consumers** (gate, flag, tests) in a new include-free header — F7 reported `TX_OK` through a hand copy of the AM gate, and doing that twice would let a host be told the radio will key after the radio refused. **Revises the brief:** Fusion-only and opt-IN, not the AM gate's opt-OUT idiom, because 0158's *correction, not regression* was argued for an unattended station and does not reach a handheld operator who deliberately opened the FM radio and deliberately pressed PTT; the fork's guardrail 5 now names F9 as its one confined exception rather than standing as a rule the tree violates. Corrects ADR 0156 decision 6 (*'there is no bidirectional interlock here'* — now stale) and re-prices 0158's latch from safety mechanism to **UI accuracy problem**, since it can now refuse a key-up the radio would allow. Host tests **144 -> 155 dock checks + 6 predicate checks across three compilations (161)**; four fail-first runs — 4 failures, 1, 5, and a CMake `FATAL_ERROR` — with the vacuous passes named, not counted, and a defect the fail-first process found in the *harness* (`\|\| exit 1` hid two compilations) recorded rather than quietly fixed. Honest limit stated: `radio.c`/`uart.c` are not host-compilable, so that the gate is *called* is a **grep, not a test**. Publishes the `0x0879` **OFF** vector (0158 R6) that F8 never did though it is the only `0x0879` the host sends, and adds F8/F9 rows to three level tables that all still stopped at F7. FLASH **106,152 B / 118 KB (87.85%)**, **+16 B**, 14,680 B free; the interlock-off build links at **exactly 106,136 B**, F8's figure to the byte, so it compiles out. `uv run pytest` **2043 passed, 5 skipped, unchanged — no `radio_server/` code changed.** **Not flashed; no hardware claim** — one new `⚠ CONFIRM AT BENCH` item, and `BENCH.md` §9's existing expectations rewritten because a run against them as they read at F8 would report the feature as a fault. | Accepted |
| [0160](0160-the-bench-answers-back.md) | **The bench answers back — eleven cycles of firmware reasoning meet a radio, and most of it holds.** ADRs 0148→0158 each ended *"nothing flashed, no hardware claim"*; the operator flashed **F8 by hand**, and this is the first cycle in the arc to run on hardware. **Pre-flight killed the brief's premise twice:** the deployed checkout was on `transmit-power` @ `4ad0a87` = **ADR 0146, twelve ADRs behind master**, running five days with none of the code under test — *a bench cycle's first job is to prove the thing under test is the thing deployed* — and **`docs/BENCH.md` does not exist here**, it is the fork's file (not created; a same-named twin would guarantee the cross-repo confusion ADR 0148 is about). **Gate A needed no instrument:** master's `__init__` already sends `0x0879` then `0x0877` (`aioc_baofeng.py:333-334`), so deploying master and reading the journal **is** `PROTOCOL.md`'s "ask the command" ladder — both answered, `set_modulation`+`clear_broadcast_fm` appeared, dock reports `F4HWN v5.7.0` (the Fusion *base*; the opcodes, not the string, distinguish F6/F7/F8). **F8 confirmed end to end.** **Raw wire bytes confirm F7 at the bit:** `0x0878` = `7808040000010100` (AM, flags `0x00`, **tx_ok False**) vs `7808040000000001` (FM, flags `0x01`, **tx_ok True**). **AM audio traverses the AIOC — the load-bearing item, proven as two separate claims** so neither leans on the other: (a) 88.1 MHz demodulated AM **RMS 9653/8738/8298** vs FM 15224/13742/12903, dead channels 0.0; (b) a **true** AM airband sweep, 760 channels at 25 kHz with **738 reading exactly 0.0**, finding voice at **126.100 (RMS 13351, speech 0.983)**, 124.775, 133.400 — and the reciprocal control that settles it, **124.775 AM → 7405.7 / FM → 0.0**, same frequency, adjacent dwells, only the demodulator moved. **Keying ran presence→absence→presence in one setup** (the station was moved to 445.800 because a UHF-only witness aimed at a 2 m station reads "no RF" regardless — *not weak evidence, no evidence*): FM **200**, witness 69%/RMS 1298.5 → AM **503** with the `VFO_STATE_TX_DISABLE` reason, witness **0%/0.0** → FM **200**, 67%/1275.6. **The station survived the arc.** Broadcast FM ON returns `on=True` **and** `tx_ok=True` off one frame — ADR 0156's dangerous pair, real; the **three blanking sentinels** confirmed field by field (`ERR_OFF` → state/band `0xFF`, hz `0`), and the **OFF-leg flash stall measured at ttfb ≤ ~0.1 s**, settling a fork placeholder whose only source was a firmware comment. **Items 8 and 9 are NOT RUN, and that is the finding:** `broadcast_fm` is written only in `__init__`, and the boot assert **clears the very condition the gate tests**, so FM switched on at the panel afterwards leaves the block `{on:false}` and `tx_ok` true — **the gate does not fire, it transmits blind.** Verifying the host refuses *was* the test; the measured answer is that it cannot see. Confirms ADR 0158's own finding 1 on hardware **without putting a carrier on air**. Also: **0155 holds** (AM left on the radio → server *states* FM, never adopts); **0157's boot assert works against a genuinely deaf radio** (`hz` read back as `104300000`, vs `64000000` on a clean boot — the block is measured, not fabricated); **`/radio/select` answers 200 throughout but a `/status` inside the window blocks 9.75 s of a 10.47 s rebuild** — "no longer blocks" is true as "no longer fails", false as "answers promptly"; **`POST /mode` AM → 500** confirmed; **the preset highlight lies**, every compared field identical while modulation goes FM→AM, and that preset's own `honoured` list contains `set_modulation` — and `/presets` carries no `modulation` key, so the fix spans server *and* browser. Findings: deployment drift unchecked by anything; **`status.rssi` is `null` on `baofeng`** — no host-side signal-strength read on the deployed mode at all; no host route to clear broadcast FM; `server-notes.md` wrong three times in one day (firmware, frequency, D-STAR). **No `radio_server/` code changed, nothing fixed, nothing flashed, fork untouched.** One addition: `scripts/bench/broadcast_fm_on.py`, an instrument for two frames the production codec deliberately cannot emit — opened via the transport's own `_default_serial_factory`, because a plain `serial.Serial(...)` asserts DTR and **DTR is this cable's PTT line** | Accepted |
| [0162](0162-broadcast-fm-must-not-reach-the-bridges.md) | **Broadcast FM must not reach the bridges — and the read [0161](0161-the-host-asks-the-radio.md) said did not exist.** ADR 0161 measured a station in broadcast FM relaying a commercial station to `/audio/rx`; one `AudioHub` feeds three subscribers and two of them are bridges, so that broadcast goes onto Mumble and onto a D-STAR reflector whose far end may be somebody else's repeater. F9 stops the local transmitter and knows nothing about an internet link — **97.113(b)**, not untidiness. The asymmetry is the design: the **browser should** hear it (that is the feature), the bridges must not. **The mute lives in each bridge's relay loop, not at the hub, and the brief was wrong to ask for the hub**: [0085](0085-mumble-rx-guard.md) decided this exact question for this exact asymmetry with the same two exemptions — browser Listen and the recorder — so those stay untouched *by construction*. Three stronger reasons than precedent: a hub can only deliver-or-not, but D-STAR needs *drop this frame **and reap the outbound over*** (a bare `continue` leaks an over that nothing can ever close, because FM arrives at full rate so the hang never fires and the level gate never closes — and since `send_operator_audio` latches on `_mode`, the tidy version jams the browser's own reflector path in the same edit); `publish` is the pump's hot path; and the counters live per bridge. A test pins the set of `subscribe()` sites so a **fourth** subscriber fails CI, which is the hub seam's one real advantage answered rather than dismissed. **Two things measured rather than carried, and both changed the cycle.** First, **`on=True` is unreachable on F8/F9** — `Dock_FmOff()` clears `gFmRadioMode` as its first statement, unconditionally, and the reply reads state back from that variable — so the mute ships **armed and blind**, and the ADR says so in its first paragraph rather than shipping a Part 97 control with a green suite and no on-air effect. Second, **0161's "the wire offers no read that is not also a repair" is wrong**: `Dock_SetFm` checks `TUNE && !gFmRadioMode → ERR_OFF` **before** the band limits, so an out-of-band TUNE returns `ERR_OFF` (hearing) or `ERR_BAND` (deaf) having touched nothing. Both statuses were already decoded, the guard is in F8, no firmware needed — and `broadcast_fm_on.py --status`, described in its own help as *"a read probe"*, had been sitting in `scripts/bench/` since [0160](0160-the-bench-answers-back.md) unrun. **The probe lands on the key-up path only** (0 Hz, not the fork's own 64 MHz vector: 64.0 is out of band only under band 0 and legal on band 3), closing **0161 finding 5** — a pre-key-up clear rescued a deaf station and could never say so; `rescues` now rides in the status block. It **writes nothing and raises nothing, structurally**: `clear_broadcast_fm` stays the only writer, so no probe result can reach `refuse_if_deafened` — because probe-says-deaf then clear-declined-with-`ERR_TX` would refuse a **busy** station's key-up, which is 0161's defect one cycle later on the same path. Counters carry a **tri-state**, since `rx_deafened: 0` beside `deafened: null` means *nobody asked* and beside `false` means *measured hearing*. **BENCH: refuse-or-clamp answered first, because everything else was built on it** — `ERR_OFF` vs `ERR_BAND` differ in **exactly the status byte**, a repeat is byte-identical, and the following OFF reported the receiver still on 104.3 MHz; the fork's host tests *force* `ERR_BAND`, so that branch had never executed anywhere before this run. **The relay, on real broadcast audio:** `None` → Mumble 4111.5 RMS + **197 AMBE frames to the reflector**; `{on:true}` → **0.0 and 0 frames** with the browser unchanged at 122 880 B; `{on:false}` → relays again. **The 0x0874/0x0878 diff the brief asked for is a null result** — byte-identical with the BK1080 running — which is the point, and which is why the brief's fallback rule needed correcting; it did surface that **`0x0878` reports `tx_ok=1` on a station F9 will refuse to key** (0159 R4, answered). Fall-through measured against a genuinely keyed radio (`ERR_TX`, witness caught the carrier), acceptance **9 of 9 attempted PASS**. **And the deploy guard 0161 certified turned out to be one-directional** — `--is-ancestor` returns 0 for *behind* as well as *on*, and a stale `origin/HEAD` reset had left the box six commits behind reporting healthy. `uv run pytest` **2088 passed, 5 skipped** (from 2070); vitest **12 files, 116 tests**, unchanged. Red run **8 behavioural failures**, after a first attempt was discarded for putting a declaration in a `runtime_checkable` protocol and cascading | Accepted |
| [0163](0163-a-cadence-for-the-probe.md) | **A cadence for the probe, and what the probe actually answers.** ADR 0162's relay mute shipped **armed and blind** — `Dock_FmOff()` clears `gFmRadioMode` first, so a key-up-measured block can never say `on=True`. The brief asked the right gating question first: does the probe work in the state it exists to detect, or does `ERR_TX` (refused while `gCurrentFunction` is TRANSMIT **or MONITOR**) make it permanently blind? **Measured before anything was built. M1: 35/35 `ERR_BAND` over 35 s with the second receiver running, zero `ERR_TX`** — `gCurrentFunction` during broadcast FM is `FUNCTION_FOREGROUND` (`fm.c` never calls `FUNCTION_Select`; `ACTION_FM`→`RADIO_SetupRegisters(true)` sets it), and `FUNCTION_MONITOR` is *unreachable* in FM mode. **M4** caught a front-panel `F+0` live: 47×`ERR_OFF` → 93×`ERR_BAND`, one transition. **M3 changed the cycle:** `gFmRadioMode` has four writers and none is `APP_StartListening`, which tears the BK1080 down for a real over *without clearing the flag* — so with the witness keying 1000 Hz on the station's own channel the AIOC recovered it at **power 0.995** while the probe still said `ERR_BAND`. **`ERR_BAND` = "FM is selected", not "deaf right now"**, so the mute also withholds real overs; that trade is stated in the ADR's own voice rather than discovered later. **The prerequisite the brief could not know:** `Uvk5Transport` had **no wire lock**, and both the probe and the clear match `isinstance(m, BroadcastFmReply)` — so a poll's refusal could be handed to a key-up's clear and **refuse the key-up**, ADR 0161's defect rebuilt by the cadence (ADR 0125's safety argument relied on register-specific matching and does not transfer). One frame in flight now; `wire_timeout=None` blocks so the key-up always wins, the poller passes `0` and skips. **Failure rule argued on its own terms, not inherited from `tx_ok`:** failing open puts a broadcast on someone's repeater, failing closed mutes on every over (unknowns are *routine* — the dock refuses `0x0879` whenever we key) — so **a non-answer is not a state transition**; the last definite reading is held and its age surfaced. `BroadcastFmPoller` (`PolledGate`-shaped, ADR 0125; 2.0 s = the leak window), polls **only while a bridge relays**, and **never writes `tuner.broadcast_fm`** so no poll can reach `refuse_if_deafened`. **Bench: the mute fired on a real radio for the first time** — browser 4 571 520 B / RMS 4221.2 unaffected while **2299 frames** were withheld from Mumble; **`rescues: 0→1` on hardware**, closing ADR 0162's unstageable gap; 20 tunes vs the cadence = 0 failures. **Corrections:** ADR 0161's "`MONITOR` makes that window far wider than during an over" is **wrong** (an ordinary open squelch is INCOMING/RECEIVE), and ADR 0162's `0x0874` control was **vacuous** (empty `0x0873` → `ERR_SHORT`, all fields zeroed). **Reported, not smoothed:** `auth` failed 1 of 3 acceptance runs (detail not captured, no cause claimed), and **acceptance does not cover the cadence** (its restart drops the link, so no bridge, no poller). A second process on the AIOC tty **killed the dock link and the transport never recovered** — the cadence's own `deafened_unknown` was the only visible symptom. Red run 23F/199P; `uv run pytest` **2117/5**; vitest 12/116. | Accepted |
| [0164](0164-the-on-path.md) | **The ON path: broadcast FM becomes a thing an operator can use.** Closes **ADR 0158 R4**, **ADR 0160 finding 3** and **ADR 0161 finding 2** — one missing piece seen from three sides, open four cycles: until now *pressing Talk was the operator's only way out of broadcast FM from the host side*, which on an unattended LAN station means nobody. `POST /broadcast-fm` in both directions, a `Radio`-level `clear_broadcast_fm`, and a SECOND RECEIVER card. **Two frame classes, and neither can do the other's job:** `ClearBroadcastFm` keeps its no-action-parameter design so the pre-key-up rescue *physically cannot deafen the station it is about to key* (ADR 0157's property, kept where it was load-bearing), and `SetBroadcastFm` expresses ON/TUNE only. **Validation splits where the firmware splits it:** host checks the 100 kHz raster and the band *number* before the wire; the band's *limits* stay the radio's verdict, because `dock.h` calls a second copy of a hardware table a drift hazard and that reaches one layer further. Refuse, never round. **A refusal that arrives is an answer; only silence is unavailability** — `TuneBusy` is a `RadioUnavailable`, so a courtesy refusal was a **503 purely by inheritance**; new `RadioBusy` in `base.py` (the API must not import a backend's exception module to pick a status code) completes the trio 501/409/503. **The relay mute arms BEFORE the wire and disarms only on proof** — the ON exchange measured 101 ms and the cadence polls every 2.0 s, so arming off the reply would relay ~2.4 s of a commercial station onto somebody else's repeater; a *hold*, not a write, so a mid-flight poll cannot disarm it and a refusal restores nothing. **The brief's own third consequence was wrong and the bench settled it:** the transmitter is NOT refused on any host path — `_clear_if_deafened` runs first in `_key_on`, so **B2 measured `POST /ptt` returning 200 with `rescues: 0→1`**; a transmission *takes the receiver back*. **Bench:** ON → 200 in 101 ms, read-back `blocks_tx: true` (F9 on the wire, fourth cycle running), mute armed in the same 2 s sample — browser 2 208 000 B / RMS 3957 unaffected while **1150 frames** were withheld from Mumble, **zero** unknowns; TUNE to 98.5 MHz on a running receiver; OFF in 106 ms and both links resumed; and four refusals against the real radio (off-raster 422 host-side, **64.0 MHz 422 carrying the radio's own `ERR_BAND`**, band 4 422, tune-while-off 409). **Two defects only the bench could find:** the ON capability **could never be earned** (the flag was set only inside the method a route gated on that capability would call — the card would never have rendered, and both pytest tests passed by reaching it through the one call that cannot run in production), and the operator's own OFF **was counted as a rescue** (the cadence arms the rescue flag every 2 s, so a published number documented as key-ups-rescued counted something else — ADR 0159's *a flag must not lie*, one layer up). **UI: nothing is hidden.** Consequences 1-3 sit in the card at all times next to a two-step arm/confirm; consequence 2 is worded to M3 (*"withheld even though the radio hears them"* — **"deaf" is the thing M3 disproved** and a test asserts the word is absent); the repurposed-keypad warning appears only while FM is ACTIVE, for whoever walks up to the radio later. The `M`-overwrites-a-channel claim is **not** in the record — ADR 0160 photographed the prompt, not the overwrite — so it is not claimed. **New findings:** browser RX is squelch-gated (0 bytes on a quiet channel is not a fault), and **the API token is written to the journal in cleartext** on every `?token=` audio-socket connection. `clear_broadcast_fm` added to, never renamed: the name is published and a client may branch on it. `uv run pytest` **2169/5**; vitest **13/131**; red run 50F/2115P. | Accepted |
| [0161](0161-the-host-asks-the-radio.md) | **The host asks the radio instead of remembering — the transmit interlock's two halves, finally connected, and proven on a radio.** [0159](0159-the-radio-refuses-to-transmit-while-deaf.md) put the interlock in the firmware and reported it on `0x087A` bit 1; [0160](0160-the-bench-answers-back.md) then measured that the host **cannot see the state at all** — `tuner.broadcast_fm` is written in one place, the boot assert, and that assert *clears the very condition the gate tests*. So the radio held a live answer nothing read while the host acted on a boot-time one. `frames.py` gains `FLAG_FM_BLOCKS_TX`, `fm_blocks_tx` and `will_key`, goldens transcribed from `test_dock.c:1405` at fork `d903881` — and **the brief's "fork main" is one merge optimistic: PR #7 is still OPEN**, `main` is F8. `_clear_if_deafened` replaces the attribute read at the head of `_key_on`, because **the wire offers no read that is not also a repair**: `0x0879` has no read-only action, `ClearBroadcastFm` builds only OFF, and state is reported *after* acting — so the one frame that answers *is this station deaf* is the frame that gives it its ears back. A key-up on a deaf station is therefore **repaired and allowed**, not refused. Gated on the *earned* `CLEAR_BROADCAST_FM`, so firmware that cannot answer pays a set-membership test rather than 3.0 s per over. **Not on a poll**, three reasons each sufficient: a GET that mutates would fight an operator at the front panel; `ERR_TX` would blank the block *during* every over; and one serial link already carries tuning traffic. **ADR 0158's latch is dropped**, recorded as a reversal rather than an about-face — it was never a mechanism, only the emergent property of never re-reading, and with the firmware refusing, a host lockout that cannot clear stops protecting anything and starts refusing key-ups the radio would allow. Wording in three places stops claiming the host is the thing refusing, branching on bit 1 because the consequence is a property of the *image*. **Found while wiring it up:** `RadioUnavailable` inside the `/audio/tx` frame loop was caught by **nothing**, so every refusal reason in this arc reached the 503 body, `tx_failed` and both bridges — and never the browser, which showed *"Transmit connection dropped."* for three cycles; the socket now sends `{"status":"refused","reason":…}` before closing, the mechanism already used for `busy`. **Fail-first in two runs, because run 1 was worthless:** 33 failures that were all *a name is absent*, which buried the evidence rather than being it; declarations added, then **16 behavioural failures**. Golden mutations 3 / 12 / 2, with the wrong-bit run's weakness named (vectors whose `flags` is 0 or 1 read alike under either mask). **BENCH, on the deployed station: F9 confirmed on the wire** (`flags=0x03`) — the brief asserted it, guardrail 1 measured it — and **ADR 0159's headline claim met a radio for the first time: witness RMS 1065.9 → 0.0 while deaf → 965.5**, carrier, no carrier, carrier. Three key-ups produced **exactly three** frames, each logged *before* the tune. **ADR 0160's open question settled: the RX pump DOES relay broadcast-FM audio** — 3841.2 RMS and **768 000 bytes** against controls of **zero bytes**, measured through a temporary tuner-less instance because the boot assert exists to destroy that state. **And the bench found a defect this cycle introduced**, which is what a bench is for: `ERR_TX` fires when `gCurrentFunction` is TRANSMIT **or MONITOR** — an open squelch, most of an active QSO — and treating it as a fault made a busy receiver into a station that would not transmit, taking the Part 97 station ID down twice before anything noticed. Fixed with a `TuneBusy` subclass that keeps the last reading and does not refuse: an unmeasured field must never lock a transmitter. `uv run pytest` **2070 passed, 5 skipped** (from 2043); `npx vitest run` **12 files, 116 tests** (from 11/110). Station restored to 147.555, both config files byte-identical, `tune_persist` off before any sweep; **the box is deliberately left ahead of master**, with the SHA, the PR and the redeploy command written down — and the deploy-state guard *run* against that state, which showed the documented snippet reports nothing and only `merge-base` answers, by exit code | Accepted |
[ADR 0123]: 0123-uvk5-v3-rx-firststart-loop-harness.md

## The live linking arc

Linking a repeater/base to a remote voice channel is done over **Mumble**, not the reverted M17 stack.
The arc is ADR **0041** (single link) → **0042** (multiple servers, DTMF-selectable, one active) →
**0043** (ungated disconnect) → **0045** (audio-correctness fixes) → **0049** (real-time DTMF mute +
Mumble→RF keying yield) → **0050** (the web UI doubles as a Mumble client). Config lives under
`[mumble]` / `[[mumble.servers]]`; see
[configuration.md](../configuration.md) and the annotated [`radio.toml.example`](../../radio.toml.example).
